# ShellX Motion Debug API commands

> Generated from `schemas/debug.json` and `fixtures/debug/coverage.expected.json`. Do not edit this file by hand. Run
> `pnpm docs:debug-api` to regenerate it and `pnpm docs:check` to detect drift.

Current contract: **300 commands** across **7 domains**.
**300** of them publish an argument contract (9 of those take no arguments at all).

The required tier is the minimum permission contract for the command. `Mutates` is the
schema's mutation classification; commands that render or write derived artifacts can therefore
be classified as mutations even when the source package itself is unchanged. For transport and
authentication examples, see [`DEBUG_API.md`](DEBUG_API.md).

Arguments below are the exact keys each command reads, and that is checked mechanically:
`scripts/debug-arg-coverage.ts` reads every debug handler and fails the build if one reads an
argument this table does not list. `Aliases` are additional accepted names for the same argument.
Large or shared value sets are named in `Allowed values` and listed once under
[Argument value enumerations](#argument-value-enumerations). Every allowed-value list here is
enforced on the wire, not merely advertised.

## Debug coverage gate scope

`pnpm debug:coverage` is a static parity gate for the fixture-defined Workbench surface map, not a
general test-coverage command. It requires each named surface to map at least one command and the
map's unique command IDs to exactly equal the fixture's required-command list.

Its exact command scope is **160 / 300 registered Debug API commands**: 160 fixture-required command IDs across 9 named surfaces (`packages`, `timeline`, `tracking`, `templateInspector`, `preview`, `receipts`, `assets`, `brand`, `prompt`). The other 140 registered commands are outside this gate's scope.

It does not execute handlers, prove action-discovery completeness, validate receipts, or measure
unit or line coverage. The gate's JSON output repeats its scope, numerator, denominator, and fixture
parity so a passing result cannot be read as all-command execution coverage.

The same contract is available at runtime: `motion.actions.guide` and `motion.actions.plan`
return each planned step with its arguments, direct required set, alternative required groups, and allowed values attached.
A reviewed `Purpose` appears only where an agent-facing semantic distinction has been audited;
it is sourced from the same action-catalog wording used by action plans and MCP tool listings,
not maintained as separate documentation prose.

## agent (13)

| Command | Required tier | Mutates |
|---|---|---:|
| [`motion.prompt.queue`](#motionpromptqueue) | `read_motion` | no |
| [`motion.prompt.cancel`](#motionpromptcancel) | `draft_motion` | yes |
| [`motion.prompt.retry`](#motionpromptretry) | `draft_motion` | yes |
| [`motion.actions.find`](#motionactionsfind) | `read_motion` | no |
| [`motion.actions.guide`](#motionactionsguide) | `read_motion` | no |
| [`motion.actions.plan`](#motionactionsplan) | `read_motion` | no |
| [`motion.actions.panel`](#motionactionspanel) | `read_motion` | no |
| [`motion.agent.panel`](#motionagentpanel) | `read_motion` | no |
| [`motion.agent.health`](#motionagenthealth) | `read_motion` | no |
| [`motion.agent.snapshot`](#motionagentsnapshot) | `read_motion` | no |
| [`motion.agent.transcript`](#motionagenttranscript) | `read_motion` | no |
| [`motion.agent.revision.plan`](#motionagentrevisionplan) | `write_local` | yes |
| [`motion.prompt.run`](#motionpromptrun) | `draft_motion` | yes |

### `motion.prompt.queue`

Tier `read_motion` · mutates: no

**Purpose:** On Linux, read stable receipt-derived prompt annotations and follow-ups; it never observes a live provider.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | no |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.prompt.cancel`

Tier `draft_motion` · mutates: yes

**Purpose:** On Linux, write a stable receipt-derived prompt-cancellation annotation; it never stops a live provider.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | yes |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |
| `receiptId` | `string` | yes | `id` |  | Queued or running prompt job receipt id to cancel. |
| `reason` | `string` | no |  |  | Optional cancellation reason for receipt evidence. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.prompt.retry`

Tier `draft_motion` · mutates: yes

**Purpose:** On Linux, write a stable receipt-derived prompt-retry annotation; it never starts a live provider.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | yes |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |
| `receiptId` | `string` | yes | `id` |  | Failed or cancelled prompt job receipt id to retry. |
| `reason` | `string` | no |  |  | Optional retry reason for receipt evidence. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.actions.find`

Tier `read_motion` · mutates: no

**Purpose:** Search the action catalog for a matching request and return a match or near misses without planning or executing work.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `request` | `string` | yes |  |  | Action id or natural-language request to match against the action catalog. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.actions.guide`

Tier `read_motion` · mutates: no

**Purpose:** Return the matching action's typed call sequence and argument contracts as guidance without executing it.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `request` | `string` | yes |  |  | Action id or natural-language request. The plan returns each step's call with its argument contract. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.actions.plan`

Tier `read_motion` · mutates: no

**Purpose:** Plan the matching action or multi-phase workflow, including its typed steps, required tiers, and verification, without executing it.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `request` | `string` | yes |  |  | Action id or natural-language request to plan a command sequence for. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.actions.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show grouped actions, permission tiers, prompt commands, and suggested prompt-run follow-ups.

Takes no arguments.

### `motion.agent.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show local CLI agent selection policy, adapter command shapes, safety guarantees, and prompt follow-ups.

Takes no arguments.

### `motion.agent.health`

Tier `read_motion` · mutates: no

**Purpose:** Probe local CLI subscription agent readiness without mutating packages.

Takes no arguments.

### `motion.agent.snapshot`

Tier `read_motion` · mutates: no

**Purpose:** Read a compact path-free snapshot from host-approved package and receipt roots, with only the authenticated caller's jobs.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | no |  |  | Optional Motion package root. Caller paths must be inside host-approved snapshot package roots and contain no symlink traversal. |
| `receiptsRoot` | `string` | no |  |  | Optional receipt root. Caller paths must be inside a trusted host receipts root. |
| `request` | `string` | no |  |  | Optional compact action find/guide/plan selector, at most 256 Unicode scalars. It is not echoed in the snapshot. Job facts are complete only when the host supplies an authenticated owner principal; otherwise jobs are empty and inexact with a warning. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.agent.transcript`

Tier `read_motion` · mutates: no

**Purpose:** On Linux, read stable prompt and agent receipt transcripts as panel-ready redacted evidence.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | yes |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |
| `receiptId` | `string` | no | `id` |  | Prompt receipt id whose transcript should be read. |
| `receiptPath` | `string` | no | `path` |  | Prompt receipt path; must resolve inside receiptsRoot. |
| `limit` | `number` | no |  |  | Maximum transcript entries to return; must be a non-negative integer. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.agent.revision.plan`

Tier `write_local` · mutates: yes

**Purpose:** Build and optionally write a trusted-root revision plan from supplied quality and contact-sheet evidence; it does not execute the proposed revision.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageId` | `string` | yes |  |  | Motion package id the revision plan applies to. |
| `templateId` | `string` | no |  |  | Optional template id the revision plan applies to. |
| `sourceJobId` | `string` | no |  |  | Optional prompt/agent job id that produced the package or render. |
| `planId` | `string` | no |  |  | Optional deterministic revision plan id. |
| `createdAt` | `string` | no |  |  | Optional deterministic ISO timestamp for the revision plan. |
| `receiptsRoot` | `string` | no |  |  | Receipts root used to resolve quality receipt ids. |
| `qualityReceiptId` | `string` | no |  |  | Quality receipt id to include as critique evidence. |
| `qualityReceiptIds` | `array` | no |  |  | Quality receipt ids to include as critique evidence. |
| `qualityReceiptPath` | `string` | no |  |  | Path to a quality receipt JSON file. |
| `qualityReceiptPaths` | `array` | no |  |  | Paths to quality receipt JSON files. |
| `qualityReceipt` | `object` | no |  |  | Inline shellx-motion receipt@1 quality-check receipt. |
| `qualityReceipts` | `array` | no |  |  | Inline shellx-motion receipt@1 quality-check receipts. |
| `contactSheet` | `object` | no |  |  | Inline contact-sheet critique evidence with status and notes. |
| `contactSheetPath` | `string` | no | `contactSheetFile` |  | Path to a contact-sheet critique JSON file. |
| `planPath` | `string` | no |  |  | Optional output path for the written revision plan JSON. |
| `outputPath` | `string` | no |  |  | Alias for planPath. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.prompt.run`

Tier `draft_motion` · mutates: yes

**Purpose:** Run a host-injected agent for a natural-language request and record its prompt receipt; raw-request retention additionally requires Linux's stable receipt purge capability, and proposed commands execute only when explicitly requested within the caller's grant.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `request` | `string` | yes | `prompt` |  | Natural-language instruction handed to the host-injected local agent runtime. The command returns capability_unavailable when the host has not injected one. |
| `packageId` | `string` | no |  |  | Package id recorded in the prompt receipt. Defaults to `unknown`. |
| `agentId` | `string` | no |  |  | Agent adapter to run; the default adapter when omitted. |
| `cwd` | `string` | no |  |  | Working directory for the agent; must be inside a trusted prompt working root. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror for the prompt receipt. |
| `executeAgentCommands` | `boolean` | no |  |  | Execute the debug commands the agent proposes. Without this the run only records proposals and changes nothing. Defaults to `false`. |
| `retainRawRequest` | `boolean` | no |  |  | Keep the raw prompt text in the receipt; requires rawRequestDeleteAfter, rawRequestPurpose, and Linux's stable receipt purge capability. Unsupported hosts refuse before writing a prompt receipt. Defaults to `false`. |
| `rawRequestDeleteAfter` | `string` | no |  |  | ISO timestamp after which Linux stable receipt reads redact the raw prompt and rewrite the stored receipt without it. Copies made before the deadline are not reached. |
| `rawRequestPurpose` | `string` | no |  | `user_requested_replay`, `debugging` | Declared purpose for retaining the raw prompt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

## authoring (38)

| Command | Required tier | Mutates |
|---|---|---:|
| [`motion.script.compile`](#motionscriptcompile) | `write_local` | yes |
| [`motion.package.script.author`](#motionpackagescriptauthor) | `write_local` | yes |
| [`motion.analysis.tracking.request`](#motionanalysistrackingrequest) | `write_local` | yes |
| [`motion.analysis.tracking.inspect`](#motionanalysistrackinginspect) | `read_motion` | no |
| [`motion.analysis.tracking.apply`](#motionanalysistrackingapply) | `edit_motion` | yes |
| [`motion.analysis.tracking.detach`](#motionanalysistrackingdetach) | `edit_motion` | yes |
| [`motion.analysis.tracking.verify`](#motionanalysistrackingverify) | `read_motion` | no |
| [`motion.keying.inspect`](#motionkeyinginspect) | `read_motion` | no |
| [`motion.keying.apply`](#motionkeyingapply) | `edit_motion` | yes |
| [`motion.keying.remove`](#motionkeyingremove) | `edit_motion` | yes |
| [`motion.roto.upsert`](#motionrotoupsert) | `edit_motion` | yes |
| [`motion.roto.tracking.detach`](#motionrototrackingdetach) | `edit_motion` | yes |
| [`motion.roto.remove`](#motionrotoremove) | `edit_motion` | yes |
| [`motion.compositing.graph.inspect`](#motioncompositinggraphinspect) | `read_motion` | no |
| [`motion.compositing.graph.set`](#motioncompositinggraphset) | `edit_motion` | yes |
| [`motion.compositing.graph.remove`](#motioncompositinggraphremove) | `edit_motion` | yes |
| [`motion.procedural.inspect`](#motionproceduralinspect) | `read_motion` | no |
| [`motion.procedural.audio-envelope.produce`](#motionproceduralaudio-envelopeproduce) | `edit_motion` | yes |
| [`motion.procedural.relationship.set`](#motionproceduralrelationshipset) | `edit_motion` | yes |
| [`motion.procedural.relationship.enabled.set`](#motionproceduralrelationshipenabledset) | `edit_motion` | yes |
| [`motion.procedural.relationship.bake`](#motionproceduralrelationshipbake) | `edit_motion` | yes |
| [`motion.procedural.relationship.detach`](#motionproceduralrelationshipdetach) | `edit_motion` | yes |
| [`motion.timeline.cutout.rig.bake`](#motiontimelinecutoutrigbake) | `edit_motion` | yes |
| [`motion.scene3d.gltf.import`](#motionscene3dgltfimport) | `write_local` | yes |
| [`motion.lottie.import`](#motionlottieimport) | `write_local` | yes |
| [`motion.dotlottie.import`](#motiondotlottieimport) | `write_local` | yes |
| [`motion.source.import`](#motionsourceimport) | `write_local` | yes |
| [`motion.source.to_scripted_video`](#motionsourceto_scripted_video) | `write_local` | yes |
| [`motion.html.snippet.export`](#motionhtmlsnippetexport) | `write_local` | yes |
| [`motion.html.snippet.import`](#motionhtmlsnippetimport) | `write_local` | yes |
| [`motion.otio.export`](#motionotioexport) | `write_local` | yes |
| [`motion.otio.import`](#motionotioimport) | `write_local` | yes |
| [`motion.template.catalog`](#motiontemplatecatalog) | `read_motion` | no |
| [`motion.template.plan`](#motiontemplateplan) | `read_motion` | no |
| [`motion.template.panel`](#motiontemplatepanel) | `read_motion` | no |
| [`motion.template.controls`](#motiontemplatecontrols) | `read_motion` | no |
| [`motion.template.apply`](#motiontemplateapply) | `edit_motion` | yes |
| [`motion.template.media.replace`](#motiontemplatemediareplace) | `edit_motion` | yes |

### `motion.script.compile`

Tier `write_local` · mutates: yes

**Purpose:** Compile structured scripted-video input from a host-approved root or inline into an empty host-approved Motion package output, committing the receipt after the complete package.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `scriptPath` | `string` | no |  |  | Scripted-video JSON path. Required unless script is given inline. |
| `script` | `object` | no |  |  | Inline scripted-video document, in place of scriptPath. |
| `packageDir` | `string` | yes |  |  | Linux-only empty or absent host-approved output directory. Motion commits the complete compiled package and its final receipt once through descriptor-relative exact closed-tree publication; macOS and Windows refuse before creating output state. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. It runs only after the complete package commits; a mirror failure is returned as a warning. |
| `createdAt` | `string` | no |  |  | Deterministic ISO timestamp for the emitted receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.package.script.author`

Tier `write_local` · mutates: yes

**Purpose:** Author one host-approved local inline web/html/canvas entry from a data-only source package into a separate empty copy-on-write output; the source remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Data-only source Motion package inside a host-approved authoring input root. It is never modified in place. |
| `outDir` | `string` | yes |  |  | Empty or absent output directory inside a host-approved authoring output root, outside packageRoot. |
| `html` | `string` | yes |  |  | Inline local HTML entry bytes. Only classic inline scripts are admitted; dynamic code construction, src/module/inert scripts, workers, frames, event handlers, javascript: URLs, and secondary composition are refused before authority minting. Motion injects a hash-based CSP that blocks eval and unlisted executable code. |
| `layer` | `object` | yes |  |  | The command owns source and allowedOrigins: callers cannot provide paths, URLs, external origins, or package claims. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.analysis.tracking.request`

Tier `write_local` · mutates: yes

**Purpose:** Run contained point or planar analysis for a manifest-declared package video, then persist its retryable lifecycle in a copied package through separate host-approved input and empty output roots.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package containing a manifest-declared video asset. |
| `outDir` | `string` | yes | `packageDir` |  | Trusted empty output directory for the package copy and persisted lifecycle. |
| `analysisId` | `string` | yes |  |  | Stable safe identifier used for retries and the lifecycle artifact path. |
| `assetId` | `string` | yes |  |  | Motion video asset id to track. |
| `mode` | `string` | yes |  | `point`, `planar` | Tracker kind: point follows a single feature, planar solves a surface. |
| `model` | `string` | yes |  | `translation`, `similarity`, `homography` | Motion model the solve fits; homography is required for planar corner-pin applies. |
| `reference` | `object` | yes |  |  | Reference time, bounds, and point coordinates in source pixels. |
| `settings` | `object` | yes |  |  | Bounded time range, direction, step, search, confidence, and deterministic solver settings. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `createdAt` | `string` | no |  |  | Optional deterministic timestamp for tests and reproducible fixtures. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.analysis.tracking.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect tracking confidence, lifecycle attempts, last-good data, and current source identity without mutation.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package containing the persisted tracking lifecycle. |
| `analysisId` | `string` | yes |  |  | Tracking lifecycle id to inspect. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.analysis.tracking.apply`

Tier `edit_motion` · mutates: yes

**Purpose:** Compile a current tracking result into reversible ordinary transform keyframes in a copied package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package containing a current persisted tracking lifecycle. |
| `outDir` | `string` | yes | `packageDir` |  | Trusted empty output directory for the stabilized package copy. |
| `analysisId` | `string` | yes |  |  | Last-good tracking analysis to compile into ordinary transform keyframes. |
| `layerId` | `string` | yes |  |  | Target footage layer id. |
| `segmentIndex` | `number` | no |  |  | Required explicit confidence-qualified segment for a partial track. |
| `includeLowConfidence` | `boolean` | no |  |  | Explicitly include low-confidence samples while preserving lost gaps. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.analysis.tracking.detach`

Tier `edit_motion` · mutates: yes

**Purpose:** Restore the exact transform keyframes that existed before tracking stabilization was attached. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package with attached tracking stabilization. |
| `outDir` | `string` | yes | `packageDir` |  | Trusted empty output directory for the detached package copy. |
| `layerId` | `string` | yes |  |  | Layer whose exact prior transform keyframes must be restored. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.analysis.tracking.verify`

Tier `read_motion` · mutates: no

**Purpose:** Verify attachment identity, source bytes, and generated stabilization keyframes without mutation.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package whose attachment, generated keyframes, and source identity are verified. |
| `layerId` | `string` | yes |  |  | Stabilized layer id. |
| `analysisId` | `string` | no |  |  | Optional expected tracking analysis id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.keying.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect one image or video layer's current chroma-key and roto-mask state without mutating the package.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `layerId` | `string` | yes |  |  | Image or video layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.keying.apply`

Tier `edit_motion` · mutates: yes

**Purpose:** Apply bounded chroma-key and matte-cleanup controls to one image or video layer in a copy-on-write package, emitting a keying receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `layerId` | `string` | yes |  |  | Image or video layer id. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory for the copy-on-write package. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `keying` | `object` | yes |  |  | Bounded chroma-key and matte-cleanup controls. |
| `mask` | `object` | no |  |  | Roto mask. Applied by motion.roto.upsert; on the other operations it is only recorded in the receipt input hash. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.keying.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one image or video layer's chroma-key controls in a copy-on-write package, emitting a keying receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `layerId` | `string` | yes |  |  | Image or video layer id. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory for the copy-on-write package. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `keying` | `object` | no |  |  | Chroma-key controls. Applied by motion.keying.apply; on the other operations it is only recorded in the receipt input hash. |
| `mask` | `object` | no |  |  | Roto mask. Applied by motion.roto.upsert; on the other operations it is only recorded in the receipt input hash. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.roto.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Upsert one bounded animated roto mask, including an optional tracking attachment, on an image or video layer in a copy-on-write package and emit a keying receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `layerId` | `string` | yes |  |  | Image or video layer id. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory for the copy-on-write package. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `keying` | `object` | no |  |  | Chroma-key controls. Applied by motion.keying.apply; on the other operations it is only recorded in the receipt input hash. |
| `mask` | `object` | yes |  |  | Bounded animated roto mask with optional tracking attachment. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.roto.tracking.detach`

Tier `edit_motion` · mutates: yes

**Purpose:** Detach a roto mask's tracking attachment while retaining the mask in a copy-on-write package and emit a keying receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `layerId` | `string` | yes |  |  | Image or video layer id. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory for the copy-on-write package. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `keying` | `object` | no |  |  | Chroma-key controls. Applied by motion.keying.apply; on the other operations it is only recorded in the receipt input hash. |
| `mask` | `object` | no |  |  | Roto mask. Applied by motion.roto.upsert; on the other operations it is only recorded in the receipt input hash. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.roto.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one image or video layer's roto mask in a copy-on-write package and emit a keying receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `layerId` | `string` | yes |  |  | Image or video layer id. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory for the copy-on-write package. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `keying` | `object` | no |  |  | Chroma-key controls. Applied by motion.keying.apply; on the other operations it is only recorded in the receipt input hash. |
| `mask` | `object` | no |  |  | Roto mask. Applied by motion.roto.upsert; on the other operations it is only recorded in the receipt input hash. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.compositing.graph.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect the current data-only compositing graph, validation, deterministic fingerprint, and compile metadata without mutating the package.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.compositing.graph.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Validate and compile one versioned acyclic data-only compositing graph into a copy-on-write package, retaining hidden round-trip source layers and emitting a graph receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent copy-on-write output directory. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in output facts. |
| `graph` | `object` | yes |  |  | Versioned, acyclic, data-only compositing graph. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.compositing.graph.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove generated compositing output, restore source-layer visibility in a copy-on-write package, and emit a graph receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent copy-on-write output directory. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in output facts. |
| `graph` | `object` | no |  |  | Ignored by remove; recorded in the receipt input hash when supplied. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.procedural.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect typed data-only scalar relationships and optionally evaluate their readable outputs at one timeline time without mutating the package.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `atMs` | `number` | no |  |  | Optional timeline time to evaluate readable relationship outputs. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.procedural.audio-envelope.produce`

Tier `edit_motion` · mutates: yes

**Purpose:** Decode one approved local source layer into bounded data-only RMS samples for procedural audio-envelope nodes. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `sourceLayerId` | `string` | yes | `sourceLayer` |  | Self-contained local audio layer to decode (WAV, FLAC, MP3, Ogg, or Opus). The v0.2 producer refuses video containers, trims, loops, and playback-rate changes it cannot read back exactly. |
| `envelopeId` | `string` | yes | `id` |  | Stable id for the data-only procedural envelope. |
| `sampleEveryMs` | `number` | no |  |  | Decoded RMS sampling window in milliseconds. The command refuses output over the existing 4096-sample graph budget. Defaults to `50`. |
| `channel` | `string` | no |  | `mix` | v0.2 decodes one bounded mixed-channel RMS envelope; separate left/right channels remain a future extension. Defaults to `mix`. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.procedural.relationship.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set one data-only typed scalar relationship in a copy-on-write package; executable expressions are refused, and a relationship receipt is emitted. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `outDir` | `string` | yes |  |  | Empty or absent copy-on-write output directory. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in output facts. |
| `relationship` | `object` | yes |  |  | Data-only typed scalar relationship; executable expressions are rejected. Required by motion.procedural.relationship.set. |
| `relationshipId` | `string` | no |  |  | Stable relationship id. Required by enabled.set and detach. |
| `enabled` | `boolean` | no |  |  | Whether the relationship participates in evaluation. Required by motion.procedural.relationship.enabled.set. |
| `relationshipIds` | `array` | no |  |  | Optional relationship ids; bake defaults to all enabled relationships. |
| `startMs` | `number` | no |  |  | First baked sample time in milliseconds; the layer start when omitted. |
| `endMs` | `number` | no |  |  | Last baked sample time in milliseconds; the layer end when omitted. |
| `sampleEveryFrames` | `number` | no |  |  | Bake one keyframe every N frames; must be a positive integer. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.procedural.relationship.enabled.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Enable or disable one typed scalar relationship in a copy-on-write package and emit a relationship receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `outDir` | `string` | yes |  |  | Empty or absent copy-on-write output directory. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in output facts. |
| `relationship` | `object` | no |  |  | Data-only typed scalar relationship; executable expressions are rejected. Required by motion.procedural.relationship.set. |
| `relationshipId` | `string` | yes |  |  | Stable relationship id. Required by enabled.set and detach. |
| `enabled` | `boolean` | yes |  |  | Whether the relationship participates in evaluation. Required by motion.procedural.relationship.enabled.set. |
| `relationshipIds` | `array` | no |  |  | Optional relationship ids; bake defaults to all enabled relationships. |
| `startMs` | `number` | no |  |  | First baked sample time in milliseconds; the layer start when omitted. |
| `endMs` | `number` | no |  |  | Last baked sample time in milliseconds; the layer end when omitted. |
| `sampleEveryFrames` | `number` | no |  |  | Bake one keyframe every N frames; must be a positive integer. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.procedural.relationship.bake`

Tier `edit_motion` · mutates: yes

**Purpose:** Bake selected enabled typed scalar relationships into ordinary keyframes in a copy-on-write package, with bounded samples and a relationship receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `outDir` | `string` | yes |  |  | Empty or absent copy-on-write output directory. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in output facts. |
| `relationship` | `object` | no |  |  | Data-only typed scalar relationship; executable expressions are rejected. Required by motion.procedural.relationship.set. |
| `relationshipId` | `string` | no |  |  | Stable relationship id. Required by enabled.set and detach. |
| `enabled` | `boolean` | no |  |  | Whether the relationship participates in evaluation. Required by motion.procedural.relationship.enabled.set. |
| `relationshipIds` | `array` | no |  |  | Optional relationship ids; bake defaults to all enabled relationships. |
| `startMs` | `number` | no |  |  | First baked sample time in milliseconds; the layer start when omitted. |
| `endMs` | `number` | no |  |  | Last baked sample time in milliseconds; the layer end when omitted. |
| `sampleEveryFrames` | `number` | no |  |  | Bake one keyframe every N frames; must be a positive integer. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.procedural.relationship.detach`

Tier `edit_motion` · mutates: yes

**Purpose:** Detach one typed scalar relationship without baking it in a copy-on-write package and emit a relationship receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package. |
| `outDir` | `string` | yes |  |  | Empty or absent copy-on-write output directory. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in output facts. |
| `relationship` | `object` | no |  |  | Data-only typed scalar relationship; executable expressions are rejected. Required by motion.procedural.relationship.set. |
| `relationshipId` | `string` | yes |  |  | Stable relationship id. Required by enabled.set and detach. |
| `enabled` | `boolean` | no |  |  | Whether the relationship participates in evaluation. Required by motion.procedural.relationship.enabled.set. |
| `relationshipIds` | `array` | no |  |  | Optional relationship ids; bake defaults to all enabled relationships. |
| `startMs` | `number` | no |  |  | First baked sample time in milliseconds; the layer start when omitted. |
| `endMs` | `number` | no |  |  | Last baked sample time in milliseconds; the layer end when omitted. |
| `sampleEveryFrames` | `number` | no |  |  | Bake one keyframe every N frames; must be a positive integer. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.cutout.rig.bake`

Tier `edit_motion` · mutates: yes

**Purpose:** Bake a bounded shellx-motion/cutout-rig@1 plan for one static PNG into flat image layers and sampled transform keyframes in a copy-on-write package, with a bake receipt; it is not a live rig. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package containing the static PNG image layer. |
| `outDir` | `string` | yes |  |  | Absent or empty copy-on-write output package directory. |
| `sourceLayerId` | `string` | yes |  |  | Visible, unlocked static image layer to replace with flat cutout layers. |
| `rig` | `object` | yes |  |  | Strict shellx-motion/cutout-rig@1 JSON: 1..16 nodes, each with one output layerId, optional parentId, explicit stackIndex, PNG-pixel crop/origin, 1..32 bounded poses, and sampleEveryFrames 1..16. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in output facts. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.scene3d.gltf.import`

Tier `write_local` · mutates: yes

**Purpose:** Create a Motion package from a host-approved local .gltf or .glb within host-approved input and output roots, lowering only the bounded static glTF 2.0 subset with source and adapter-lowering receipts.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `sourcePath` | `string` | yes |  |  | Host-approved local .gltf or .glb source path. |
| `outDir` | `string` | yes |  |  | Host-approved empty or absent package output directory. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in package provenance. |
| `createdAt` | `string` | no |  |  | Optional deterministic ISO timestamp for receipts. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.lottie.import`

Tier `write_local` · mutates: yes

**Purpose:** Create a Motion package from a host-approved Lottie JSON source inside host-configured input and output roots.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `sourcePath` | `string` | yes |  |  | Host-approved local Lottie JSON source path. |
| `outDir` | `string` | yes |  |  | Host-approved empty or absent package output directory. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in package provenance. |
| `createdAt` | `string` | no |  |  | Optional deterministic ISO timestamp for receipts. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.dotlottie.import`

Tier `write_local` · mutates: yes

**Purpose:** Create a Motion package from a host-approved dotLottie container inside host-configured input and output roots, optionally selecting its declared animation and theme.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `sourcePath` | `string` | yes |  |  | Host-approved local .lottie container path. |
| `outDir` | `string` | yes |  |  | Host-approved empty or absent package output directory. |
| `animationId` | `string` | no |  |  | Animation to select from the container. Defaults to the container's declared default. |
| `themeId` | `string` | no |  |  | Theme to apply from the container. Defaults to no theme override. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in package provenance. |
| `createdAt` | `string` | no |  |  | Optional deterministic ISO timestamp for receipts. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.source.import`

Tier `write_local` · mutates: yes

**Purpose:** Import a public source or supplied Markdown into a host-approved empty output, preserving source identity and receipt evidence.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `url` | `string` | yes |  |  | Public http(s) source URL to preserve as source identity. |
| `outDir` | `string` | yes |  |  | Trusted empty output directory for the imported Markdown source. |
| `receiptsRoot` | `string` | no |  |  | Optional host receipts root for source-import receipt copies. |
| `markdown` | `string` | no |  |  | Optional pre-fetched Markdown content. When omitted, the debug API fetches the URL. |
| `title` | `string` | no |  |  | Optional source title. |
| `kind` | `string` | no |  |  | Optional source kind: article, repo, or text. |
| `maxChars` | `number` | no |  |  | Maximum Markdown body characters kept before truncation. |
| `createdBy` | `string` | no |  |  | Optional actor recorded in receipt output. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.source.to_scripted_video`

Tier `write_local` · mutates: yes

**Purpose:** Lower imported Markdown from a host-approved input root into deterministic scripted-video JSON and receipt evidence in a host-approved empty output.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `sourcePath` | `string` | yes | `source`, `sourceMarkdownPath` |  | Imported source.md path inside a host-approved authoring input root. |
| `outDir` | `string` | yes |  |  | Trusted empty output directory for scripted-video JSON and receipt evidence. |
| `receiptsRoot` | `string` | no |  |  | Optional host receipts root for source-storyboard receipt copies. |
| `maxFrames` | `number` | no |  |  | Maximum storyboard frames to emit. |
| `frameDurationMs` | `number` | no |  |  | Duration for each generated storyboard frame. |
| `width` | `number` | no |  |  | Scripted-video output width. |
| `height` | `number` | no |  |  | Scripted-video output height. |
| `fps` | `number` | no |  |  | Scripted-video frame rate. |
| `createdBy` | `string` | no |  |  | Optional actor recorded in receipt output. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.html.snippet.export`

Tier `write_local` · mutates: yes

**Purpose:** Export a Motion package from host-approved roots as standalone HTML/CSS to a trusted empty output, with timing metadata and adapter lossiness diagnostics.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root to export. |
| `outDir` | `string` | yes |  |  | Trusted empty output directory for the standalone HTML snippet. |
| `createdAt` | `string` | no |  |  | Optional deterministic receipt timestamp. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.html.snippet.import`

Tier `write_local` · mutates: yes

**Purpose:** Import a bounded inert ShellX/HyperFrames-style HTML composition from a host-approved path into a trusted empty package output, staging verified local media and a receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `htmlPath` | `string` | yes |  |  | Trusted ShellX/HyperFrames-style HTML snippet path to import. |
| `packageDir` | `string` | yes |  |  | Trusted empty output package directory. |
| `createdAt` | `string` | no |  |  | Optional deterministic receipt timestamp. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.otio.export`

Tier `write_local` · mutates: yes

**Purpose:** Export a Motion package from host-approved roots to a trusted OpenTimelineIO output path with a receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root to export. |
| `outPath` | `string` | yes |  |  | Trusted output .otio timeline path. |
| `createdAt` | `string` | no |  |  | Optional deterministic receipt timestamp. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.otio.import`

Tier `write_local` · mutates: yes

**Purpose:** Import an OpenTimelineIO timeline from a host-approved path into a trusted Motion package output with a receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `otioPath` | `string` | yes |  |  | OpenTimelineIO .otio timeline path to import. |
| `packageDir` | `string` | yes |  |  | Trusted output Motion package directory. |
| `createdAt` | `string` | no |  |  | Optional deterministic receipt timestamp. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.template.catalog`

Tier `read_motion` · mutates: no

**Purpose:** List available Motion templates from explicit package roots.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `templateRoot` | `string` | no | `templatesRoot`, `root` |  | Directory containing one or more Motion packages with template sidecars. |
| `packageRoot` | `string` | no |  |  | Single Motion package root to inspect. |
| `packageRoots` | `array` | no |  |  | Explicit Motion package roots to inspect. |
| `targetHost` | `string` | no | `host` |  | Optional host target such as shellx-cut or shellx-canvas for compatibility scoring. |
| `targetLane` | `string` | no | `lane` |  | Optional renderer lane target such as browser, native, or ffmpeg. |
| `aspectRatio` | `string` | no | `targetAspectRatio` |  | Optional output aspect ratio such as 16:9 or 9:16; derived from width and height when omitted. |
| `durationMs` | `number` | no | `targetDurationMs` |  | Optional target duration in milliseconds for template bounds scoring. |
| `width` | `number` | no | `targetWidth` |  | Optional target output width for template bounds scoring. |
| `height` | `number` | no | `targetHeight` |  | Optional target output height for template bounds scoring. |
| `targetCommercialUse` | `boolean` | no | `commercialUse` |  | Keep only templates whose licence permits commercial use. |
| `renderCost` | `string` | no |  | `low`, `medium`, `high` | Keep only templates in this render-cost band. |
| `outputType` | `string` | no |  |  | Keep only templates that produce this output type. |
| `requiresMedia` | `boolean` | no |  |  | Keep only templates that do (true) or do not (false) require caller-supplied media. |
| `requiresAudio` | `boolean` | no |  |  | Keep only templates that do (true) or do not (false) require caller-supplied audio. |
| `designFamily` | `string` | no |  |  | Keep only templates in this design family. |

At least one input shape is required: `templateRoot`; `packageRoot`; `packageRoots`.

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.template.plan`

Tier `read_motion` · mutates: no

**Purpose:** Read a compatible template plan for a request, declared controls or media slots, authoring loop, input readiness, and quality targets without applying values or rendering.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `request` | `string` | yes |  |  | Prompt or user intent to match against available templates. |
| `prompt` | `string` | no |  |  | Alias for request. |
| `values` | `object` | no |  |  | Optional draft template param values keyed by param id; used to classify provided, defaulted, and missing inputs before mutation. |
| `templateRoot` | `string` | no | `templatesRoot`, `root` |  | Directory containing one or more Motion packages with template sidecars. |
| `packageRoot` | `string` | no |  |  | Single Motion package root to inspect. |
| `packageRoots` | `array` | no |  |  | Explicit Motion package roots to inspect. |
| `targetHost` | `string` | no | `host` |  | Optional host target such as shellx-cut or shellx-canvas for compatibility scoring. |
| `targetLane` | `string` | no | `lane` |  | Optional renderer lane target such as browser, native, or ffmpeg. |
| `aspectRatio` | `string` | no | `targetAspectRatio` |  | Optional output aspect ratio such as 16:9 or 9:16; derived from width and height when omitted. |
| `durationMs` | `number` | no | `targetDurationMs` |  | Optional target duration in milliseconds for template bounds scoring. |
| `width` | `number` | no | `targetWidth` |  | Optional target output width for template bounds scoring. |
| `height` | `number` | no | `targetHeight` |  | Optional target output height for template bounds scoring. |
| `targetCommercialUse` | `boolean` | no | `commercialUse` |  | Keep only templates whose licence permits commercial use. |
| `renderCost` | `string` | no |  | `low`, `medium`, `high` | Keep only templates in this render-cost band. |
| `outputType` | `string` | no |  |  | Keep only templates that produce this output type. |
| `requiresMedia` | `boolean` | no |  |  | Keep only templates that do (true) or do not (false) require caller-supplied media. |
| `requiresAudio` | `boolean` | no |  |  | Keep only templates that do (true) or do not (false) require caller-supplied audio. |
| `designFamily` | `string` | no |  |  | Keep only templates in this design family. |

At least one input shape is required: `templateRoot`; `packageRoot`; `packageRoots`.

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.template.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show grouped TemplateIR controls with current bound Motion values and follow-up actions.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.template.controls`

Tier `read_motion` · mutates: no

**Purpose:** List host-editable template controls for the current package.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.template.apply`

Tier `edit_motion` · mutates: yes

**Purpose:** Apply host-editable template values through declared bindings. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Template Motion package root. |
| `outDir` | `string` | yes | `packageDir` |  | Output package directory for applied template values. |
| `receiptsRoot` | `string` | no |  |  | Optional host receipts root for a copied receipt. |
| `values` | `object` | yes |  |  | Template param values keyed by param id. |
| `createdBy` | `string` | no |  |  | Agent or host actor label for receipt provenance. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.template.media.replace`

Tier `edit_motion` · mutates: yes

**Purpose:** Replace a TemplateIR media slot with a copied package-local asset and bound MotionIR refs. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `paramId` | `string` | yes |  |  | Template media parameter to rebind. |
| `assetPath` | `string` | yes |  |  | Host-approved regular file inside a configured authoring input root; copied with retained identity and a 64 MiB ceiling. |
| `assetRef` | `string` | no |  |  | Package-relative asset reference to write; assets/<basename> when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

## integration (12)

| Command | Required tier | Mutates |
|---|---|---:|
| [`motion.connector.catalog`](#motionconnectorcatalog) | `read_motion` | no |
| [`motion.connector.submit`](#motionconnectorsubmit) | `render_motion` | yes |
| [`motion.connector.panel`](#motionconnectorpanel) | `read_motion` | no |
| [`motion.canvas.package`](#motioncanvaspackage) | `write_local` | yes |
| [`motion.canvas.bridge_export`](#motioncanvasbridge_export) | `write_local` | yes |
| [`motion.browser.workflow.capture`](#motionbrowserworkflowcapture) | `render_motion` | yes |
| [`motion.connector.canvas_to_mp4`](#motionconnectorcanvas_to_mp4) | `write_local` | yes |
| [`motion.connector.canvas_to_cut`](#motionconnectorcanvas_to_cut) | `write_local` | yes |
| [`motion.connector.script_to_cut`](#motionconnectorscript_to_cut) | `write_local` | yes |
| [`motion.connector.source_to_cut`](#motionconnectorsource_to_cut) | `write_local` | yes |
| [`motion.connector.cut_generate_to_cut`](#motionconnectorcut_generate_to_cut) | `write_local` | yes |
| [`motion.connector.template_to_cut`](#motionconnectortemplate_to_cut) | `write_local` | yes |

### `motion.connector.catalog`

Tier `read_motion` · mutates: no

**Purpose:** Return the canonical v2 connector catalog with immutable descriptor fingerprints and closed submit-preparation fields without reading a package, provider/authentication state, network, output, or host authority.

Takes no arguments.

### `motion.connector.submit`

Tier `render_motion` · mutates: yes

**Purpose:** Submit one exact discovered connector descriptor and closed request through the host's stable authenticated caller, caller-scoped opaque-reference authority, and immutable binding journal; filesystem paths and URLs are refused and a durable coordinator job is returned.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `jobId` | `string` | no |  |  | Optional stable job handle. Motion mints one when omitted. |
| `capabilityId` | `string` | yes |  |  | Exact capability id selected from the current Motion capability catalog. |
| `descriptorRevision` | `number` | yes |  |  | Exact descriptor revision observed during discovery. |
| `descriptorFingerprint` | `string` | yes |  |  | Exact descriptor fingerprint observed during discovery. |
| `requestSchemaId` | `string` | yes |  |  | Exact request schema id advertised by the selected descriptor. |
| `request` | `object` | yes |  |  | Closed descriptor-defined request. Filesystem paths and URLs are refused; reference fields contain only host-issued opaque handles, resolved only with the authenticated caller identity. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.connector.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show the current P2A Template-to-Cut and P2B Canvas/Script/Source-to-Cut connector lifecycle; legacy named routes are compatibility-only.

Takes no arguments.

### `motion.canvas.package`

Tier `write_local` · mutates: yes

**Purpose:** Convert a Canvas frame selection into a Motion package and resource catalog under host-approved roots, requiring an empty output and a host-approved sourceRoot for inline assets.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `canvasSelectionPath` | `string` | no |  |  | Canvas frame-selection JSON path. Required unless selection is given inline. |
| `selection` | `object` | no | `canvasSelection` |  | Inline Canvas frame-selection document, in place of canvasSelectionPath. Accepted schema ids: shellx-motion/canvas-frame-selection@1, shellx-canvas/frame-selection@1 (the canonical shellx-motion id additionally requires integration and identity blocks). Required fields — fixture: schema, selectedFrameId, project, brand, frames, imageEditorOutputs; project: id, name; brand: tokens; each frame: id, name, durationMs, fps, width, height, layers; each layer: id, kind, startMs, durationMs. Accepted layer kinds: adjustment, audio, camera, canvas, caption, environment, group, html, image, particles, points, scene3d, shader, shape, text, video, web. Rectangles, ellipses and stars are kind "shape" with a shape field: {"kind":"shape","shape":"rect"}. There is no "rect", "ellipse" or "circle" layer kind. Minimal working example: {"schema":"shellx-canvas/frame-selection@1","selectedFrameId":"frame_intro","project":{"id":"demo","name":"Demo"},"brand":{"tokens":{}},"frames":[{"id":"frame_intro","name":"Intro","durationMs":1000,"fps":30,"width":1920,"height":1080,"background":"#f8fafc","layers":[{"id":"panel","kind":"shape","shape":"rect","startMs":0,"durationMs":1000,"transform":{"x":100,"y":100,"width":600,"height":300,"opacity":1},"style":{"fill":"#2563eb"}}]}],"imageEditorOutputs":[]}. A rejected document returns every problem at once in result.problems, and this same contract in result.contract. |
| `packageDir` | `string` | yes | `outDir` |  | Linux-only empty or absent output directory for the generated Motion package. Exact closed-tree publication is unavailable on macOS and Windows, which refuse before creating output state. |
| `selectedFrameId` | `string` | no |  |  | Frame to package when the selection carries several. |
| `sourceRoot` | `string` | no |  |  | Host-approved root used to resolve declared assets. Required for inline selections that declare assets; file-backed selections derive their selection parent when omitted. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `createdAt` | `string` | no |  |  | Deterministic ISO timestamp for the emitted receipt. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.canvas.bridge_export`

Tier `write_local` · mutates: yes

**Purpose:** Export a trusted Canvas frame selection and bridge receipt to a no-clobber destination.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `canvasRoot` | `string` | yes |  |  | Trusted shellx-canvas checkout root containing app/server/motion-package.mjs. |
| `outPath` | `string` | yes |  |  | Frame-selection JSON path to write. Debug refuses an existing selection or fixed sibling Canvas bridge receipt; use CLI --force to replace both. |
| `path` | `string` | no |  |  | Alias for outPath. |
| `target` | `string` | no |  |  | Bridge export target label for Canvas provenance. |
| `projectName` | `string` | no |  |  | Project name to request from the Canvas bridge. |
| `frameName` | `string` | no |  |  | Frame name to request from the Canvas bridge. |
| `selectedIds` | `array` | no |  |  | Optional selected Canvas node ids to export. |
| `generatedAt` | `string` | no |  |  | Deterministic ISO timestamp for the bridge selection and receipt. |
| `durationMs` | `number` | no |  |  | Default duration for the generated frame selection. |
| `fps` | `number` | no |  |  | Default frame rate for the generated frame selection. |

Other arguments are accepted and passed through unread; this command does not close its argument set.

### `motion.browser.workflow.capture`

Tier `render_motion` · mutates: yes

**Purpose:** Capture a package through deterministic browser workflow replay, writing a frame, receipt, and optional trace, catalog, or recording evidence only inside host-approved output roots; recording is capped at 240 rendered samples.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root to capture through the deterministic browser lane. |
| `outDir` | `string` | no |  |  | Directory for captured frame, trace, catalog, and receipt artifacts. |
| `outputPath` | `string` | no |  |  | Explicit browser-captured frame output path. |
| `atMs` | `number` | no |  |  | Frame timestamp in milliseconds. |
| `workflow` | `object` | no |  |  | Inline shellx-motion/browser-workflow@1 replay plan. |
| `workflowPath` | `string` | no |  |  | Path to a shellx-motion/browser-workflow@1 replay plan. |
| `catalogPath` | `string` | no |  |  | Optional browser workflow catalog path for drift evidence. |
| `workflowCatalogPath` | `string` | no |  |  | Alias for catalogPath. |
| `recordingManifestPath` | `string` | no |  |  | Optional path for a sampled deterministic browser recording manifest, strictly below outDir and distinct from recordingFramesDir and other capture leaves. |
| `recordingFramesDir` | `string` | no |  |  | Optional directory for sampled browser recording frames, strictly below outDir and distinct from recordingManifestPath and other capture leaves. |
| `recordingSampleCount` | `number` | no |  |  | Number of deterministic frame samples to include in the recording manifest. Capped because each sample is a browser render written to disk. |
| `failOnDrift` | `boolean` | no |  |  | Return an error when catalog drift is detected. |

Other arguments are accepted and passed through unread; this command does not close its argument set.

### `motion.connector.canvas_to_mp4`

Tier `write_local` · mutates: yes

**Purpose:** On Linux, run the Canvas-to-MP4 compatibility harness, writing Motion package and resource evidence to a trusted no-clobber output and either dry-running or rendering the selected preset, with no Cut plan or Cut mutation; macOS and Windows refuse before creating connector output state.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `canvasSelectionPath` | `string` | yes |  |  | Canvas frame-selection JSON exported by Canvas or motion.canvas.bridge_export. |
| `outDir` | `string` | yes |  |  | Linux-only trusted output directory for the Motion package, render artifacts, and connector receipt. Exact closed-tree package publication is unavailable on macOS and Windows, which refuse before creating connector output state. |
| `preset` | `string` | no |  | [`exportPreset`](#argument-value-enumerations) (10 values) | Export preset for the rendered output. Defaults to `mp4-h264`. |
| `dryRunRender` | `boolean` | no |  |  | Plan the render without encoding media. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.connector.canvas_to_cut`

Tier `write_local` · mutates: yes

**Purpose:** On Linux, turn host-approved Canvas selection input evidence into one no-clobber P2B delivery in an absent or empty output: Browser preview and Browser-to-FFmpeg H.264 rendered_media with artifact handle, receipts, and Cut plan; never mutate Cut.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `canvasSelectionPath` | `string` | yes |  |  | Host-approved Canvas frame-selection input evidence. |
| `outDir` | `string` | yes |  |  | Linux-only absent or empty delivery directory for one committed P2B package, Browser preview, H.264 media, artifact handle, Cut plan, and receipts. |
| `cutImportMode` | `string` | no |  | `rendered_media` | Optional P2B mode. Only rendered_media is accepted. Defaults to `rendered_media`. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.connector.script_to_cut`

Tier `write_local` · mutates: yes

**Purpose:** On Linux, turn scripted-video input evidence into one no-clobber P2B delivery in an absent or empty output: Browser preview and Browser-to-FFmpeg H.264 rendered_media with artifact handle, receipts, and Cut plan; never mutate Cut.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `scriptPath` | `string` | no |  |  | Host-approved scripted-video JSON input evidence; the external input is not a delivery artifact. |
| `script` | `object` | no |  |  | Inline scripted-video input. Inline success emits no scripted_video file. |
| `storyboard` | `object` | no |  |  | Alias for one inline scripted-video input. It cannot be combined with script or scriptPath. |
| `outDir` | `string` | yes |  |  | Linux-only absent or empty delivery directory for one committed P2B package, Browser preview, H.264 media, artifact handle, Cut plan, and receipts. |
| `cutImportMode` | `string` | no |  | `rendered_media` | Optional P2B mode. Only rendered_media is accepted. Defaults to `rendered_media`. |
| `startMs` | `number` | no |  |  | Optional non-negative Cut placement start in milliseconds. |
| `durationMs` | `number` | no |  |  | Optional positive Cut placement duration in milliseconds. |
| `track` | `string` | no |  |  | Optional non-empty Cut placement track. |

Exactly one input shape is required: `scriptPath`; `script`; `storyboard`.

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.connector.source_to_cut`

Tier `write_local` · mutates: yes

**Purpose:** On Linux, turn host-approved imported Markdown input evidence into one no-clobber P2B delivery in an absent or empty output: Browser preview and Browser-to-FFmpeg H.264 rendered_media with artifact handle, receipts, and Cut plan; never mutate Cut.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `sourcePath` | `string` | yes |  |  | Host-approved imported Markdown input evidence; source bytes and import receipt are not delivery artifacts. |
| `outDir` | `string` | yes |  |  | Linux-only absent or empty delivery directory for one committed P2B storyboard, package, Browser preview, H.264 media, artifact handle, Cut plan, and receipts. |
| `maxFrames` | `number` | no |  |  | Maximum review-required storyboard frames to derive from the source. |
| `frameDurationMs` | `number` | no |  |  | Duration for each generated storyboard frame. |
| `width` | `number` | no |  |  | Storyboard and Motion package width. |
| `height` | `number` | no |  |  | Storyboard and Motion package height. |
| `fps` | `number` | no |  |  | Storyboard and Motion package frame rate. |
| `cutImportMode` | `string` | no |  | `rendered_media` | Optional P2B mode. Only rendered_media is accepted. Defaults to `rendered_media`. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.connector.cut_generate_to_cut`

Tier `write_local` · mutates: yes

**Purpose:** On Linux, run the legacy Cut Generate compatibility flow from one script to package, Browser preview, Browser-to-FFmpeg H.264 rendered_media, and Cut plan; macOS and Windows refuse before creating output state, and it never inserts anything directly into Cut.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `scriptPath` | `string` | no |  |  | Path to a Cut Generate scripted-video JSON document. |
| `script` | `object` | no |  |  | Inline scripted-video document emitted by Cut Generate. |
| `storyboard` | `object` | no |  |  | Alias for script when called from storyboard prompts. |
| `outDir` | `string` | yes |  |  | Linux-only trusted output directory for exact closed-tree package, preview, render, Cut import plan, and connector receipt artifacts; macOS and Windows refuse before creating output state. |
| `cutImportMode` | `string` | no |  |  | Cut import mode, such as rendered_media or editable_lowering. |
| `dryRunRender` | `boolean` | no |  |  | Plan required renders without encoding media. |
| `createdAt` | `string` | no |  |  | Deterministic ISO timestamp for connector receipts and package provenance. |

Exactly one input shape is required: `scriptPath`; `script`; `storyboard`.

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.connector.template_to_cut`

Tier `write_local` · mutates: yes

**Purpose:** On Linux, apply values to immutable admitted template input evidence and publish one no-clobber P2A Browser-to-FFmpeg H.264 rendered_media delivery with browser preview, artifact handle, receipts, and Cut plan; it allows no dry run and never mutates Cut.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Template Motion package root admitted as input evidence before one P2A rendered-media delivery. |
| `outDir` | `string` | yes |  |  | Linux-only absent or empty output directory for one no-clobber P2A package, Browser preview/final, handle, Cut plan, and connector receipt delivery. |
| `values` | `object` | yes |  |  | Template param values keyed by param id. |
| `cutImportMode` | `string` | no |  | `rendered_media` | Optional P2A mode. Only rendered_media is accepted; it is the default. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

## render (19)

| Command | Required tier | Mutates |
|---|---|---:|
| [`motion.preview.frame`](#motionpreviewframe) | `render_motion` | yes |
| [`motion.preview.panel`](#motionpreviewpanel) | `read_motion` | no |
| [`motion.preview.playhead`](#motionpreviewplayhead) | `render_motion` | yes |
| [`motion.preview.strip`](#motionpreviewstrip) | `render_motion` | yes |
| [`motion.render.final`](#motionrenderfinal) | `render_motion` | yes |
| [`motion.render.cache.plan`](#motionrendercacheplan) | `render_motion` | no |
| [`motion.render.batch`](#motionrenderbatch) | `render_motion` | yes |
| [`motion.render.cancel`](#motionrendercancel) | `render_motion` | yes |
| [`motion.render.retry`](#motionrenderretry) | `render_motion` | yes |
| [`motion.render.status`](#motionrenderstatus) | `read_motion` | no |
| [`motion.render.queue`](#motionrenderqueue) | `read_motion` | no |
| [`motion.job.get`](#motionjobget) | `read_motion` | no |
| [`motion.job.list`](#motionjoblist) | `read_motion` | no |
| [`motion.job.events`](#motionjobevents) | `read_motion` | no |
| [`motion.job.submit`](#motionjobsubmit) | `render_motion` | yes |
| [`motion.job.cancel`](#motionjobcancel) | `render_motion` | yes |
| [`motion.job.retry`](#motionjobretry) | `render_motion` | yes |
| [`motion.quality.panel`](#motionqualitypanel) | `read_motion` | no |
| [`motion.quality.check`](#motionqualitycheck) | `render_motion` | yes |

### `motion.preview.frame`

Tier `render_motion` · mutates: yes

**Purpose:** Render one deterministic PNG preview with a receipt; the strict no-fallback GPU lane is visual-only and proves neither audio nor final media.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root to preview. |
| `lane` | `string` | no |  | `browser`, `gpu` | Preview raster lane. gpu is the strict general hardware WebGPU PNG lane with no CPU/browser fallback. For admitted CFR video, its host-owned provider consumes Core exact-microsecond requests, binds immutable-source and decoded-RGBA evidence, and replaces stable dynamic textures under fixed cache limits. This is visual-only preview: no audio, final-video staging, encoding, or mux claim; provider controls are never command arguments. Defaults to `browser`. |
| `atMs` | `number` | no |  |  | Frame timestamp in milliseconds. |
| `outDir` | `string` | no |  |  | Directory for generated preview artifacts. |
| `outputPath` | `string` | no |  |  | Explicit preview frame output path. When omitted, the host allocates a unique no-clobber PNG inside outDir. |
| `createdAt` | `string` | no |  |  | Deterministic ISO timestamp for emitted preview receipts. |
| `workflowPath` | `string` | no |  |  | Optional deterministic browser workflow JSON path. |
| `workflow` | `object` | no |  |  | Optional inline deterministic browser workflow, used instead of workflowPath. |

Other arguments are accepted and passed through unread; this command does not close its argument set.

### `motion.preview.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show a read-only preview player with playhead state, active timeline refs, preview modes, and render follow-ups.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.preview.playhead`

Tier `render_motion` · mutates: yes

**Purpose:** Render the current timeline playhead as a deterministic preview frame.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `outDir` | `string` | no |  |  | Directory for the rendered frame; a scratch directory when omitted. |
| `outputPath` | `string` | no |  |  | Explicit output file path for the rendered frame. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `createdAt` | `string` | no |  |  | Deterministic ISO timestamp for the emitted receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.preview.strip`

Tier `render_motion` · mutates: yes

**Purpose:** Render a deterministic timeline preview strip with per-frame evidence.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `outDir` | `string` | no |  |  | Directory for the rendered strip frames; a scratch directory when omitted. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `frameCount` | `number` | no | `frames` |  | Number of frames to sample; must be a positive integer of 60 or less. |
| `startMs` | `number` | no |  |  | First sampled time in milliseconds; must be within the motion duration. |
| `endMs` | `number` | no |  |  | Last sampled time in milliseconds; must be at or after startMs and within the motion duration. |
| `createdAt` | `string` | no |  |  | Deterministic ISO timestamp for the emitted receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.render.final`

Tier `render_motion` · mutates: yes

**Purpose:** Render final media through the selected renderer lane, opt into closed durable segments, or reuse an exact v2 attested artifact at the same output path.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `jobId` | `string` | no |  |  | Name this job so a host can query it with motion.job.get while the render runs. Omitted, Motion mints one and returns it as jobId on the result. 1..128 chars of letters, digits, dot, underscore, colon or hyphen. |
| `packageRoot` | `string` | yes |  |  | Motion package root to render. |
| `outputPath` | `string` | yes |  |  | Final media, still-frame, or image-sequence output path. |
| `preset` | `string` | no |  | [`exportPreset`](#argument-value-enumerations) (10 values) | Export preset for the rendered output. Defaults to `mp4-h264`. |
| `frameLane` | `string` | no |  | `browser`, `native`, `gpu` | Frame rasterizer lane. native uses the bounded native capability and refuses unsupported layers or non-deliverable text instead of falling back. gpu uses the strict WebGPU raw-RGBA producer for direct or durable-segmented FFmpeg final-video delivery; unsupported content or an unavailable GPU refuses without browser/native fallback. Browser is the production typography authority only for generated MotionIR text backed by manifest-declared font bytes; HTML/web/canvas typography is unverified and a maxFontFallbacks attestation refuses it. The CLI's separate --lane flag selects the delivery lane (native \| ffmpeg). Defaults to `browser`. |
| `atMs` | `number` | no |  |  | Timestamp in milliseconds for still-frame presets. |
| `framesDir` | `string` | no |  |  | Optional trusted scratch directory used only for materialized final-video frames; it does not request retention. |
| `keepFrames` | `boolean` | no |  |  | Explicitly retain materialized final-video FFmpeg frame PNGs. File-video exports stream directly to FFmpeg by default; a framesDir alone does not request retention. |
| `segmented` | `object` | no |  |  | Opt into cancellable durable segmented final delivery. It is incompatible with framesDir, keepFrames, browser workflows, exact-source quality manifests, and reuseAttested. |
| `minUniqueFrameHashes` | `number` | no |  |  | Minimum unique rendered frame hashes for motion-quality gating. |
| `workflowPath` | `string` | no |  |  | Optional deterministic browser workflow JSON path to replay before capture. |
| `workflow` | `object` | no |  |  | Optional inline deterministic browser workflow, used instead of workflowPath. |
| `qualityManifestPath` | `string` | no |  |  | Optional shellx-motion/quality-manifest@1 path to gate final output quality. |
| `manifestPath` | `string` | no |  |  | Alias for qualityManifestPath. |
| `receiptsRoot` | `string` | no |  |  | Host receipts root for render and optional quality-check receipts. |
| `dryRun` | `boolean` | no |  |  | Plan the render without writing media. |
| `reuseAttested` | `boolean` | no |  |  | Opt in to reusing only a v2 content-bound, receipt-attested file artifact at this exact output path. It performs no fresh browser or FFmpeg run on a verified hit, writes a fresh render.reuse receipt, and refuses dryRun, png-sequence, and keepFrames. No cache root, key, descriptor path, or receipt selector is caller-controlled. Defaults to `false`. |

Other arguments are accepted and passed through unread; this command does not close its argument set.

### `motion.render.cache.plan`

Tier `render_motion` · mutates: no

**Purpose:** Observe whether an exact v2 attested render entry is currently verified, without creating files or authorizing a later render.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root whose bounded current bytes contribute to the v2 reuse identity. The value is never echoed. |
| `outputPath` | `string` | yes |  |  | Requested file output path used only to derive v2 root-relative identity. No output, descriptor, or path is returned. |
| `preset` | `string` | no |  | [`exportPreset`](#argument-value-enumerations) (10 values) | File-producing export preset to inspect for exact v2 attested reuse. Defaults to `mp4-h264`. |
| `frameLane` | `string` | no |  | `browser`, `native` | Frame rasterizer lane included in the exact v2 render identity. GPU post-render identity is completed-render evidence only; it does not authorize cache planning or attested reuse. Defaults to `browser`. |
| `atMs` | `number` | no |  |  | Timestamp in milliseconds for a still-frame identity. |
| `minUniqueFrameHashes` | `number` | no |  |  | Optional final-video quality identity threshold. |
| `workflowPath` | `string` | no |  |  | Optional trusted shellx-motion/browser-workflow@1 file. Inline workflow is intentionally outside this compact plan surface. |
| `qualityManifestPath` | `string` | no |  |  | Optional trusted shellx-motion/quality-manifest@1 file and its bounded baseline closure. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.render.batch`

Tier `render_motion` · mutates: yes

**Purpose:** Render data rows into per-row media outputs with the selected export preset.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `jobId` | `string` | no |  |  | Name this job so a host can query it with motion.job.get while the render runs. Omitted, Motion mints one and returns it as jobId on the result. 1..128 chars of letters, digits, dot, underscore, colon or hyphen. |
| `packageRoot` | `string` | yes |  |  | Motion package root containing manifest, motion, assets, and optional data rows. |
| `outDir` | `string` | yes |  |  | Directory for expanded row packages, render outputs, and batch receipts. |
| `rowsPath` | `string` | no |  |  | Optional external CSV or JSON data rows file. |
| `rowId` | `string` | no |  |  | Single data row ID to render; normalized the same way as Motion data rows. |
| `rowIds` | `array` | no |  |  | Subset of at most 256 data row IDs to render; preserves source row order. |
| `preset` | `string` | no |  | [`exportPreset`](#argument-value-enumerations) (10 values) | Export preset for the rendered output. Defaults to `mp4-h264`. |
| `frameLane` | `string` | no |  | `browser`, `native`, `gpu` | Frame rasterizer for every row. GPU rows are fresh-only strict streamed FFmpeg video: absent host capability, GIF/still/sequence output, resume/cache, retained frames, or browser workflows refuse before queueing. Defaults to `browser`. |
| `keepFrames` | `boolean` | no |  |  | Pass explicit final-video FFmpeg frame retention to every batch row; batch plans containing a non-video preset are refused. File-video rows stream directly to FFmpeg by default. |
| `minUniqueFrameHashes` | `number` | no |  |  | Minimum unique rendered frame hashes for motion-quality gating. |
| `qualityManifestPath` | `string` | no |  |  | Optional shellx-motion/quality-manifest@1 path to gate each row output. |
| `manifestPath` | `string` | no |  |  | Alias for qualityManifestPath. |
| `workflow` | `object` | no |  |  | Inline shellx-motion/browser-workflow@1 replay plan for each rendered row. |
| `workflowPath` | `string` | no |  |  | Optional deterministic browser workflow JSON path to replay for each rendered row. |
| `dryRun` | `boolean` | no |  |  | Plan expanded row packages and receipts without rendering media. |
| `resume` | `boolean` | no |  |  | Reuse completed row outputs only when idempotency and retained authenticated caller ownership match; resume without a host caller principal fails before output writes. |

Other arguments are accepted and passed through unread; this command does not close its argument set.

### `motion.render.cancel`

Tier `render_motion` · mutates: yes

**Purpose:** On Linux, write a historical render-cancellation annotation through the stable receipt store. It never signals a running worker.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | yes |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |
| `receiptId` | `string` | yes | `id` |  | Receipt id of the job to act on. |
| `reason` | `string` | no |  |  | Optional human-readable reason recorded in the control receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.render.retry`

Tier `render_motion` · mutates: yes

**Purpose:** On Linux, write a historical render-retry annotation through the stable receipt store. It does not create a new render.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | yes |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |
| `receiptId` | `string` | yes | `id` |  | Receipt id of the job to act on. |
| `reason` | `string` | no |  |  | Optional human-readable reason recorded in the control receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.render.status`

Tier `read_motion` · mutates: no

**Purpose:** On Linux, read historical render status from stable host receipt files, which only describe work that has finished writing evidence and never observe a live worker.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | no |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.render.queue`

Tier `read_motion` · mutates: no

**Purpose:** On Linux, read stable historical render receipt rows and their annotations; it never observes or queues a live worker.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | no |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.job.get`

Tier `read_motion` · mutates: no

**Purpose:** Ask what one job is doing right now: pending, running, or ended with an outcome. Answers live work the receipt views cannot see.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `jobId` | `string` | yes |  |  | The job to read. Either the id returned by the render, or the id you supplied when starting it. |
| `scope` | `string` | no |  | `own`, `all` | Whose jobs to read. "all" needs a host that granted cross-caller visibility and is refused otherwise. Defaults to `own`. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.job.list`

Tier `read_motion` · mutates: no

**Purpose:** List this caller's jobs, live work first and finished work newest first.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `scope` | `string` | no |  | `own`, `all` | Whose jobs to list. "all" needs a host that granted cross-caller visibility and is refused otherwise. Defaults to `own`. |
| `limit` | `number` | no |  |  | Maximum jobs to return. Live work is listed first, then finished work newest first. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.job.events`

Tier `read_motion` · mutates: no

**Purpose:** Read the durable ordered event stream for one visible coordinator job.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `jobId` | `string` | yes |  |  | Job whose durable coordinator events to read. |
| `after` | `number` | no |  |  | Return events strictly after this sequence number; omit for the retained event log. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.job.submit`

Tier `render_motion` · mutates: yes

**Purpose:** Submit a coordinator-cancellable ordinary streamed or closed segmented final-video render and receive its durable job id before expensive work starts.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `jobId` | `string` | no |  |  | Optional stable 1..128-character job handle. Motion mints one when omitted. |
| `packageRoot` | `string` | yes |  |  | Existing Motion package to render. |
| `outputPath` | `string` | yes |  |  | Final local output path, subject to the host's normal render policy. |
| `preset` | `string` | no |  |  | Final-video FFmpeg export preset; defaults to mp4-h264. Still and image-sequence presets use motion.render.final instead. |
| `frameLane` | `string` | no |  | `browser`, `native`, `gpu` | Rasterizer for the coordinator-cancellable final render. GPU is strict raw-RGBA FFmpeg final video, direct or durably segmented: no materialization, workflow, exact-source quality, reuse, or cache path. Defaults to `browser`. |
| `segmented` | `object` | no |  |  | Optional closed durable segmented-final selector. It cannot be combined with streaming workflow or quality-manifest selectors. |
| `receiptsRoot` | `string` | no |  |  | Optional host-governed receipt root. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.job.cancel`

Tier `render_motion` · mutates: yes

**Purpose:** Request cancellation from the coordinator that owns the live worker; wait for its terminal state before claiming it stopped.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `jobId` | `string` | yes |  |  | Active job to stop. |
| `reason` | `string` | no |  |  | Optional operator explanation recorded with the terminal cancellation. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.job.retry`

Tier `render_motion` · mutates: yes

**Purpose:** Create a new coordinator job linked to a retryable failed source job.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `jobId` | `string` | yes |  |  | Retryable failed source job. |
| `newJobId` | `string` | no |  |  | Optional distinct handle for the new linked run. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.quality.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show quality-manifest samples, baselines, regions, audio policy, and suggested quality follow-up commands.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `qualityManifestPath` | `string` | yes |  |  | Quality manifest JSON path to inspect. |
| `manifestPath` | `string` | no |  |  | Alias for qualityManifestPath. |
| `inputPath` | `string` | no |  |  | Optional final media path used to seed quality-check follow-up commands. |
| `packageRoot` | `string` | no |  |  | Optional Motion package root for package and render follow-up metadata. |
| `preset` | `string` | no |  | [`exportPreset`](#argument-value-enumerations) (10 values) | Optional export preset used for render/export follow-up commands. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.quality.check`

Tier `render_motion` · mutates: yes

**Purpose:** Check rendered-media dimensions, audio, and sampled-frame quality against optional baseline thresholds with a receipt; it does not accept delivery or release.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `inputPath` | `string` | yes |  |  | Rendered media file to check. |
| `manifestPath` | `string` | no | `qualityManifestPath` |  | Optional quality manifest supplying the thresholds. |
| `framePath` | `string` | no |  |  | Optional pre-extracted frame to analyse instead of sampling inputPath. |
| `baselinePath` | `string` | no |  |  | Optional baseline image for the pixel-difference checks. |
| `outDir` | `string` | no |  |  | Directory for extracted frames and diffs; a scratch directory when omitted. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror. |
| `packageId` | `string` | no |  |  | Package id recorded in the emitted receipt. Defaults to `quality-check`. |
| `atMs` | `number` | no |  |  | Time to sample the frame from, in milliseconds. Defaults to `0`. |
| `expectWidth` | `number` | no |  |  | Required output width in pixels. |
| `expectHeight` | `number` | no |  |  | Required output height in pixels. |
| `expectAudio` | `boolean` | no |  |  | Fail when the media carries no audio stream. Defaults to `false`. |
| `maxAudioPeakDb` | `number` | no |  |  | Maximum sample peak in dBFS. |
| `minAudioLoudnessLufs` | `number` | no |  |  | Minimum integrated loudness in LUFS. |
| `maxAudioLoudnessLufs` | `number` | no |  |  | Maximum integrated loudness in LUFS; must be at or above minAudioLoudnessLufs. |
| `maxAudioTruePeakDbtp` | `number` | no |  |  | Maximum true peak in dBTP. |
| `maxAudioLoudnessRangeLu` | `number` | no |  |  | Maximum loudness range in LU. |
| `minBrightPixels` | `number` | no |  |  | Minimum count of non-black pixels in the sampled frame. |
| `minEdgePixels` | `number` | no |  |  | Minimum count of edge pixels in the sampled frame. |
| `minTransparentPixels` | `number` | no |  |  | Minimum count of transparent pixels. |
| `minNonTransparentPixels` | `number` | no |  |  | Minimum count of opaque pixels. |
| `maxChangedPixels` | `number` | no |  |  | Maximum pixels allowed to differ from baselinePath. |
| `maxMeanDiff` | `number` | no |  |  | Maximum mean per-pixel difference from baselinePath. |
| `minPsnrDb` | `number` | no |  |  | Minimum PSNR against baselinePath, in dB. |
| `minSsim` | `number` | no |  |  | Minimum SSIM against baselinePath, between 0 and 1. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

## surface (18)

| Command | Required tier | Mutates |
|---|---|---:|
| [`motion.state`](#motionstate) | `read_motion` | no |
| [`motion.open`](#motionopen) | `read_motion` | no |
| [`motion.select`](#motionselect) | `read_motion` | no |
| [`motion.highlight`](#motionhighlight) | `read_motion` | no |
| [`motion.platform.verification.panel`](#motionplatformverificationpanel) | `read_motion` | no |
| [`motion.platform.requirements`](#motionplatformrequirements) | `read_motion` | no |
| [`motion.platform.gpu.probe`](#motionplatformgpuprobe) | `render_motion` | yes |
| [`motion.assets.panel`](#motionassetspanel) | `read_motion` | no |
| [`motion.brand.panel`](#motionbrandpanel) | `read_motion` | no |
| [`motion.audio.panel`](#motionaudiopanel) | `read_motion` | no |
| [`motion.media.panel`](#motionmediapanel) | `read_motion` | no |
| [`motion.storyboard.panel`](#motionstoryboardpanel) | `read_motion` | no |
| [`motion.storyboard.graph`](#motionstoryboardgraph) | `read_motion` | no |
| [`motion.capabilities.match`](#motioncapabilitiesmatch) | `read_motion` | no |
| [`motion.capabilities.panel`](#motioncapabilitiespanel) | `read_motion` | no |
| [`motion.export.presets`](#motionexportpresets) | `read_motion` | no |
| [`motion.export.panel`](#motionexportpanel) | `read_motion` | no |
| [`motion.export.plan`](#motionexportplan) | `read_motion` | no |

### `motion.state`

Tier `read_motion` · mutates: no

**Purpose:** Read current package and UI state before mutation; retained host-receipt summaries currently require the Linux stable receipt reader.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | no |  |  | Optional Motion package root to summarize. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt root. identity-stable receipt reads and receipt-derived controls currently require linux; macos and windows return capability_unavailable before reading or writing receipt state. Package-only state remains portable when it is omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.open`

Tier `read_motion` · mutates: no

**Purpose:** Return a transient intent to focus a named Motion surface panel without mutating package data.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `panel` | `string` | no |  |  | Surface panel to focus, such as preview, timeline, assets, or receipts. Defaults to `preview`. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.select`

Tier `read_motion` · mutates: no

**Purpose:** Return a transient timeline selection intent for one layer, track, marker, scene, or target without mutating package data.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `layerId` | `string` | no | `layer` |  | Layer to select. One selection target is required. |
| `trackId` | `string` | no | `track` |  | Track to select. |
| `markerId` | `string` | no | `marker` |  | Marker to select. |
| `sceneId` | `string` | no | `scene` |  | Scene to select. |
| `targetId` | `string` | no | `id` |  | Generic surface target to select. |
| `durationMs` | `number` | no |  |  | How long the highlight stays visible, in milliseconds. |
| `packageId` | `string` | no |  |  | Optional package id recorded in the visible state. |
| `motionId` | `string` | no |  |  | Optional motion id recorded in the visible state. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.highlight`

Tier `read_motion` · mutates: no

**Purpose:** Return a transient timeline highlight intent for one layer, track, marker, scene, or target, optionally with a display duration, without mutating package data.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `layerId` | `string` | no | `layer` |  | Layer to select. One selection target is required. |
| `trackId` | `string` | no | `track` |  | Track to select. |
| `markerId` | `string` | no | `marker` |  | Marker to select. |
| `sceneId` | `string` | no | `scene` |  | Scene to select. |
| `targetId` | `string` | no | `id` |  | Generic surface target to select. |
| `durationMs` | `number` | no |  |  | How long the highlight stays visible, in milliseconds. |
| `packageId` | `string` | no |  |  | Optional package id recorded in the visible state. |
| `motionId` | `string` | no |  |  | Optional motion id recorded in the visible state. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.platform.verification.panel`

Tier `read_motion` · mutates: no

**Purpose:** On Linux, summarize stable Linux, Windows, macOS, and aggregate platform verification receipts.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | no |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |
| `requiredHosts` | `array` | no |  |  | Optional required host ids to compare against collected receipts. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.platform.requirements`

Tier `read_motion` · mutates: no

**Purpose:** Report per-operation readiness: satisfied for this invocation versus possible by any route, with FFmpeg, FFprobe, and browser status plus installOptions; it only reports source-only GPU policy and never proves GPU.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `operation` | `string` | no |  | `preview.frame`, `render.final`, `quality.check` | Scope the answer to the operation you are about to attempt, so a tool you do not need cannot report you as unready. The response also reports source-only GPU launch policy; it never opens Chromium or WebGPU. Omit to ask about the whole machine. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.platform.gpu.probe`

Tier `render_motion` · mutates: yes

**Purpose:** Run one explicitly confirmed, host-contained Chromium WebGPU frame/readback hardware proof; it is host GPU evidence, not renderer, artifact, or release qualification.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `confirm` | `boolean` | yes |  |  | Required explicit acknowledgement. Set true only to open one pre-contained Chromium WebGPU session and perform its bounded 4-by-4 frame/readback hardware proof. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.assets.panel`

Tier `read_motion` · mutates: no

**Purpose:** Summarize package assets, MotionIR asset refs, layer usage, hashes, and missing local files.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.brand.panel`

Tier `read_motion` · mutates: no

**Purpose:** Summarize design-token groups and brand provenance for package handoff.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.audio.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show resolved audio mix inputs, automation, track controls, document-master declaration, ducking, and export-preset compatibility warnings.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root to inspect for resolved audio mix inputs. |
| `preset` | `string` | no |  | [`exportPreset`](#argument-value-enumerations) (10 values) | Optional export preset used to report audio compatibility warnings. |
| `exportPreset` | `string` | no |  |  | Alias for preset. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.media.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show media layer source readiness, trim/loop/playback controls, and export-preset compatibility warnings.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root to inspect for image, video, audio, and web media layer readiness. |
| `preset` | `string` | no |  | [`exportPreset`](#argument-value-enumerations) (10 values) | Optional export preset used to report media/audio compatibility warnings. |
| `exportPreset` | `string` | no |  |  | Alias for preset. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.storyboard.panel`

Tier `read_motion` · mutates: no

**Purpose:** Review scripted-video storyboard metadata, readiness diagnostics, source refs, frame timings, and follow-up compile or Cut handoff actions.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `scriptPath` | `string` | no |  |  | Path to a shellx-motion scripted-video JSON document. |
| `storyboardPath` | `string` | no |  |  | Alias for scriptPath when called from storyboard workflows. |
| `path` | `string` | no |  |  | Alias for scriptPath. |
| `script` | `object` | no |  |  | Inline shellx-motion scripted-video document. |
| `storyboard` | `object` | no |  |  | Alias for script when called from Cut Generate or prompt storyboard workflows. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.storyboard.graph`

Tier `read_motion` · mutates: no

**Purpose:** Review scripted-video source, asset, template, engine, review, sequence graph facts, and readiness diagnostics before compile or Cut handoff.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `scriptPath` | `string` | no |  |  | Path to a shellx-motion scripted-video JSON document. |
| `storyboardPath` | `string` | no |  |  | Alias for scriptPath when called from storyboard workflows. |
| `path` | `string` | no |  |  | Alias for scriptPath. |
| `script` | `object` | no |  |  | Inline shellx-motion scripted-video document. |
| `storyboard` | `object` | no |  |  | Alias for script when called from Cut Generate or prompt storyboard workflows. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.capabilities.match`

Tier `read_motion` · mutates: no

**Purpose:** Match the package and requested output against renderer lane capability cards before rendering.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | no |  |  | Optional Motion package root to match against renderer capability cards. |
| `output` | `string` | no |  |  | Requested output such as png-frame, mp4-h264, mp4-hevc, webm-av1, webm-vp9, gif, cut-plan, or motion-package. |
| `target` | `string` | no |  |  | Requested workflow target such as preview, final, batch, cut, canvas, or handoff. |
| `needsAlpha` | `boolean` | no |  |  | Whether the selected lane must preserve alpha. |
| `needsAudio` | `boolean` | no |  |  | Whether the selected lane must handle audio. |
| `needsSubtitles` | `boolean` | no |  |  | Whether the selected lane must handle subtitles or captions. |
| `preferLane` | `string` | no |  |  | Optional preferred lane used as a tie breaker. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.capabilities.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show grouped renderer lane cards with support badges, package fit, recommended lane, and follow-up match or export-plan actions.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | no |  |  | Optional Motion package root to summarize against renderer capability cards. |
| `output` | `string` | no |  |  | Requested output such as png-frame, mp4-h264, mp4-hevc, webm-av1, webm-vp9, gif, cut-plan, or motion-package. |
| `target` | `string` | no |  |  | Requested workflow target such as preview, final, batch, cut, canvas, or handoff. |
| `needsAlpha` | `boolean` | no |  |  | Whether the selected lane must preserve alpha. |
| `needsAudio` | `boolean` | no |  |  | Whether the selected lane must handle audio. |
| `needsSubtitles` | `boolean` | no |  |  | Whether the selected lane must handle subtitles or captions. |
| `preferLane` | `string` | no |  |  | Optional preferred lane used as a tie breaker. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.export.presets`

Tier `read_motion` · mutates: no

**Purpose:** List supported export presets with codecs, extensions, MIME types, and audio/alpha support.

Takes no arguments.

### `motion.export.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show export presets as grouped UI cards with recommended choices and render arguments; optional retained platform-receipt coverage currently requires Linux.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt root. identity-stable receipt reads and receipt-derived controls currently require linux; macos and windows return capability_unavailable before reading or writing receipt state. Export presets remain portable when receipt coverage is omitted. |
| `requiredHosts` | `array` | no |  |  | Host ids that must be verified, as a string array. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.export.plan`

Tier `read_motion` · mutates: no

**Purpose:** Plan the export preset, feature impact, deterministic capture preflight, quality gates, and render follow-up arguments.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | no |  |  | Optional Motion package root to inspect before choosing an export preset. |
| `target` | `string` | no |  |  | Requested delivery target such as Cut, Canvas MP4, transparent overlay, thumbnail, or batch frames. |
| `preset` | `string` | no |  | [`exportPreset`](#argument-value-enumerations) (10 values) | Optional explicit export preset. If omitted Motion chooses a preset from target and feature needs. |
| `outputPath` | `string` | no | `out` |  | Optional final output path to include in follow-up render arguments. |
| `qualityManifestPath` | `string` | no | `manifestPath` |  | Optional quality manifest path to include in render and quality-check follow-ups. |
| `receiptsRoot` | `string` | no |  |  | Optional receipts root containing platform verification evidence. |
| `requiredHosts` | `array` | no |  |  | Required host ids for platform verification status. |
| `needsAlpha` | `boolean` | no |  |  | Whether the export must preserve transparency. |
| `needsAudio` | `boolean` | no |  |  | Whether the export must preserve audio. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

## timeline (188)

| Command | Required tier | Mutates |
|---|---|---:|
| [`motion.audio.master.set`](#motionaudiomasterset) | `edit_motion` | yes |
| [`motion.audio.crossfade.set`](#motionaudiocrossfadeset) | `edit_motion` | yes |
| [`motion.timeline.panel`](#motiontimelinepanel) | `read_motion` | no |
| [`motion.timeline.inspect`](#motiontimelineinspect) | `read_motion` | no |
| [`motion.timeline.playhead.set`](#motiontimelineplayheadset) | `draft_motion` | yes |
| [`motion.timeline.range.select`](#motiontimelinerangeselect) | `draft_motion` | yes |
| [`motion.timeline.viewport.set`](#motiontimelineviewportset) | `draft_motion` | yes |
| [`motion.timeline.duration.policy`](#motiontimelinedurationpolicy) | `read_motion` | no |
| [`motion.timeline.duration.policy.set`](#motiontimelinedurationpolicyset) | `edit_motion` | yes |
| [`motion.timeline.scene.create`](#motiontimelinescenecreate) | `edit_motion` | yes |
| [`motion.timeline.scene.delete`](#motiontimelinescenedelete) | `edit_motion` | yes |
| [`motion.timeline.scene.reorder`](#motiontimelinescenereorder) | `edit_motion` | yes |
| [`motion.timeline.scene.resize`](#motiontimelinesceneresize) | `edit_motion` | yes |
| [`motion.timeline.scene.name.set`](#motiontimelinescenenameset) | `edit_motion` | yes |
| [`motion.timeline.marker.upsert`](#motiontimelinemarkerupsert) | `edit_motion` | yes |
| [`motion.timeline.marker.delete`](#motiontimelinemarkerdelete) | `edit_motion` | yes |
| [`motion.timeline.keyframe.upsert`](#motiontimelinekeyframeupsert) | `edit_motion` | yes |
| [`motion.timeline.keyframe.delete`](#motiontimelinekeyframedelete) | `edit_motion` | yes |
| [`motion.timeline.keyframe.range.delete`](#motiontimelinekeyframerangedelete) | `edit_motion` | yes |
| [`motion.timeline.keyframe.move`](#motiontimelinekeyframemove) | `edit_motion` | yes |
| [`motion.timeline.keyframe.easing.apply`](#motiontimelinekeyframeeasingapply) | `edit_motion` | yes |
| [`motion.timeline.keyframe.shift`](#motiontimelinekeyframeshift) | `edit_motion` | yes |
| [`motion.timeline.keyframe.scale`](#motiontimelinekeyframescale) | `edit_motion` | yes |
| [`motion.timeline.keyframe.duplicate`](#motiontimelinekeyframeduplicate) | `edit_motion` | yes |
| [`motion.timeline.keyframe.distribute`](#motiontimelinekeyframedistribute) | `edit_motion` | yes |
| [`motion.timeline.keyframe.reverse`](#motiontimelinekeyframereverse) | `edit_motion` | yes |
| [`motion.timeline.keyframe.snap`](#motiontimelinekeyframesnap) | `edit_motion` | yes |
| [`motion.timeline.spatial.position.upsert`](#motiontimelinespatialpositionupsert) | `edit_motion` | yes |
| [`motion.timeline.spatial.position.move`](#motiontimelinespatialpositionmove) | `edit_motion` | yes |
| [`motion.timeline.spatial.position.delete`](#motiontimelinespatialpositiondelete) | `edit_motion` | yes |
| [`motion.revision.transaction.plan`](#motionrevisiontransactionplan) | `read_motion` | no |
| [`motion.revision.transaction`](#motionrevisiontransaction) | `edit_motion` | yes |
| [`motion.timeline.keyframes.panel`](#motiontimelinekeyframespanel) | `read_motion` | no |
| [`motion.timeline.transitions.panel`](#motiontimelinetransitionspanel) | `read_motion` | no |
| [`motion.timeline.easing.panel`](#motiontimelineeasingpanel) | `read_motion` | no |
| [`motion.timeline.easing.presets`](#motiontimelineeasingpresets) | `read_motion` | no |
| [`motion.timeline.animation.presets`](#motiontimelineanimationpresets) | `read_motion` | no |
| [`motion.timeline.animation.preset.apply`](#motiontimelineanimationpresetapply) | `edit_motion` | yes |
| [`motion.timeline.transition.presets`](#motiontimelinetransitionpresets) | `read_motion` | no |
| [`motion.timeline.transition.preset.apply`](#motiontimelinetransitionpresetapply) | `edit_motion` | yes |
| [`motion.timeline.layer.create`](#motiontimelinelayercreate) | `edit_motion` | yes |
| [`motion.timeline.layer.trim`](#motiontimelinelayertrim) | `edit_motion` | yes |
| [`motion.timeline.layer.split`](#motiontimelinelayersplit) | `edit_motion` | yes |
| [`motion.timeline.layer.text.set`](#motiontimelinelayertextset) | `edit_motion` | yes |
| [`motion.timeline.layer.style.set`](#motiontimelinelayerstyleset) | `edit_motion` | yes |
| [`motion.timeline.layer.transform.set`](#motiontimelinelayertransformset) | `edit_motion` | yes |
| [`motion.timeline.layer.effect.set`](#motiontimelinelayereffectset) | `edit_motion` | yes |
| [`motion.timeline.layer.rich.set`](#motiontimelinelayerrichset) | `edit_motion` | yes |
| [`motion.timeline.layer.blend.set`](#motiontimelinelayerblendset) | `edit_motion` | yes |
| [`motion.timeline.layer.crop.set`](#motiontimelinelayercropset) | `edit_motion` | yes |
| [`motion.timeline.layer.mask.set`](#motiontimelinelayermaskset) | `edit_motion` | yes |
| [`motion.timeline.layer.fit.set`](#motiontimelinelayerfitset) | `edit_motion` | yes |
| [`motion.timeline.layer.media.set`](#motiontimelinelayermediaset) | `edit_motion` | yes |
| [`motion.timeline.layer.name.set`](#motiontimelinelayernameset) | `edit_motion` | yes |
| [`motion.timeline.layer.visibility.set`](#motiontimelinelayervisibilityset) | `edit_motion` | yes |
| [`motion.timeline.layer.lock`](#motiontimelinelayerlock) | `edit_motion` | yes |
| [`motion.timeline.layer.delete`](#motiontimelinelayerdelete) | `edit_motion` | yes |
| [`motion.timeline.layer.duplicate`](#motiontimelinelayerduplicate) | `edit_motion` | yes |
| [`motion.timeline.layer.reorder`](#motiontimelinelayerreorder) | `edit_motion` | yes |
| [`motion.timeline.checkpoint-storyboard.create`](#motiontimelinecheckpoint-storyboardcreate) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.inspect`](#motiontimelinecheckpoint-storyboardinspect) | `read_motion` | no |
| [`motion.timeline.checkpoint-storyboard.revise`](#motiontimelinecheckpoint-storyboardrevise) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.remove`](#motiontimelinecheckpoint-storyboardremove) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.archive`](#motiontimelinecheckpoint-storyboardarchive) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.materialize`](#motiontimelinecheckpoint-storyboardmaterialize) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.detach`](#motiontimelinecheckpoint-storyboarddetach) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.behavior.resolve`](#motiontimelinecheckpoint-storyboardbehaviorresolve) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.behavior.detach`](#motiontimelinecheckpoint-storyboardbehaviordetach) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.relation.resolve`](#motiontimelinecheckpoint-storyboardrelationresolve) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.relation.detach`](#motiontimelinecheckpoint-storyboardrelationdetach) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.relation-action.resolve`](#motiontimelinecheckpoint-storyboardrelation-actionresolve) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.relation-action.detach`](#motiontimelinecheckpoint-storyboardrelation-actiondetach) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.lifecycle.resolve`](#motiontimelinecheckpoint-storyboardlifecycleresolve) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.lifecycle.detach`](#motiontimelinecheckpoint-storyboardlifecycledetach) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.geometry-morph.resolve`](#motiontimelinecheckpoint-storyboardgeometry-morphresolve) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.geometry-morph.detach`](#motiontimelinecheckpoint-storyboardgeometry-morphdetach) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.retained-trace.resolve`](#motiontimelinecheckpoint-storyboardretained-traceresolve) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.retained-trace.detach`](#motiontimelinecheckpoint-storyboardretained-tracedetach) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.retained-trace.preview`](#motiontimelinecheckpoint-storyboardretained-tracepreview) | `render_motion` | yes |
| [`motion.timeline.checkpoint-storyboard.retained-trace.review.bind`](#motiontimelinecheckpoint-storyboardretained-tracereviewbind) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.preview`](#motiontimelinecheckpoint-storyboardpreview) | `render_motion` | yes |
| [`motion.timeline.checkpoint-storyboard.creative-review.bind`](#motiontimelinecheckpoint-storyboardcreative-reviewbind) | `write_local` | yes |
| [`motion.timeline.checkpoint-storyboard.preview-quality.review`](#motiontimelinecheckpoint-storyboardpreview-qualityreview) | `write_local` | yes |
| [`motion.timeline.relations.inspect`](#motiontimelinerelationsinspect) | `read_motion` | no |
| [`motion.timeline.relations.upsert`](#motiontimelinerelationsupsert) | `edit_motion` | yes |
| [`motion.timeline.relations.enabled.set`](#motiontimelinerelationsenabledset) | `edit_motion` | yes |
| [`motion.timeline.relations.remove`](#motiontimelinerelationsremove) | `edit_motion` | yes |
| [`motion.timeline.relations.detach`](#motiontimelinerelationsdetach) | `edit_motion` | yes |
| [`motion.timeline.relations.bake`](#motiontimelinerelationsbake) | `edit_motion` | yes |
| [`motion.timeline.relation-actions.inspect`](#motiontimelinerelation-actionsinspect) | `read_motion` | no |
| [`motion.timeline.relation-actions.upsert`](#motiontimelinerelation-actionsupsert) | `edit_motion` | yes |
| [`motion.timeline.relation-actions.remove`](#motiontimelinerelation-actionsremove) | `edit_motion` | yes |
| [`motion.timeline.relation-actions.apply`](#motiontimelinerelation-actionsapply) | `edit_motion` | yes |
| [`motion.timeline.scene3d-animation.inspect`](#motiontimelinescene3d-animationinspect) | `read_motion` | no |
| [`motion.timeline.scene3d-animation.track.upsert`](#motiontimelinescene3d-animationtrackupsert) | `edit_motion` | yes |
| [`motion.timeline.scene3d-animation.track.remove`](#motiontimelinescene3d-animationtrackremove) | `edit_motion` | yes |
| [`motion.timeline.scene3d-animation.keyframe.upsert`](#motiontimelinescene3d-animationkeyframeupsert) | `edit_motion` | yes |
| [`motion.timeline.scene3d-animation.keyframe.delete`](#motiontimelinescene3d-animationkeyframedelete) | `edit_motion` | yes |
| [`motion.timeline.scene3d-animation.keyframe.move`](#motiontimelinescene3d-animationkeyframemove) | `edit_motion` | yes |
| [`motion.timeline.layout-gap-animation.inspect`](#motiontimelinelayout-gap-animationinspect) | `read_motion` | no |
| [`motion.timeline.layout-gap-animation.track.upsert`](#motiontimelinelayout-gap-animationtrackupsert) | `edit_motion` | yes |
| [`motion.timeline.layout-gap-animation.track.remove`](#motiontimelinelayout-gap-animationtrackremove) | `edit_motion` | yes |
| [`motion.timeline.layout-gap-animation.keyframe.upsert`](#motiontimelinelayout-gap-animationkeyframeupsert) | `edit_motion` | yes |
| [`motion.timeline.layout-gap-animation.keyframe.delete`](#motiontimelinelayout-gap-animationkeyframedelete) | `edit_motion` | yes |
| [`motion.timeline.layout-gap-animation.keyframe.move`](#motiontimelinelayout-gap-animationkeyframemove) | `edit_motion` | yes |
| [`motion.timeline.behaviors.inspect`](#motiontimelinebehaviorsinspect) | `read_motion` | no |
| [`motion.timeline.behaviors.upsert`](#motiontimelinebehaviorsupsert) | `edit_motion` | yes |
| [`motion.timeline.behaviors.remove`](#motiontimelinebehaviorsremove) | `edit_motion` | yes |
| [`motion.timeline.layer.text-runs.inspect`](#motiontimelinelayertext-runsinspect) | `read_motion` | no |
| [`motion.timeline.layer.text-runs.replace`](#motiontimelinelayertext-runsreplace) | `edit_motion` | yes |
| [`motion.timeline.layer.text-runs.remove`](#motiontimelinelayertext-runsremove) | `edit_motion` | yes |
| [`motion.timeline.group.create`](#motiontimelinegroupcreate) | `edit_motion` | yes |
| [`motion.timeline.group.child.add`](#motiontimelinegroupchildadd) | `edit_motion` | yes |
| [`motion.timeline.group.child.remove`](#motiontimelinegroupchildremove) | `edit_motion` | yes |
| [`motion.timeline.group.child.move`](#motiontimelinegroupchildmove) | `edit_motion` | yes |
| [`motion.timeline.group.child.reorder`](#motiontimelinegroupchildreorder) | `edit_motion` | yes |
| [`motion.timeline.group.wrap`](#motiontimelinegroupwrap) | `edit_motion` | yes |
| [`motion.timeline.group.unwrap`](#motiontimelinegroupunwrap) | `edit_motion` | yes |
| [`motion.timeline.group.delete`](#motiontimelinegroupdelete) | `edit_motion` | yes |
| [`motion.timeline.group.duplicate`](#motiontimelinegroupduplicate) | `edit_motion` | yes |
| [`motion.timeline.group.trim`](#motiontimelinegrouptrim) | `edit_motion` | yes |
| [`motion.timeline.group.root.reorder`](#motiontimelinegrouprootreorder) | `edit_motion` | yes |
| [`motion.timeline.group.split`](#motiontimelinegroupsplit) | `edit_motion` | yes |
| [`motion.timeline.layout.inspect`](#motiontimelinelayoutinspect) | `read_motion` | no |
| [`motion.timeline.layout.compile`](#motiontimelinelayoutcompile) | `read_motion` | no |
| [`motion.timeline.layout.apply`](#motiontimelinelayoutapply) | `edit_motion` | yes |
| [`motion.timeline.layout.remove`](#motiontimelinelayoutremove) | `edit_motion` | yes |
| [`motion.timeline.adjustment.fixed.inspect`](#motiontimelineadjustmentfixedinspect) | `read_motion` | no |
| [`motion.timeline.adjustment.fixed.set`](#motiontimelineadjustmentfixedset) | `edit_motion` | yes |
| [`motion.timeline.adjustment.fixed.remove`](#motiontimelineadjustmentfixedremove) | `edit_motion` | yes |
| [`motion.timeline.gradient.color-keyframes.inspect`](#motiontimelinegradientcolor-keyframesinspect) | `read_motion` | no |
| [`motion.timeline.gradient.color-keyframes.upsert`](#motiontimelinegradientcolor-keyframesupsert) | `edit_motion` | yes |
| [`motion.timeline.gradient.color-keyframes.delete`](#motiontimelinegradientcolor-keyframesdelete) | `edit_motion` | yes |
| [`motion.timeline.gradient.color-keyframes.move`](#motiontimelinegradientcolor-keyframesmove) | `edit_motion` | yes |
| [`motion.timeline.points.range.inspect`](#motiontimelinepointsrangeinspect) | `read_motion` | no |
| [`motion.timeline.points.trajectory.inspect`](#motiontimelinepointstrajectoryinspect) | `read_motion` | no |
| [`motion.timeline.points.point.upsert`](#motiontimelinepointspointupsert) | `edit_motion` | yes |
| [`motion.timeline.points.point.move`](#motiontimelinepointspointmove) | `edit_motion` | yes |
| [`motion.timeline.points.point.delete`](#motiontimelinepointspointdelete) | `edit_motion` | yes |
| [`motion.timeline.points.point.range.delete`](#motiontimelinepointspointrangedelete) | `edit_motion` | yes |
| [`motion.timeline.particles.structural.inspect`](#motiontimelineparticlesstructuralinspect) | `read_motion` | no |
| [`motion.timeline.particles.field.source.insert`](#motiontimelineparticlesfieldsourceinsert) | `edit_motion` | yes |
| [`motion.timeline.particles.field.source.replace`](#motiontimelineparticlesfieldsourcereplace) | `edit_motion` | yes |
| [`motion.timeline.particles.field.source.move`](#motiontimelineparticlesfieldsourcemove) | `edit_motion` | yes |
| [`motion.timeline.particles.field.source.delete`](#motiontimelineparticlesfieldsourcedelete) | `edit_motion` | yes |
| [`motion.timeline.particles.emitter.origin.insert`](#motiontimelineparticlesemitterorigininsert) | `edit_motion` | yes |
| [`motion.timeline.particles.emitter.origin.replace`](#motiontimelineparticlesemitteroriginreplace) | `edit_motion` | yes |
| [`motion.timeline.particles.emitter.origin.move`](#motiontimelineparticlesemitteroriginmove) | `edit_motion` | yes |
| [`motion.timeline.particles.emitter.origin.delete`](#motiontimelineparticlesemitterorigindelete) | `edit_motion` | yes |
| [`motion.timeline.particles.field.collision.axis.update`](#motiontimelineparticlesfieldcollisionaxisupdate) | `edit_motion` | yes |
| [`motion.timeline.particles.emitter.trail.add`](#motiontimelineparticlesemittertrailadd) | `edit_motion` | yes |
| [`motion.timeline.particles.emitter.trail.replace`](#motiontimelineparticlesemittertrailreplace) | `edit_motion` | yes |
| [`motion.timeline.particles.emitter.trail.remove`](#motiontimelineparticlesemittertrailremove) | `edit_motion` | yes |
| [`motion.timeline.particles.emitter.shading.add`](#motiontimelineparticlesemittershadingadd) | `edit_motion` | yes |
| [`motion.timeline.particles.emitter.shading.replace`](#motiontimelineparticlesemittershadingreplace) | `edit_motion` | yes |
| [`motion.timeline.particles.emitter.shading.remove`](#motiontimelineparticlesemittershadingremove) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry.inspect`](#motiontimelineshapegeometryinspect) | `read_motion` | no |
| [`motion.timeline.shape.geometry.replace`](#motiontimelineshapegeometryreplace) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry.point.update`](#motiontimelineshapegeometrypointupdate) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry.point.insert`](#motiontimelineshapegeometrypointinsert) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry.point.move`](#motiontimelineshapegeometrypointmove) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry.point.range.delete`](#motiontimelineshapegeometrypointrangedelete) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry.arc.update`](#motiontimelineshapegeometryarcupdate) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry.path.replace`](#motiontimelineshapegeometrypathreplace) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry.legacy.migrate`](#motiontimelineshapegeometrylegacymigrate) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry.dash.set`](#motiontimelineshapegeometrydashset) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry.dash.remove`](#motiontimelineshapegeometrydashremove) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry-keyframes.inspect`](#motiontimelineshapegeometry-keyframesinspect) | `read_motion` | no |
| [`motion.timeline.shape.geometry-keyframes.upsert`](#motiontimelineshapegeometry-keyframesupsert) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry-keyframes.delete`](#motiontimelineshapegeometry-keyframesdelete) | `edit_motion` | yes |
| [`motion.timeline.shape.geometry-keyframes.move`](#motiontimelineshapegeometry-keyframesmove) | `edit_motion` | yes |
| [`motion.timeline.cleanup`](#motiontimelinecleanup) | `edit_motion` | yes |
| [`motion.timeline.track.create`](#motiontimelinetrackcreate) | `edit_motion` | yes |
| [`motion.timeline.track.reorder`](#motiontimelinetrackreorder) | `edit_motion` | yes |
| [`motion.timeline.track.delete`](#motiontimelinetrackdelete) | `edit_motion` | yes |
| [`motion.timeline.track.rename`](#motiontimelinetrackrename) | `edit_motion` | yes |
| [`motion.timeline.track.lock`](#motiontimelinetracklock) | `edit_motion` | yes |
| [`motion.timeline.track.mute`](#motiontimelinetrackmute) | `edit_motion` | yes |
| [`motion.timeline.track.solo`](#motiontimelinetracksolo) | `edit_motion` | yes |
| [`motion.timeline.track.volume`](#motiontimelinetrackvolume) | `edit_motion` | yes |
| [`motion.timeline.track.fade`](#motiontimelinetrackfade) | `edit_motion` | yes |
| [`motion.timeline.track.pan`](#motiontimelinetrackpan) | `edit_motion` | yes |
| [`motion.timeline.layer.ducking.set`](#motiontimelinelayerduckingset) | `edit_motion` | yes |
| [`motion.timeline.layer.track.assign`](#motiontimelinelayertrackassign) | `edit_motion` | yes |
| [`motion.timeline.caption.import`](#motiontimelinecaptionimport) | `edit_motion` | yes |
| [`motion.timeline.caption.upsert`](#motiontimelinecaptionupsert) | `edit_motion` | yes |
| [`motion.timeline.transition.upsert`](#motiontimelinetransitionupsert) | `edit_motion` | yes |
| [`motion.timeline.transition.delete`](#motiontimelinetransitiondelete) | `edit_motion` | yes |

### `motion.audio.master.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set or clear bounded document-level final-program audio controls; a declared loudness target is realized post-mix and proved only by a delivered-file receipt. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `master` | `object` | no |  |  | Bounded document master object: gain, fades, and an optional post-mix loudness target. It accepts no scripts, filter graphs, plugins, or external sources. |
| `clear` | `boolean` | no |  |  | Remove the document master instead of setting master. Cannot be combined with master. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.audio.crossfade.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Apply a matched linear or equal-power crossfade to two already-exactly-overlapping audio sources without moving their timing. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `fromLayerId` | `string` | yes | `fromLayer` |  | Outgoing audio or video-with-audio layer id. |
| `toLayerId` | `string` | yes | `toLayer` |  | Incoming audio or video-with-audio layer id. |
| `durationMs` | `number` | yes |  |  | Positive overlap length in milliseconds. It must exactly align the outgoing end with the incoming start; this command never moves layers. |
| `curve` | `string` | no |  | `linear`, `equal-power` | Matched fade curve applied to both layers. Defaults to `equal-power`. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show the timeline as panel-ready playhead controls, layer rows, scenes, tracks, markers, and suggested actions.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect timeline scenes, tracks, markers, and layer track refs.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.playhead.set`

Tier `draft_motion` · mutates: yes

**Purpose:** On Linux, move the timeline playhead as durable editor state without mutating MotionIR; unsupported hosts refuse before creating state.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror for the control-state receipt. Durable playhead, range, and viewport persistence currently requires Linux; macOS and Windows return capability_unavailable before creating package state or a receipt. |
| `atMs` | `number` | no |  |  | New playhead time in milliseconds. Required unless playheadMs is given; must be within the motion duration. |
| `playheadMs` | `number` | no |  |  | Alternate name for atMs. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.range.select`

Tier `draft_motion` · mutates: yes

**Purpose:** On Linux, select an in/out timeline range as durable editor state without mutating MotionIR; unsupported hosts refuse before creating state.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror for the control-state receipt. Durable playhead, range, and viewport persistence currently requires Linux; macOS and Windows return capability_unavailable before creating package state or a receipt. |
| `startMs` | `number` | yes |  |  | Selection start in milliseconds. |
| `endMs` | `number` | yes |  |  | Selection end in milliseconds; must be at or after startMs and within the motion duration. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.viewport.set`

Tier `draft_motion` · mutates: yes

**Purpose:** On Linux, set timeline viewport range, zoom, and pixels-per-second as durable editor state; unsupported hosts refuse before creating state.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror for the control-state receipt. Durable playhead, range, and viewport persistence currently requires Linux; macOS and Windows return capability_unavailable before creating package state or a receipt. |
| `startMs` | `number` | yes |  |  | Visible window start in milliseconds. |
| `endMs` | `number` | yes |  |  | Visible window end in milliseconds; must be greater than startMs and within the motion duration. |
| `zoom` | `number` | no |  |  | Optional zoom factor; must be positive. |
| `pixelsPerSecond` | `number` | no |  |  | Optional horizontal scale; must be positive. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.duration.policy`

Tier `read_motion` · mutates: no

**Purpose:** Read package duration policy and protected intro/outro regions.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.duration.policy.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set package duration policy metadata for protected regions, min/max duration, and resize behavior. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `policy` | `object` | yes |  |  | Duration policy { minDurationMs?, maxDurationMs?, resizeMode?, protectedRegions? }. resizeMode is one of the durationResizeMode values; protectedRegions is an array of { id, startMs, durationMs } with unique ids. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene.create`

Tier `edit_motion` · mutates: yes

**Purpose:** Create a storyboard scene with timing, optional display name, and optional track or marker refs. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `sceneId` | `string` | yes | `scene`, `id` |  | Target scene id. |
| `name` | `string` | no | `sceneName` |  | Optional display name for the scene. |
| `startMs` | `number` | yes |  |  | Scene start in milliseconds. |
| `durationMs` | `number` | yes |  |  | Scene duration in milliseconds; must be positive. |
| `index` | `number` | no |  |  | Insertion index in the scene list; appended when omitted. |
| `layerIds` | `array` | no |  |  | Layer ids to attach, as a string array. |
| `trackIds` | `array` | no |  |  | Track ids to attach, as a string array. |
| `markerIds` | `array` | no |  |  | Marker ids to attach, as a string array. |
| `layerId` | `string` | no | `layer` |  | Single layer id to attach, in place of layerIds. |
| `trackId` | `string` | no | `track` |  | Single track id to attach, in place of trackIds. |
| `markerId` | `string` | no | `marker` |  | Single marker id to attach, in place of markerIds. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove a storyboard scene while preserving referenced timeline content and package duration. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `sceneId` | `string` | yes | `scene`, `id` |  | Target scene id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene.reorder`

Tier `edit_motion` · mutates: yes

**Purpose:** Reorder storyboard scene rows while preserving timing, refs, and package duration. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `sceneId` | `string` | yes | `scene`, `id` |  | Target scene id. |
| `index` | `number` | yes |  |  | New position in the scene list. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene.resize`

Tier `edit_motion` · mutates: yes

**Purpose:** Resize a scene duration and optionally ripple later scenes, layers, and markers. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `sceneId` | `string` | yes | `scene` |  | Target scene id. |
| `durationMs` | `number` | yes |  |  | New scene duration in milliseconds; must be positive. |
| `ripple` | `boolean` | no |  |  | Shift later scenes by the same delta instead of leaving a gap or overlap. Defaults to `false`. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene.name.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set a timeline scene display name without renaming stable scene ids or hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `sceneId` | `string` | yes | `scene` |  | Target scene id. |
| `name` | `string` | yes | `sceneName`, `value` |  | New scene name; must not be blank. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.marker.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace a timeline marker and optionally attach it to a scene. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `id` | `string` | yes | `markerId` |  | Marker id; an existing marker with this id is replaced. |
| `atMs` | `number` | yes |  |  | Marker time in milliseconds. |
| `durationMs` | `number` | no |  |  | Optional marker span in milliseconds; a point marker when omitted. |
| `label` | `string` | no |  |  | Optional marker label. |
| `type` | `string` | no |  |  | Optional marker classification such as chapter or note. |
| `color` | `string` | no |  |  | Optional marker color. |
| `sceneId` | `string` | no | `scene` |  | Optional scene to attach the marker to. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.marker.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove a timeline marker and prune stale scene marker refs. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `id` | `string` | yes | `markerId` |  | Marker id to remove. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace a typed timeline keyframe without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `atMs` | `number` | yes |  |  | Keyframe time in milliseconds. An existing keyframe at this time is replaced. |
| `value` | `number,string` | yes |  |  | Keyframe value: a finite NUMBER for numeric targets (transform.*, opacity), or a CSS colour string for colour targets (fill, stroke). Numeric targets reject a numeric string such as "0". |
| `easing` | `string,object` | no |  |  | Exact bounded Motion easing. Core remains the single evaluator authority. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove a typed timeline keyframe without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `atMs` | `number` | yes |  |  | Time of the keyframe to remove, in milliseconds. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.range.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove a selected typed keyframe range while preserving other targets and pruning empty keyframe containers. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `startMs` | `number` | no |  |  | Window start in milliseconds; unbounded when omitted. |
| `endMs` | `number` | no |  |  | Window end in milliseconds; unbounded when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Move an existing typed timeline keyframe to a new timestamp while preserving its value and easing. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `fromMs` | `number` | yes |  |  | Current time of the keyframe, in milliseconds. |
| `toMs` | `number` | yes |  |  | New time for the keyframe, in milliseconds. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.easing.apply`

Tier `edit_motion` · mutates: yes

**Purpose:** Apply an easing preset to one keyframe or a selected keyframe time range. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `easing` | `string,object` | yes |  |  | Exact bounded Motion easing. Core remains the single evaluator authority. |
| `atMs` | `number` | no |  |  | Apply to the single keyframe at this time; use startMs/endMs for a window instead. |
| `startMs` | `number` | no |  |  | Window start in milliseconds; unbounded when omitted. |
| `endMs` | `number` | no |  |  | Window end in milliseconds; unbounded when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.shift`

Tier `edit_motion` · mutates: yes

**Purpose:** Shift a selected typed keyframe range by a positive or negative delta while preserving values and easing. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `deltaMs` | `number` | yes |  |  | Milliseconds to move the selected keyframes; must be finite and non-zero. |
| `startMs` | `number` | no |  |  | Window start in milliseconds; unbounded when omitted. |
| `endMs` | `number` | no |  |  | Window end in milliseconds; unbounded when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.scale`

Tier `edit_motion` · mutates: yes

**Purpose:** Stretch or compress a selected typed keyframe range around an origin while preserving values and easing. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `scale` | `number` | yes |  |  | Time-scale factor; must be positive and not 1. |
| `originMs` | `number` | yes |  |  | Fixed point the scaling pivots around, in milliseconds. |
| `startMs` | `number` | no |  |  | Window start in milliseconds; unbounded when omitted. |
| `endMs` | `number` | no |  |  | Window end in milliseconds; unbounded when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.duplicate`

Tier `edit_motion` · mutates: yes

**Purpose:** Copy a selected typed keyframe range by a positive or negative offset while preserving original keyframes, values, and easing. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `deltaMs` | `number` | yes |  |  | Offset for the duplicated keyframes; must be finite and non-zero. |
| `startMs` | `number` | no |  |  | Window start in milliseconds; unbounded when omitted. |
| `endMs` | `number` | no |  |  | Window end in milliseconds; unbounded when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.distribute`

Tier `edit_motion` · mutates: yes

**Purpose:** Space three or more typed keyframes evenly between the first and last selected timestamps while preserving values and easing. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `startMs` | `number` | no |  |  | Window start in milliseconds; unbounded when omitted. |
| `endMs` | `number` | no |  |  | Window end in milliseconds; unbounded when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.reverse`

Tier `edit_motion` · mutates: yes

**Purpose:** Reverse a selected typed keyframe range in time while preserving values and easing. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `startMs` | `number` | no |  |  | Window start in milliseconds; unbounded when omitted. |
| `endMs` | `number` | no |  |  | Window end in milliseconds; unbounded when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframe.snap`

Tier `edit_motion` · mutates: yes

**Purpose:** Snap a selected typed keyframe range to the package frame grid while preserving values and easing. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `target` | `string` | yes |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Animated property path the keyframes belong to. |
| `fps` | `number` | no |  |  | Frame rate to snap to; must be positive. Defaults to the motion document fps. |
| `mode` | `string` | no |  | `nearest`, `floor`, `ceil` | Rounding direction; nearest when omitted. |
| `startMs` | `number` | no |  |  | Window start in milliseconds; unbounded when omitted. |
| `endMs` | `number` | no |  |  | Window end in milliseconds; unbounded when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.spatial.position.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace one aligned transform.x/transform.y spatial keyframe at a non-negative millisecond time, with optional closed path handles, in one copy-on-write revision. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `atMs` | `number` | yes |  |  | Position time in milliseconds. |
| `x` | `number` | yes |  |  | Horizontal position in pixels. |
| `y` | `number` | yes |  |  | Vertical position in pixels. |
| `easing` | `string,object` | no |  |  | Exact bounded Motion easing. Core remains the single evaluator authority. |
| `spatial` | `object` | no |  |  | Optional tangent control { mode, in: { x, y }, out: { x, y } }. mode must be one of the spatialTangentMode values and all four handle numbers must be finite. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.spatial.position.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Move one aligned transform.x/transform.y spatial keyframe pair to a non-negative millisecond time in one copy-on-write revision, preserving its value, easing, and path geometry. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `fromMs` | `number` | yes |  |  | Current time of the spatial position, in milliseconds. |
| `toMs` | `number` | yes |  |  | New time for the spatial position, in milliseconds. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.spatial.position.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete one aligned transform.x/transform.y spatial keyframe pair at a non-negative millisecond time in one copy-on-write revision. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `atMs` | `number` | yes |  |  | Time of the spatial position to remove, in milliseconds. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.revision.transaction.plan`

Tier `read_motion` · mutates: no

**Purpose:** Preview a bounded list of typed timeline edits without writing a package or receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Existing Motion package root, at most 4096 UTF-8 bytes with no NUL byte, inside host-configured authoring input roots. |
| `base` | `object` | yes |  |  | Exact identity and authored-document hashes observed on the source revision. |
| `steps` | `array` | yes |  |  | Ordered, closed, data-only mutation steps replayed only in memory to predict the final document. Planning writes no package or receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.revision.transaction`

Tier `edit_motion` · mutates: yes

**Purpose:** Apply a bounded list of typed timeline edits atomically as one copy-on-write package revision. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root, at most 4096 UTF-8 bytes with no NUL byte. It is never changed in place. |
| `outDir` | `string` | yes |  |  | One absent or empty output directory outside packageRoot, at most 4096 UTF-8 bytes with no NUL byte. The completed revision is atomically published here. |
| `base` | `object` | yes |  |  | Exact identity and authored-document hashes observed on the source revision. |
| `steps` | `array` | yes |  |  | Ordered, closed, data-only mutation steps. They run against one copied base package and publish once only when every step succeeds. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the one aggregate package receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.keyframes.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show animated layers, keyframe target ranges, easing usage, preset counts, and suggested keyframe actions.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root to inspect for timeline keyframes. |
| `layerId` | `string` | no | `layer` |  | Optional layer id filter. |
| `target` | `string` | no |  | [`keyframeTarget`](#argument-value-enumerations) (115 values) | Optional keyframe target filter. |
| `includeEmpty` | `boolean` | no |  |  | Include layers that have no matching keyframes. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.transitions.panel`

Tier `read_motion` · mutates: no

**Purpose:** Show enter and exit transitions with timing windows, easing usage, type counts, and suggested transition actions.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root to inspect for layer transitions. |
| `layerId` | `string` | no | `layer` |  | Optional layer id filter. |
| `edge` | `string` | no |  | `in`, `out` | Optional transition edge filter. |
| `includeEmpty` | `boolean` | no |  |  | Include layers that have no matching transitions. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.easing.panel`

Tier `read_motion` · mutates: no

**Purpose:** Inspect sampled easing curves and package usage across keyframes and transitions.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root to inspect for easing curve usage. |
| `sampleCount` | `number` | no |  |  | Number of normalized t/value samples to include per easing curve. Capped because the panel samples every easing row, so the cost is rowCount * sampleCount. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.easing.presets`

Tier `read_motion` · mutates: no

**Purpose:** List named and cubic-bezier easing presets available for keyframes and transitions.

Takes no arguments.

### `motion.timeline.animation.presets`

Tier `read_motion` · mutates: no

**Purpose:** List reusable entrance and exit animation presets available for layer keyframes.

Takes no arguments.

### `motion.timeline.animation.preset.apply`

Tier `edit_motion` · mutates: yes

**Purpose:** Apply a reusable animation preset to one or more layers as deterministic keyframes with receipt evidence. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | no | `layer` |  | Single target layer. Supply this or layerIds. |
| `layerIds` | `array` | no | `layers` |  | Target layer ids as a string array; enables staggering across the group. |
| `preset` | `string` | yes |  | `fade-in`, `fade-out`, `slide-up-in`, `slide-down-out`, `lower-third-in`, `lower-third-out` | Animation preset to apply. |
| `startMs` | `number` | no |  |  | Preset start time in milliseconds; the layer start when omitted. |
| `durationMs` | `number` | no |  |  | Preset duration in milliseconds; the preset default when omitted. |
| `distancePx` | `number` | no |  |  | Travel distance for slide-style presets, in pixels. |
| `staggerMs` | `number` | no |  |  | Delay added per layer when layerIds is used. |
| `easing` | `string,object` | no |  |  | Exact bounded Motion easing. Core remains the single evaluator authority. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.transition.presets`

Tier `read_motion` · mutates: no

**Purpose:** List reusable named transition presets with lane and use-case guidance.

Takes no arguments.

### `motion.timeline.transition.preset.apply`

Tier `edit_motion` · mutates: yes

**Purpose:** Apply one named transition preset to a layer as validated transitions, keyframes, and effects. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `preset` | `string` | yes |  | [`transitionPreset`](#argument-value-enumerations) (7 values) | Named transition preset to apply. |
| `durationMs` | `number` | no |  |  | Preset duration in milliseconds; must be positive when supplied. |
| `direction` | `string` | no |  | `left`, `right`, `up`, `down` | Optional direction override for directional presets. |
| `distance` | `number` | no |  |  | Optional non-negative travel distance override in pixels. |
| `easing` | `string` | no |  |  | Named Motion easing or a bounded functional cubic-bezier(...)/steps(...) string (at most 256 UTF-16 code units). |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.create`

Tier `edit_motion` · mutates: yes

**Purpose:** Create a typed text, shape, media, environment, points, or other bounded data-only layer with stack and optional track placement evidence. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layer` | `object` | no |  |  | Full layer object (id, type, startMs, durationMs, plus optional name/opacity/transform/keyframes/transitions/mask/effects/environment/emitter/pointCloud/blendMode/crop/ducking/style). A points layer carries bounded declarative data, never code; particles and points may declare static effects.trail lookback strokes (durationMs 1..2000, samples 2..8), not physics, history, formulas, or GPU work. A particles emitter may carry one to three bounded analytic radial/vortex field sources. Supply this, or the shorthand fields below. |
| `layerId` | `string` | no | `layer` |  | Id for the new layer. Required unless layer.id is set. |
| `trackId` | `string` | no | `track` |  | Track to attach the new layer to. |
| `startMs` | `number` | no |  |  | Layer start time in milliseconds. Required unless layer.startMs is set. |
| `durationMs` | `number` | no |  |  | Layer duration in milliseconds; must be positive. Required unless layer.durationMs is set. |
| `index` | `number` | no |  |  | Insertion index in the layer stack; appended when omitted. |
| `trackIndex` | `number` | no |  |  | Insertion index inside the target track's layer list. |
| `type` | `string` | no |  |  | Layer type such as text, shape, image, video, audio, or environment. |
| `text` | `string` | no |  |  | Text content for text layers. |
| `shape` | `string` | no |  |  | Shape kind for shape layers such as rect or ellipse. |
| `fill` | `string` | no |  |  | Fill color for shape layers. |
| `source` | `string` | no |  |  | Media source path relative to the package for image, video, and audio layers. |
| `src` | `string` | no |  |  | Alternate media source field written straight onto the layer. |
| `assetId` | `string` | no |  |  | Package asset id to bind the layer to. |
| `assetRef` | `string` | no |  |  | Package asset reference to bind the layer to. |
| `color` | `string` | no |  |  | Text color; folded into layer.style.color. |
| `fontSize` | `number` | no |  |  | Text size in pixels; folded into layer.style.fontSize. Must be positive. |
| `width` | `number` | no |  |  | Layer width in pixels. Must be positive. |
| `height` | `number` | no |  |  | Layer height in pixels. Must be positive. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.trim`

Tier `edit_motion` · mutates: yes

**Purpose:** Update layer start, duration, and optional media source trim fields without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `startMs` | `number` | no |  |  | New timeline start in milliseconds. |
| `durationMs` | `number` | no |  |  | New timeline duration in milliseconds; must be positive. |
| `trimStartMs` | `number` | no |  |  | New in-point inside the source media, in milliseconds. |
| `trimDurationMs` | `number` | no |  |  | New length of the used source span; must be positive. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.split`

Tier `edit_motion` · mutates: yes

**Purpose:** Split a layer at an absolute timeline timestamp while preserving segment timing, media trim, keyframes, transitions, and track order. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `atMs` | `number` | yes |  |  | Absolute timeline position of the cut, in milliseconds. |
| `newLayerId` | `string` | no | `newLayer` |  | Id for the layer created after the cut; derived when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.text.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set text on an existing text or caption layer without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `text` | `string` | yes | `value` |  | New text content. An empty string is accepted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.style.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set a supported top-level layer style property without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `property` | `string` | yes | `property`, `styleProperty` |  | Style property name such as color, fontSize, backgroundColor, or letterSpacing. |
| `value` | `string` | yes | `styleValue` |  | New value. Strings, numbers, and booleans are all accepted; the type is validated against the target property. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.transform.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set a supported static layer transform or opacity value without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `property` | `string` | yes | `property`, `transformProperty` |  | Transform property name: x, y, width, height, opacity, scale, rotation, originX, or originY. |
| `value` | `string` | yes | `transformValue` |  | New value. Strings, numbers, and booleans are all accepted; the type is validated against the target property. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.effect.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set a supported static layer visual effect value without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `property` | `string` | yes | `property`, `effectProperty` |  | Effect property name: blur, brightness, contrast, saturate, or grayscale. |
| `value` | `string` | yes | `effectValue` |  | New value. Strings, numbers, and booleans are all accepted; the type is validated against the target property. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.rich.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set one inspector-exposed, allow-listed path reveal, environment, shader, particle, 3D, camera/depth, motion-blur, or film control. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `property` | `string` | yes | `property`, `richPath` |  | Dotted path into the layer's rich controls, for example pathReveal.start, pathReveal.end, effects.trail.durationMs, effects.trail.samples, environment.intensity, or emitter.field.sources.0.strength. Trails are static particles/points fields, not keyframe targets. |
| `value` | `string` | yes | `richValue` |  | New value. Strings, numbers, and booleans are all accepted; the type is validated against the target property. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.blend.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set a supported static layer blend mode without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `blendMode` | `string` | yes | `mode`, `value` | [`blendMode`](#argument-value-enumerations) (17 values) | New compositing blend mode. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.crop.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set a static image or video source crop rectangle without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `crop` | `object` | no |  |  | Crop rectangle { x, y, width, height }. All four fields are required when this object is used. |
| `x` | `number` | no |  |  | Crop left edge; used when crop is not supplied. Required with y, width, and height. |
| `y` | `number` | no |  |  | Crop top edge; used when crop is not supplied. |
| `width` | `number` | no |  |  | Crop width; used when crop is not supplied. |
| `height` | `number` | no |  |  | Crop height; used when crop is not supplied. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.mask.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set a static visual-layer rectangular or rounded mask without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `mask` | `object` | no |  |  | Mask object { type, inset?: { top, right, bottom, left }, radius? }. type is required. |
| `type` | `string` | no | `maskType` |  | Mask type; used when mask is not supplied. Required in that form. |
| `radius` | `number` | no | `maskRadius` |  | Corner radius; used when mask is not supplied. |
| `top` | `number` | no |  |  | Mask inset from the top; used when mask is not supplied. |
| `right` | `number` | no |  |  | Mask inset from the right; used when mask is not supplied. |
| `bottom` | `number` | no |  |  | Mask inset from the bottom; used when mask is not supplied. |
| `left` | `number` | no |  |  | Mask inset from the left; used when mask is not supplied. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.fit.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set a static image or video media fit mode without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `fit` | `string` | yes | `value`, `mode` | `fill`, `contain`, `cover`, `none`, `scale-down` | How the media fills the layer box. Image and video layers only. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.media.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set a media layer source and canonicalize stale direct source aliases without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `source` | `string` | yes | `assetRef`, `src`, `ref` |  | New media source path or asset reference. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.name.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set a timeline layer display name without renaming stable layer ids or hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `name` | `string` | yes | `layerName`, `value` |  | New display name; must not be blank. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.visibility.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Hide or show a timeline layer without deleting it or hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `visible` | `boolean` | yes |  |  | Whether the layer renders. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.lock`

Tier `edit_motion` · mutates: yes

**Purpose:** Lock or unlock a timeline layer with receipt evidence so later edit commands can honor layer protection. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `locked` | `boolean` | yes |  |  | Whether further edits to this layer are refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete a layer while removing stale track refs and preserving receipt evidence. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.duplicate`

Tier `edit_motion` · mutates: yes

**Purpose:** Duplicate a layer while preserving animation, transitions, timing, and track order evidence. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `newLayerId` | `string` | no | `newLayer` |  | Id for the duplicate; derived when omitted. |
| `offsetMs` | `number` | no |  |  | Milliseconds to shift the duplicate later on the timeline. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.reorder`

Tier `edit_motion` · mutates: yes

**Purpose:** Reorder a layer in the global timeline stack while syncing track layer order evidence. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `index` | `number` | yes |  |  | New position in the layer stack. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.create`

Tier `write_local` · mutates: yes

**Purpose:** Seal one bounded checkpoint-storyboard descriptor as an immutable host-owned record; it does not create or render a package.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `descriptor` | `object` | yes |  |  | Closed C6C record profile union through retained-trace B7 plus the C6D trace and choreography data-recipe lowerers. A lineage cannot switch profile on revision. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Read one exact sealed checkpoint-storyboard record, lineage, bindings, and review state without mutating it.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Closed exact host-sealed checkpoint storyboard record identity; loose ids and names are refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.revise`

Tier `write_local` · mutates: yes

**Purpose:** Seal a successor checkpoint-storyboard record from an exact active parent; the original sealed record remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `parent` | `object` | yes |  |  | Exact active host-sealed parent identity to reopen; the host injects it into the new sealed revision. |
| `descriptor` | `object` | yes |  |  | Closed C6C record profile union through retained-trace B7 plus the C6D trace and choreography data-recipe lowerers. A lineage cannot switch profile on revision. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.remove`

Tier `write_local` · mutates: yes

**Purpose:** Tombstone one exact checkpoint-storyboard record while retaining its sealed audit bytes.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Closed exact host-sealed checkpoint storyboard record identity; loose ids and names are refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.archive`

Tier `write_local` · mutates: yes

**Purpose:** Terminally archive one checkpoint-storyboard lineage after its required bindings have been retired.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Closed exact host-sealed checkpoint storyboard record identity; loose ids and names are refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.materialize`

Tier `write_local` · mutates: yes

**Purpose:** Ask the host-owned authority to bind one exact storyboard record to its configured materialized package output.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact host-sealed storyboard identity. The host alone selects source, workspace, output, and private C6B bindings. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.detach`

Tier `write_local` · mutates: yes

**Purpose:** Retire one verified storyboard materialization binding without deleting the materialized package.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact host-sealed storyboard identity. Detach only retires a verified durable binding; it never deletes its package. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.behavior.resolve`

Tier `write_local` · mutates: yes

**Purpose:** Ask the host-owned C6B2 authority to resolve one exact behavior storyboard record into its configured output binding.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B2 behavior-record identity. This Debug/MCP-only command accepts no package, workspace, output, receipt, approval, renderer, object, layer, or authority field. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.behavior.detach`

Tier `write_local` · mutates: yes

**Purpose:** Retire one verified C6B2 behavior-resolution binding without deleting its installed output.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B2 behavior-record identity. Detach retires only the durable behavior-resolution link; it never deletes the installed output and accepts no host authority data. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.relation.resolve`

Tier `write_local` · mutates: yes

**Purpose:** Ask the host-owned C6B3 authority to resolve one exact relation storyboard record into its configured output binding.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B3 relation-record identity. This Debug/MCP-only command accepts no package, workspace, output, receipt, approval, renderer, object, layer, or authority field. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.relation.detach`

Tier `write_local` · mutates: yes

**Purpose:** Retire one verified C6B3 relation-resolution binding without deleting its installed output.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B3 relation-record identity. Detach retires only the durable relation-resolution link; it never deletes the installed output and accepts no host authority data. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.relation-action.resolve`

Tier `write_local` · mutates: yes

**Purpose:** Ask the host-owned C6B4 authority to resolve one exact relation-action storyboard record into its configured output binding.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B4 relation-action record identity. This Debug/MCP-only command accepts no package, workspace, output, receipt, approval, renderer, object, layer, action, or authority field. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.relation-action.detach`

Tier `write_local` · mutates: yes

**Purpose:** Retire one verified C6B4 relation-action-resolution binding without deleting its installed output.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B4 relation-action record identity. Detach retires only the durable relation-action-resolution link; it never deletes the installed output and accepts no host authority data. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.lifecycle.resolve`

Tier `write_local` · mutates: yes

**Purpose:** Ask the host-owned C6B5 authority to resolve one exact lifecycle storyboard record into its configured output binding.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B5 lifecycle-record identity. This Debug/MCP-only command accepts no package, workspace, output, receipt, approval, renderer, object, layer, or authority field. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.lifecycle.detach`

Tier `write_local` · mutates: yes

**Purpose:** Retire one verified C6B5 lifecycle-resolution binding without deleting its installed output.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B5 lifecycle-record identity. Detach retires only the durable lifecycle-resolution link; it never deletes the installed output and accepts no host authority data. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.geometry-morph.resolve`

Tier `write_local` · mutates: yes

**Purpose:** Ask the host-owned C6B6 authority to resolve one exact geometry-morph storyboard record into its configured output binding.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B6 geometry-morph record identity. This Debug/MCP-only command accepts no package, workspace, output, receipt, approval, renderer, object, layer, geometry, or authority field. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.geometry-morph.detach`

Tier `write_local` · mutates: yes

**Purpose:** Retire one verified C6B6 geometry-morph-resolution binding without deleting its installed output.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B6 geometry-morph record identity. Detach retires only the durable geometry-morph-resolution link; it never deletes the installed output and accepts no host authority data. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.retained-trace.resolve`

Tier `write_local` · mutates: yes

**Purpose:** Ask the host-owned C6B7 authority to resolve one exact retained-trace storyboard record into its configured output binding.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B7 retained-trace record identity. This Debug/MCP-only command accepts no package, workspace, output, sidecar, receipt, approval, renderer, GPU, trace, or authority field. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.retained-trace.detach`

Tier `write_local` · mutates: yes

**Purpose:** Retire one verified C6B7 retained-trace-resolution binding without deleting its installed output.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B7 retained-trace record identity. Debug/MCP-only: detach retires only the durable retained-trace-resolution link; it never deletes the installed output and accepts no host authority data. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.retained-trace.preview`

Tier `render_motion` · mutates: yes

**Purpose:** Render one exact scheduled retained-trace sample through the host-owned C6B7 preview binding.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact sealed C6B7 retained-trace record identity with one active host-resolved output binding. |
| `atUs` | `number` | yes |  |  | Exact integer microsecond in the sealed retained-trace schedule, including the final document sample. This Debug/MCP-only command accepts no path, package, output, sidecar, receipt, plan, trace, lane, renderer, GPU, session, or authority field. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.retained-trace.review.bind`

Tier `write_local` · mutates: yes

**Purpose:** Bind one host-minted review decision to an exact receipt-backed retained-trace preview.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact active nonarchived C6B7 record identity. |
| `preview` | `object` | yes |  |  | Exact paired opaque handles for one complete arbitrary-time B7 preview. |
| `reviewHandle` | `string` | yes |  |  | Host-minted opaque decision handle. The command accepts no reviewer, decision, storyboard, recipe, package, frame, receipt, path, renderer, or authority data. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.preview`

Tier `render_motion` · mutates: yes

**Purpose:** Render one exact checkpoint or time target through the host-owned receipt-backed storyboard preview authority.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact active host-sealed storyboard identity bound through B1a to the C6B output. |
| `target` | `object` | yes |  |  | Closed target union. Checkpoint times must seal to whole milliseconds. B1d accepts whole-millisecond checkpoint and time targets in [0, duration]. The exact terminal duration D is a background-only terminal-boundary sample: all layers remain excluded with no hold or final-state claim. This command accepts no package, output, workflow, network, lane, or playhead fields. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.creative-review.bind`

Tier `write_local` · mutates: yes

**Purpose:** Bind one host-minted creative-review result to an exact receipt-backed storyboard PNG preview.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact active nonarchived host-sealed storyboard identity. |
| `preview` | `object` | yes |  |  | Exact paired opaque handles for one complete B1b receipt-first PNG preview. |
| `creativeReviewHandle` | `string` | yes |  |  | Exact opaque host-minted creative-review handle. It selects no caller-supplied brief, plan, ledger, run, approval, reviewer, shot, or authentication data. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.checkpoint-storyboard.preview-quality.review`

Tier `write_local` · mutates: yes

**Purpose:** Associate one exact storyboard PNG preview with its authenticated interior or terminal-endpoint quality review; it cannot establish final acceptance.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `identity` | `object` | yes |  |  | Exact active nonarchived host-sealed storyboard identity. |
| `preview` | `object` | yes |  |  | Exact paired opaque handles for one complete B1b v2 receipt-first PNG preview. |
| `review` | `object` | yes |  |  | Closed quality-association union. `interior` reopens one durable end-exclusive B1c association. `terminal-endpoint` requires a separate host-only witness at exact D and carries no visible-final-state, held-content, human pixel review, final-media, or final-acceptance claim. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.relations.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect the bounded root-owned-shape attach or aim relation store and, when requested, its whole-millisecond frame plan without mutation or a receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `atUs` | `number` | no |  |  | Optional exact inspection playhead. This authoring bridge currently accepts whole-millisecond-representable microseconds only. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.relations.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace one bounded attach (follow or similarity) or aim relation between root-owned shape endpoints in a copy-on-write revision, within 32 bindings and a one-hour relation duration. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Receipt mirroring is selected only by the trusted Debug host. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `binding` | `object` | yes |  |  | One exact attach/follow/similarity or aim binding. Core owns target authority, DAG, lock, time, and budget validation. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.relations.enabled.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set one persisted relation's enabled state in a copy-on-write revision while retaining its validated transform-authority reservation. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Receipt mirroring is selected only by the trusted Debug host. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `id` | `string` | yes |  |  | Stable relation id. |
| `enabled` | `boolean` | yes |  |  | New persisted enable state; the mask remains reserved either way. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.relations.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one persisted relation definition in a copy-on-write revision without baking ordinary keyframes. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Receipt mirroring is selected only by the trusted Debug host. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `id` | `string` | yes |  |  | Stable relation id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.relations.detach`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one persisted relation in a copy-on-write revision while leaving the target's current ordinary transform unchanged; it never writes a synthetic final hold. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Receipt mirroring is selected only by the trusted Debug host. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `id` | `string` | yes |  |  | Stable relation id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.relations.bake`

Tier `edit_motion` · mutates: yes

**Purpose:** Bake one full-document relation into ordinary transform keyframes in a copy-on-write revision, using a whole-millisecond inclusive grid of at most 3,600 samples; the sampled result is not equivalent between samples. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Receipt mirroring is selected only by the trusted Debug host. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `id` | `string` | yes |  |  | Stable relation id. |
| `sampleEveryUs` | `number` | yes |  |  | Positive safe-integer interval in microseconds. Core requires a whole-millisecond multiple and an inclusive grid of at most 3,600 samples. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.relation-actions.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect persisted closed relation-action definitions and their render truth without mutating a package or materializing an instance.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.relation-actions.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace one closed persisted relation-action definition in a copy-on-write revision, within 16 definitions, 32 template layers, 16 roles/parameters/relations, and 32 sequence steps. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source package root. The source is never edited in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent copy-on-write destination outside packageRoot. |
| `createdBy` | `string` | no |  |  | Optional attribution for the one mutation receipt. |
| `definition` | `object` | yes |  |  | One closed persisted relation-actions@2 definition; Core validates all exact branch and aggregate caps. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.relation-actions.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one persisted relation-action definition in a copy-on-write revision without searching for or changing already materialized output. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source package root. The source is never edited in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent copy-on-write destination outside packageRoot. |
| `createdBy` | `string` | no |  |  | Optional attribution for the one mutation receipt. |
| `id` | `string` | yes |  |  | Safe stable identifier. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.relation-actions.apply`

Tier `edit_motion` · mutates: yes

**Purpose:** Materialize one exact-package-base relation action into ordinary data in a copy-on-write revision, capped at 32 created layers, 16 relations, and 128 generated keyframe writes; the request is not persisted as an instance. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source package root. The source is never edited in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent copy-on-write destination outside packageRoot. |
| `createdBy` | `string` | no |  |  | Optional attribution for the one mutation receipt. |
| `expectedPackageId` | `string` | yes |  |  | Safe stable identifier. |
| `expectedPackageManifestSha256` | `string` | yes |  |  | Lowercase SHA-256 identity. |
| `request` | `object` | yes |  |  | Ephemeral exact-base Core materialization request; never persisted as instance data. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene3d-animation.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect the persisted closed Scene3D animation store, stable track ids, and route-specific render refusal without mutating a package or rendering a frame.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene3d-animation.track.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace one complete locator-typed Scene3D track in a copy-on-write revision; it allows one track per immutable locator, at most 64 tracks, 64 exact-microsecond keyframes per track, and 2,048 total keyframes, never a generic property path. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Host receipt mirrors are configured by the trusted Debug host, never by a caller field. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `track` | `object` | yes |  |  | One complete closed scene3d track. Its id can replace only the same immutable locator; a second track for that locator is refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene3d-animation.track.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one persisted Scene3D animation track in a host-receipted copy-on-write revision without changing static scene topology or assets. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Host receipt mirrors are configured by the trusted Debug host, never by a caller field. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes |  |  | Safe stable identifier; Core requires `[A-Za-z0-9][A-Za-z0-9._-]{0,63}`. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene3d-animation.keyframe.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace one locator-typed Scene3D keyframe at an exact safe-integer microsecond timestamp in a host-receipted copy-on-write revision. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Host receipt mirrors are configured by the trusted Debug host, never by a caller field. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes |  |  | Safe stable identifier; Core requires `[A-Za-z0-9][A-Za-z0-9._-]{0,63}`. |
| `keyframe` | `object` | yes |  |  | One complete exact-time typed keyframe. Core resolves value kind/bounds from the stored track locator. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene3d-animation.keyframe.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete one exact-microsecond Scene3D keyframe in a host-receipted copy-on-write revision while retaining the track's required keyframe. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Host receipt mirrors are configured by the trusted Debug host, never by a caller field. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes |  |  | Safe stable identifier; Core requires `[A-Za-z0-9][A-Za-z0-9._-]{0,63}`. |
| `atUs` | `number` | yes |  |  | Exact safe-integer physical microsecond timestamp; milliseconds and floating-point time are refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.scene3d-animation.keyframe.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Move one Scene3D keyframe between distinct exact safe-integer microsecond timestamps in a host-receipted copy-on-write revision, preserving its typed value and easing. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Host receipt mirrors are configured by the trusted Debug host, never by a caller field. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes |  |  | Safe stable identifier; Core requires `[A-Za-z0-9][A-Za-z0-9._-]{0,63}`. |
| `fromAtUs` | `number` | yes |  |  | Exact safe-integer physical microsecond timestamp; milliseconds and floating-point time are refused. |
| `toAtUs` | `number` | yes |  |  | Exact safe-integer physical microsecond timestamp; milliseconds and floating-point time are refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layout-gap-animation.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect the persisted C2 gap-only track for an existing static layout application without mutation, receipt creation, or a renderer route.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layout-gap-animation.track.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace the single permitted application-bound row or column gap track in a host-receipted copy-on-write revision, with exact application fingerprint, ordered child ids, and at most 64 keyframes. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; host-owned layout continuation authority and receipt mirrors are configured by the trusted Debug host. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `track` | `object` | yes |  |  | One application-bound row/column gap track. Generic paths and transform/timing targets are not accepted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layout-gap-animation.track.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one C2 gap track in a host-receipted copy-on-write revision; removing the final track restores static-layout removal authority and still creates no renderer route. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; host-owned layout continuation authority and receipt mirrors are configured by the trusted Debug host. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes |  |  | Safe stable identifier; Core requires `[A-Za-z0-9][A-Za-z0-9._-]{0,63}`. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layout-gap-animation.keyframe.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace one gap-only C2 keyframe at an exact safe-integer microsecond timestamp in a host-receipted copy-on-write revision; its value is 0 through 1,000,000 layout units. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; host-owned layout continuation authority and receipt mirrors are configured by the trusted Debug host. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes |  |  | Safe stable identifier; Core requires `[A-Za-z0-9][A-Za-z0-9._-]{0,63}`. |
| `keyframe` | `object` | yes |  |  | One exact gap keyframe; terminal state holds and it never becomes a generic keyframe target. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layout-gap-animation.keyframe.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete one exact-microsecond C2 gap keyframe in a host-receipted copy-on-write revision while retaining the track's required keyframe. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; host-owned layout continuation authority and receipt mirrors are configured by the trusted Debug host. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes |  |  | Safe stable identifier; Core requires `[A-Za-z0-9][A-Za-z0-9._-]{0,63}`. |
| `atUs` | `number` | yes |  |  | Exact safe-integer microsecond timestamp; milliseconds and floating point are refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layout-gap-animation.keyframe.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Move one C2 gap keyframe between distinct exact safe-integer microsecond timestamps in a host-receipted copy-on-write revision, preserving its non-overshooting easing. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; host-owned layout continuation authority and receipt mirrors are configured by the trusted Debug host. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes |  |  | Safe stable identifier; Core requires `[A-Za-z0-9][A-Za-z0-9._-]{0,63}`. |
| `fromAtUs` | `number` | yes |  |  | Exact safe-integer microsecond timestamp; milliseconds and floating point are refused. |
| `toAtUs` | `number` | yes |  |  | Exact safe-integer microsecond timestamp; milliseconds and floating point are refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.behaviors.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect bounded analytic behavior bindings, transform authority, and static plan facts without mutation or a receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.behaviors.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace one closed path-follow or analytic transform behavior binding for an eligible root-owned shape in a copy-on-write revision, within 32 bindings and a one-hour physical duration. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Any behavior receipt mirror is configured by the trusted Debug host; callers must not supply receiptsRoot. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `binding` | `object` | yes |  |  | One exact behavior binding. Core validates target ownership, transform conflicts, timing, path topology, work, and store budgets. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.behaviors.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one target layer's persisted analytic behavior binding in a copy-on-write revision without baking synthetic transform keyframes. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Any behavior receipt mirror is configured by the trusted Debug host; callers must not supply receiptsRoot. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `targetLayerId` | `string` | yes |  |  | Existing behavior target layer id to remove. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.text-runs.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect one text or caption layer's manifest-bound styled text-runs record and immutable font-asset authority without mutation or a receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `layerId` | `string` | yes |  |  | Target text or caption layer identifier. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.text-runs.replace`

Tier `edit_motion` · mutates: yes

**Purpose:** Replace one layer's complete text-runs@1 record in a copy-on-write revision, capped at 32 runs, 16 distinct font assets, and 16 KiB concatenated UTF-8 text. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes |  |  | Target text or caption layer identifier. |
| `textRuns` | `object` | yes |  |  | Closed text-runs@1 record. The run fontAssetId is the sole family, weight, and style authority. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.text-runs.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one layer's text-runs styling in a copy-on-write revision only when the supplied plain text exactly matches the concatenated runs, preserving that content. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes |  |  | Target text or caption layer identifier. |
| `expectedPlainText` | `string` | yes |  |  | Must exactly equal the concatenated run text. Removal derives that same plain text and cannot change content. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.create`

Tier `edit_motion` · mutates: yes

**Purpose:** Create one complete bounded group layer at a root or direct group position in a copy-on-write revision; Core validates the full hierarchy, timing, and placement. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `group` | `object` | yes |  |  | Group layer data. Create requires a complete group with type group, startMs, durationMs, and childLayerIds; wrap derives those fields and accepts only id plus neutral metadata. Core validates all structure and bounds. |
| `layerIndex` | `number` | no |  |  | Flat layer-store insertion index; appended when omitted. |
| `parentGroupId` | `string` | no |  |  | Optional parent group id; omit for a root group. |
| `childIndex` | `number` | no |  |  | Insertion index in the parent group; appended when omitted. |
| `trackIndex` | `number` | no |  |  | Insertion index in the selected group track. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.child.add`

Tier `edit_motion` · mutates: yes

**Purpose:** Add one existing layer as a direct child of a group in a copy-on-write revision, optionally at a bounded insertion index. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `groupId` | `string` | yes |  |  | Target group layer id. |
| `childLayerId` | `string` | yes |  |  | Direct child layer id. |
| `index` | `number` | no |  |  | Child insertion index; appended when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.child.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one direct child membership from a group in a copy-on-write revision without deleting the child layer itself. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `groupId` | `string` | yes |  |  | Target group layer id. |
| `childLayerId` | `string` | yes |  |  | Direct child layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.child.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Move one direct child from its explicitly named source owner, including root, into a destination group in a copy-on-write revision. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `sourceGroupId` | `string,null` | yes |  |  | Current direct parent id, or null when the child is a root layer. This must be explicit. |
| `destinationGroupId` | `string` | yes |  |  | Destination direct parent group id. |
| `childLayerId` | `string` | yes |  |  | Direct child layer id. |
| `index` | `number` | no |  |  | Destination child insertion index; appended when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.child.reorder`

Tier `edit_motion` · mutates: yes

**Purpose:** Reorder one direct child within its group at a zero-based index in a copy-on-write revision. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `groupId` | `string` | yes |  |  | Target group layer id. |
| `childLayerId` | `string` | yes |  |  | Direct child layer id. |
| `index` | `number` | yes |  |  | Zero-based insertion or reorder index. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.wrap`

Tier `edit_motion` · mutates: yes

**Purpose:** Wrap one contiguous range of 1 through 256 direct siblings in a group in a copy-on-write revision, normalizing the supplied order to the owner order. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `group` | `object` | yes |  |  | Group layer data. Create requires a complete group with type group, startMs, durationMs, and childLayerIds; wrap derives those fields and accepts only id plus neutral metadata. Core validates all structure and bounds. |
| `childLayerIds` | `array` | yes |  |  | One contiguous range of direct sibling ids. Input order is normalized to owner order. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.unwrap`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one group container in a copy-on-write revision and promote its direct children through Core's validated hierarchy operation. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `groupId` | `string` | yes |  |  | Target group layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete one group in a copy-on-write revision by cascading its subtree or, only for an exactly neutral group, unwrapping its children. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `groupId` | `string` | yes |  |  | Target group layer id. |
| `disposition` | `string` | yes |  | `cascade`, `unwrap` | Required child disposition: cascade deletes the full subtree; unwrap only succeeds for an exactly neutral group. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.duplicate`

Tier `edit_motion` · mutates: yes

**Purpose:** Duplicate one group subtree in a copy-on-write revision, optionally with a deterministic new root id and a non-negative timeline offset. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `groupId` | `string` | yes |  |  | Target group layer id. |
| `newGroupId` | `string` | no |  |  | Optional deterministic id for the cloned root group. |
| `offsetMs` | `number` | no |  |  | Non-negative timeline offset for the cloned root group. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.trim`

Tier `edit_motion` · mutates: yes

**Purpose:** Retiming one group in its direct owner's timeline in a copy-on-write revision, only when every direct child still fits its positive duration. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `groupId` | `string` | yes |  |  | Target group layer id. |
| `startMs` | `number` | no |  |  | Optional new group start in its direct owner's local timeline. |
| `durationMs` | `number` | no |  |  | Optional new positive group duration; every direct child must still fit. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.root.reorder`

Tier `edit_motion` · mutates: yes

**Purpose:** Reorder one root group at a zero-based index in a copy-on-write revision without treating it as a nested-child reorder. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `groupId` | `string` | yes |  |  | Target group layer id. |
| `index` | `number` | yes |  |  | Zero-based insertion or reorder index. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.group.split`

Tier `edit_motion` · mutates: yes

**Purpose:** Split one group at a timestamp strictly inside its direct-owner timeline into head and tail groups in a copy-on-write revision. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `groupId` | `string` | yes |  |  | Target group layer id. |
| `atMs` | `number` | yes |  |  | Split point in the direct owner's timeline; it must be strictly inside the group. |
| `newGroupId` | `string` | no |  |  | Optional explicit id for the new tail group. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layout.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect deterministic layout ownership, slots, overflow facts, budgets, and fingerprint for a proposed closed layout and repeaters without a receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `groupId` | `string` | yes |  |  | Bounded Motion layer identifier. |
| `layout` | `object` | yes |  |  | Inspect deterministic layout ownership, slots, budgets, overflow facts, and fingerprint without a receipt. |
| `repeaters` | `array` | yes |  |  | Sorted unique source repeaters; Core checks source ownership, derived bounds, and compile budget. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layout.compile`

Tier `read_motion` · mutates: no

**Purpose:** Compile deterministic slots, repeater expansion, overflow facts, budgets, and fingerprint for a proposed closed layout without mutating a package or writing a receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `groupId` | `string` | yes |  |  | Bounded Motion layer identifier. |
| `layout` | `object` | yes |  |  | Compile deterministic layout slots, repeaters, budgets, overflow facts, and fingerprint without a receipt. |
| `repeaters` | `array` | yes |  |  | Sorted unique source repeaters; Core checks source ownership, derived bounds, and compile budget. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layout.apply`

Tier `edit_motion` · mutates: yes

**Purpose:** Apply one closed deterministic row, column, stack, grid, or radial layout with up to 16 repeaters of 128 instances in a host-receipted copy-on-write revision and return its application fingerprint. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Layout apply/remove require an immutable receipt-authority root configured by the trusted Debug host; callers must not supply receiptsRoot. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `groupId` | `string` | yes |  |  | Bounded Motion layer identifier. |
| `layout` | `object` | yes |  |  | One exact shellx-motion/layout@1 variant; no expression, custom solver, or code is accepted. |
| `repeaters` | `array` | yes |  |  | Sorted unique source repeaters; Core checks source ownership, derived bounds, and compile budget. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layout.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one exact fingerprint-bound layout application in a host-receipted copy-on-write revision through trusted authority, restoring its owned layout changes; an active C2 gap track refuses removal. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Layout apply/remove require an immutable receipt-authority root configured by the trusted Debug host; callers must not supply receiptsRoot. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `removal` | `object` | yes |  |  | Exact stable marker for a document-resident layout application. The trusted Debug host verifies immutable apply authority before Core permits removal. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.adjustment.fixed.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect one root fixed adjustment layer and its closed full-frame effects without mutation or a receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.adjustment.fixed.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Create or replace one complete root fixed adjustment in a copy-on-write revision using only vignette and/or deterministic film grain, evaluated in canonical vignette-then-filmGrain order. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Any host receipt mirror is configured by the trusted Debug host; callers must not supply receiptsRoot. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `adjustment` | `object` | yes |  |  | Complete closed adjustment record. Set replaces an existing exact id or appends a new root stack entry; it accepts no index, track, transform, module, or extension fields. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.adjustment.fixed.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one root fixed adjustment layer in a copy-on-write revision without accepting transform, track, module, or extension fields. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Any host receipt mirror is configured by the trusted Debug host; callers must not supply receiptsRoot. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.gradient.color-keyframes.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect one gradient layer's fixed-topology stop-color snapshot store and evaluation without mutation or a receipt.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.gradient.color-keyframes.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace one complete exact-microsecond stop-color snapshot in a copy-on-write revision, preserving the existing stop order and count; each gradient holds at most 32 snapshots of at most 16 colors. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `snapshot` | `object` | yes |  |  | Exact color snapshot for shellx-motion/gradient-color-keyframes@1; each gradient holds 1..32 ordered snapshots. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.gradient.color-keyframes.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete one exact-microsecond gradient color snapshot in a copy-on-write revision while retaining the required one-snapshot floor and fixed stop topology. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `atUs` | `number` | yes |  |  | Exact non-negative safe-integer microsecond timestamp; Core rejects fractional values. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.gradient.color-keyframes.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Move one gradient color snapshot between exact safe-integer microsecond timestamps in a copy-on-write revision, then keep the fixed-topology snapshots sorted by time. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `fromAtUs` | `number` | yes |  |  | Exact non-negative safe-integer microsecond timestamp; Core rejects fractional values. |
| `toAtUs` | `number` | yes |  |  | Exact non-negative safe-integer microsecond timestamp; Core rejects fractional values. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.points.range.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect one exact half-open range of authored point identities and sample slices without mutation, interpolation, or history; the response is capped at 256 points and 12 samples.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `layerId` | `string` | yes |  |  | Target points-layer identifier; this surface uses layerId only. |
| `startIndex` | `number` | yes |  |  | Inclusive stable point index starting the half-open range. |
| `endIndexExclusive` | `number` | yes |  |  | Exclusive point range endpoint; Core returns at most 256 points. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.points.trajectory.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect one stable point's authored base value and up to 12 exact sample positions without mutation; it reports no retained particle or point history.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `layerId` | `string` | yes |  |  | Target points-layer identifier; this surface uses layerId only. |
| `index` | `number` | yes |  |  | Stable zero-based point index; Core checks it against the current authored cloud. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.points.point.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Explicitly insert or replace one stable indexed point in a copy-on-write revision, supplying one aligned position for every authored sample when the point order or sampled value changes. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes |  |  | Target points-layer identifier; this surface uses layerId only. |
| `index` | `number` | yes |  |  | Stable zero-based point index; Core checks it against the current authored cloud. |
| `insert` | `boolean` | no |  |  | Explicitly true to insert at index; omit or false to replace the existing stable identity. |
| `point` | `object` | yes |  |  | Exact base-point record. Point identity is its stable ordered index, not an invented id. |
| `samplePositions` | `array` | no |  |  | One position for every existing authored sample in source order. Required for sampled insertion; Core refuses any partial or misaligned vector. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.points.point.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Move one stable point index and every parallel authored sample position in lockstep in a copy-on-write revision. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes |  |  | Target points-layer identifier; this surface uses layerId only. |
| `fromIndex` | `number` | yes |  |  | Existing stable point index to move. |
| `toIndex` | `number` | yes |  |  | Destination index after removing fromIndex; every sample position moves in lockstep. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.points.point.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete one stable point identity and its matching positions from every authored sample in a copy-on-write revision while leaving at least one base point. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes |  |  | Target points-layer identifier; this surface uses layerId only. |
| `index` | `number` | yes |  |  | Stable zero-based point index; Core checks it against the current authored cloud. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.points.point.range.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete one exact half-open stable-point range and its matching positions from every authored sample in a copy-on-write revision while leaving at least one base point. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes |  |  | Target points-layer identifier; this surface uses layerId only. |
| `startIndex` | `number` | yes |  |  | Inclusive stable point index starting the half-open range. |
| `endIndexExclusive` | `number` | yes |  |  | Exclusive stable point index ending the half-open [startIndex, endIndexExclusive) range. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.structural.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect one particle layer's bounded analytic field sources, emitter origins, trail, shading, and current limits without mutating it.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.field.source.insert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert one complete bounded analytic field-source record at an ordered index in a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `index` | `number` | yes |  |  | Zero-based bounded record index; Core checks the current list length. |
| `source` | `object` | yes |  |  | Complete exact source record. v1 admits radial/vortex only and at most 3; v2 admits all listed kinds and at most 4. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.field.source.replace`

Tier `edit_motion` · mutates: yes

**Purpose:** Replace one existing bounded analytic field-source record at its ordered index in a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `index` | `number` | yes |  |  | Zero-based bounded record index; Core checks the current list length. |
| `source` | `object` | yes |  |  | Complete exact source record. v1 admits radial/vortex only and at most 3; v2 admits all listed kinds and at most 4. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.field.source.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Move one bounded analytic field-source record to a different ordered index in a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `fromIndex` | `number` | yes |  |  | Zero-based bounded record index; Core checks the current list length. |
| `toIndex` | `number` | yes |  |  | Zero-based bounded record index; Core checks the current list length. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.field.source.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one bounded analytic field-source record at its ordered index from a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `index` | `number` | yes |  |  | Zero-based bounded record index; Core checks the current list length. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.emitter.origin.insert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert one bounded emitter spawn-origin record at an ordered index in a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `index` | `number` | yes |  |  | Insertion position in the bounded v2 origin order. |
| `origin` | `object` | yes |  |  | Exact plain-data record; optional fields: directionOffsetDeg, speedScale. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.emitter.origin.replace`

Tier `edit_motion` · mutates: yes

**Purpose:** Replace one existing bounded emitter spawn-origin record at its ordered index in a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `index` | `number` | yes |  |  | Existing bounded v2 origin index. |
| `origin` | `object` | yes |  |  | Exact plain-data record; optional fields: directionOffsetDeg, speedScale. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.emitter.origin.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Move one bounded emitter spawn-origin record to a different ordered index in a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `fromIndex` | `number` | yes |  |  | Existing v2 origin index. |
| `toIndex` | `number` | yes |  |  | Final v2 origin position after removal/reinsertion. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.emitter.origin.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove one bounded emitter spawn-origin record at its ordered index from a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `index` | `number` | yes |  |  | Existing bounded v2 origin index. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.field.collision.axis.update`

Tier `edit_motion` · mutates: yes

**Purpose:** Change only the x/y collision axis of one existing bounded collision field source in a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `index` | `number` | yes |  |  | Zero-based bounded record index; Core checks the current list length. |
| `axis` | `string` | yes |  | `x`, `y` | Replacement axis on an existing v2 collision source. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.emitter.trail.add`

Tier `edit_motion` · mutates: yes

**Purpose:** Add one bounded analytic trail to a copied particle layer that does not already declare one. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `trail` | `object` | yes |  |  | Exact plain-data record; optional fields: opacity. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.emitter.trail.replace`

Tier `edit_motion` · mutates: yes

**Purpose:** Replace the existing bounded analytic trail in a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `trail` | `object` | yes |  |  | Exact plain-data record; optional fields: opacity. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.emitter.trail.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove the existing bounded analytic trail from a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.emitter.shading.add`

Tier `edit_motion` · mutates: yes

**Purpose:** Add one bounded shading record to a copied particle layer that does not already declare one. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `shading` | `object` | yes |  |  | Exact plain-data record; optional fields: sizeJitter, opacityJitter, glow. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.emitter.shading.replace`

Tier `edit_motion` · mutates: yes

**Purpose:** Replace the existing bounded shading record in a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `shading` | `object` | yes |  |  | Exact plain-data record; optional fields: sizeJitter, opacityJitter, glow. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.particles.emitter.shading.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove the existing bounded shading record from a copied particle layer. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect one shape layer's v1 or legacy geometry, stroke dash, and resolved contour without mutating it.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.replace`

Tier `edit_motion` · mutates: yes

**Purpose:** Replace one shape layer's complete v1 geometry in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `geometry` | `object` | yes |  |  | Complete exact shellx-motion/shape-geometry@1 record; partial merge is not supported. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.point.update`

Tier `edit_motion` · mutates: yes

**Purpose:** Replace one indexed point in a v1 line, polyline, or polygon geometry in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `index` | `number` | yes |  |  | Zero-based geometry point index. |
| `point` | `object` | yes |  |  | Exact finite geometry-space point. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.point.insert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert one point at an ordered index in a v1 line, polyline, or polygon geometry in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `index` | `number` | yes |  |  | Zero-based geometry point index. |
| `point` | `object` | yes |  |  | Exact finite geometry-space point. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.point.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Reorder one existing point between ordered indexes in a v1 line, polyline, or polygon geometry in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `fromIndex` | `number` | yes |  |  | Existing zero-based point index. |
| `toIndex` | `number` | yes |  |  | Destination zero-based point index after removal. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.point.range.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete one half-open indexed point range from a v1 line, polyline, or polygon geometry while retaining its required minimum in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `startIndex` | `number` | yes |  |  | Inclusive zero-based start of the point range. |
| `endIndexExclusive` | `number` | yes |  |  | Exclusive zero-based end of the half-open point range. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.arc.update`

Tier `edit_motion` · mutates: yes

**Purpose:** Update one or more bounded arc or sector controls in a v1 shape geometry in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `center` | `object` | no |  |  | Optional exact finite arc or sector center. |
| `radius` | `number` | no |  |  | Optional finite outer radius. |
| `innerRadius` | `number` | no |  |  | Optional finite sector inner radius. |
| `startAngleDeg` | `number` | no |  |  | Optional finite start angle in degrees. |
| `sweepAngleDeg` | `number` | no |  |  | Optional finite signed sweep angle in degrees. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.path.replace`

Tier `edit_motion` · mutates: yes

**Purpose:** Replace only the bounded v1 path data string in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `data` | `string` | yes |  |  | Complete bounded path data for a v1 path geometry. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.legacy.migrate`

Tier `edit_motion` · mutates: yes

**Purpose:** One-way migrate one legacy path or freeform shape to v1 path geometry with resolved-contour equivalence evidence in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.dash.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set one bounded v1 shape stroke dash array and optional phase offset in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `strokeDasharray` | `array` | yes |  |  | Positive rendered-length dash runs; odd arrays repeat once under SVG semantics. |
| `strokeDashoffset` | `number` | no |  |  | Optional rendered-length phase offset. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry.dash.remove`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove both stroke dash fields from a v1 shape geometry in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry-keyframes.inspect`

Tier `read_motion` · mutates: no

**Purpose:** Inspect one v1 shape layer's persisted exact-time geometry snapshots and evaluation without mutating it.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry-keyframes.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace one complete fixed-topology v1 geometry snapshot at an exact microsecond in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Any host receipt mirror is configured by the trusted Debug host; callers must not supply receiptsRoot. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `snapshot` | `object` | yes |  |  | One complete fixed-topology shape geometry snapshot. It replaces only an equal exact atUs; partial geometry patches and generic keyframe targets are refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry-keyframes.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete one exact-time v1 geometry snapshot while retaining a non-empty snapshot record in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Any host receipt mirror is configured by the trusted Debug host; callers must not supply receiptsRoot. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `atUs` | `number` | yes |  |  | Exact safe-integer physical microsecond timestamp; no milliseconds or floating-point bridge is accepted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.shape.geometry-keyframes.move`

Tier `edit_motion` · mutates: yes

**Purpose:** Move one v1 geometry snapshot to a different exact microsecond while preserving its complete geometry in a copy-on-write package. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. Any host receipt mirror is configured by the trusted Debug host; callers must not supply receiptsRoot. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `fromAtUs` | `number` | yes |  |  | Exact safe-integer physical microsecond timestamp; no milliseconds or floating-point bridge is accepted. |
| `toAtUs` | `number` | yes |  |  | Exact safe-integer physical microsecond timestamp; no milliseconds or floating-point bridge is accepted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.cleanup`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove stale or duplicate track, scene, and marker refs while normalizing package duration with receipt evidence. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.track.create`

Tier `edit_motion` · mutates: yes

**Purpose:** Create a timeline track and optionally attach existing untracked layers with receipt evidence. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `track` | `object` | no |  |  | Full track object (id, type, plus optional name/layerIds/order/volume/pan/fade/locked/muted/solo). Supply this or the shorthand fields. |
| `trackId` | `string` | no | `track` |  | Id for the new track. Required unless track.id is set. |
| `type` | `string` | no |  |  | Track type such as video, audio, or caption. Required unless track.type is set. |
| `name` | `string` | no |  |  | Optional display name. |
| `layerIds` | `array` | no | `layers` |  | Layer ids to attach, as a string array. |
| `index` | `number` | no |  |  | Insertion index in the track list; appended when omitted. |
| `order` | `number` | no |  |  | Explicit sort order value; must be finite. |
| `volume` | `number` | no |  |  | Initial track volume; must be non-negative. |
| `pan` | `number` | no |  |  | Initial stereo pan between -1 and 1. |
| `fadeInMs` | `number` | no |  |  | Initial fade-in length in milliseconds. |
| `fadeOutMs` | `number` | no |  |  | Initial fade-out length in milliseconds. |
| `locked` | `boolean` | no |  |  | Whether the track starts locked. |
| `muted` | `boolean` | no |  |  | Whether the track starts muted. |
| `solo` | `boolean` | no |  |  | Whether the track starts soloed. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.track.reorder`

Tier `edit_motion` · mutates: yes

**Purpose:** Reorder timeline tracks with old/new stack index and track-order receipt evidence. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes | `track` |  | Target track id. |
| `index` | `number` | yes |  |  | New position in the track list. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.track.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Delete a timeline track while preserving layers, optionally detaching layer refs, and pruning scene refs. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes | `track` |  | Target track id. |
| `detachLayers` | `boolean` | no |  |  | Detach the track's layers instead of leaving them referenced. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.track.rename`

Tier `edit_motion` · mutates: yes

**Purpose:** Rename a timeline track with old/new name and changed-path receipt evidence. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes | `track` |  | Target track id. |
| `name` | `string` | yes | `trackName` |  | New track name; must not be blank. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.track.lock`

Tier `edit_motion` · mutates: yes

**Purpose:** Lock or unlock a timeline track with receipt evidence so later edit commands can honor track protection. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes | `track` |  | Target track id. |
| `locked` | `boolean` | yes |  |  | Whether edits to layers on this track are refused. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.track.mute`

Tier `edit_motion` · mutates: yes

**Purpose:** Mute or unmute a timeline track with receipt evidence so final renders can honor track audio state. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes | `track` |  | Target track id. |
| `muted` | `boolean` | yes |  |  | Whether the track is silenced. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.track.solo`

Tier `edit_motion` · mutates: yes

**Purpose:** Solo or unsolo a timeline track with receipt evidence so final renders can isolate track audio state. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes | `track` |  | Target track id. |
| `solo` | `boolean` | yes |  |  | Whether only soloed tracks are audible. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.track.volume`

Tier `edit_motion` · mutates: yes

**Purpose:** Set timeline track gain with receipt evidence so final renders can honor track audio level. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes | `track` |  | Target track id. |
| `volume` | `number` | yes |  |  | Track volume; must be a non-negative finite number. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.track.fade`

Tier `edit_motion` · mutates: yes

**Purpose:** Set timeline track fade-in and fade-out defaults with receipt evidence so final renders can honor track audio fades. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes | `track` |  | Target track id. |
| `fadeInMs` | `number` | no |  |  | Fade-in length in milliseconds. At least one of fadeInMs or fadeOutMs is required. |
| `fadeOutMs` | `number` | no |  |  | Fade-out length in milliseconds. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.track.pan`

Tier `edit_motion` · mutates: yes

**Purpose:** Set timeline track stereo balance with receipt evidence so final renders can honor track audio pan. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `trackId` | `string` | yes | `track` |  | Target track id. |
| `pan` | `number` | yes |  |  | Stereo pan; must be a finite number between -1 and 1. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.ducking.set`

Tier `edit_motion` · mutates: yes

**Purpose:** Set ducking on an audio layer so final renders lower it under trigger layers. Default mode 'timed' lowers the layer across each trigger's time window (level-independent); mode 'sidechain' uses true level-dependent FFmpeg sidechaincompress (threshold/ratio/attack/release) so it only ducks while the trigger audio is actually loud. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `triggerLayerIds` | `array` | yes | `triggers` |  | Layer ids whose audio ducks this layer. Must be a non-empty array of strings. |
| `mode` | `string` | no |  | `timed`, `sidechain` | Ducking mode; timed when omitted. |
| `duckToVolume` | `number` | no |  |  | Volume held while ducking. |
| `attackMs` | `number` | no |  |  | Milliseconds to reach the ducked volume. |
| `releaseMs` | `number` | no |  |  | Milliseconds to return to full volume. |
| `threshold` | `number` | no |  |  | Sidechain compressor threshold. Only meaningful with mode sidechain. |
| `ratio` | `number` | no |  |  | Sidechain compressor ratio. Only meaningful with mode sidechain. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.layer.track.assign`

Tier `edit_motion` · mutates: yes

**Purpose:** Assign or reorder a layer on a timeline track while keeping layer track refs and track layer order consistent. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `trackId` | `string` | yes | `track` |  | Target track id. |
| `index` | `number` | no |  |  | Position inside the destination track's layer list; appended when omitted. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.caption.import`

Tier `edit_motion` · mutates: yes

**Purpose:** Parse SRT, VTT, or plain captions into typed caption layers on a timeline caption track. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `captionsPath` | `string` | no | `captionsFile`, `path` |  | Caption file inside a host-approved authoring input root. Required unless captionsText is given. |
| `captionsText` | `string` | no | `source` |  | Inline caption text, in place of captionsPath. |
| `format` | `string` | no |  | `srt`, `vtt`, `plain` | Caption source format; inferred from the file extension when omitted. |
| `layerPrefix` | `string` | no |  |  | Prefix for generated caption layer ids. At most 123 raw code units; Core reserves the generated cue suffix separately. |
| `trackId` | `string` | no | `track` |  | Existing track to place caption layers on. |
| `trackName` | `string` | no |  |  | Name for a caption track created when trackId is omitted. |
| `transform` | `object` | no |  |  | Transform applied to every caption layer, for example { y: 900 }. |
| `style` | `object` | no |  |  | Style applied to every caption layer, for example { fontSize: 42, color: "#fff" }. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.caption.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace one caption layer with text, timing, style, and caption track evidence. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `id` | `string` | yes | `layerId`, `layer` |  | Caption layer id; an existing caption with this id is replaced. |
| `text` | `string` | yes |  |  | Caption text; must not be empty. |
| `startMs` | `number` | yes |  |  | Caption start in milliseconds. |
| `durationMs` | `number` | yes |  |  | Caption duration in milliseconds; must be positive. |
| `trackId` | `string` | no | `track` |  | Existing track to place caption layers on. |
| `trackName` | `string` | no |  |  | Name for a caption track created when trackId is omitted. |
| `transform` | `object` | no |  |  | Transform applied to every caption layer, for example { y: 900 }. |
| `style` | `object` | no |  |  | Style applied to every caption layer, for example { fontSize: 42, color: "#fff" }. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.transition.upsert`

Tier `edit_motion` · mutates: yes

**Purpose:** Insert or replace a layer enter or exit transition without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `edge` | `string` | yes |  | `in`, `out` | Which end of the layer the transition applies to. |
| `type` | `string` | yes |  | `fade`, `slide`, `wipe` | Transition kind. |
| `durationMs` | `number` | yes |  |  | Transition length in milliseconds; must be positive. |
| `easing` | `string` | no |  |  | Named Motion easing or a bounded functional cubic-bezier(...)/steps(...) string (at most 256 UTF-16 code units). |
| `direction` | `string` | no |  | `left`, `right`, `up`, `down` | Optional direction for slide and wipe transitions. |
| `distance` | `number` | no |  |  | Optional travel distance in pixels for slide transitions. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.timeline.transition.delete`

Tier `edit_motion` · mutates: yes

**Purpose:** Remove a layer enter or exit transition without hand-authoring JSON pointer patches. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `layerId` | `string` | yes | `layer` |  | Target layer id. |
| `edge` | `string` | yes |  | `in`, `out` | Which end of the layer to clear. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

## workspace (12)

| Command | Required tier | Mutates |
|---|---|---:|
| [`motion.packages.browse`](#motionpackagesbrowse) | `read_motion` | no |
| [`motion.package.create`](#motionpackagecreate) | `write_local` | yes |
| [`motion.package.asset.import`](#motionpackageassetimport) | `edit_motion` | yes |
| [`motion.package.validate`](#motionpackagevalidate) | `read_motion` | no |
| [`motion.receipts.list`](#motionreceiptslist) | `read_motion` | no |
| [`motion.receipts.read`](#motionreceiptsread) | `read_motion` | no |
| [`motion.receipts.panel`](#motionreceiptspanel) | `read_motion` | no |
| [`motion.package.archive`](#motionpackagearchive) | `write_local` | yes |
| [`motion.package.extract`](#motionpackageextract) | `write_local` | yes |
| [`motion.review.html.bundle`](#motionreviewhtmlbundle) | `write_local` | yes |
| [`motion.support.bundle`](#motionsupportbundle) | `write_local` | yes |
| [`motion.package.patch`](#motionpackagepatch) | `edit_motion` | yes |

### `motion.packages.browse`

Tier `read_motion` · mutates: no

**Purpose:** Browse local Motion packages with package metadata, template availability, asset counts, and suggested follow-up actions.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `root` | `string` | no |  |  | Directory to scan for Motion packages. One of root, packageRoot, or packageRoots is required. |
| `packageRoot` | `string` | no |  |  | Single Motion package root to include. |
| `packageRoots` | `array` | no |  |  | Motion package roots to include, as a string array. |
| `packagesRoot` | `string` | no |  |  | Alternate name for root. |
| `packageBrowserRoot` | `string` | no |  |  | Alternate name for root. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.package.create`

Tier `write_local` · mutates: yes

**Purpose:** Create a new, valid, renderable Motion package from nothing. The first step when building something original rather than importing it.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Empty or non-existent directory to create the package in. |
| `name` | `string` | no |  |  | Human-readable name, at most 128 characters; also seeds the readable half of the package and motion ids. |
| `width` | `number` | no |  |  | Frame width in pixels, 1 to 7680. Width x height may not exceed 33177600 pixels (7680x4320). Defaults to `1920`. |
| `height` | `number` | no |  |  | Frame height in pixels, 1 to 7680. Width x height may not exceed 33177600 pixels (7680x4320). Defaults to `1080`. |
| `fps` | `number` | no |  |  | Frames per second, 1 to 120. Defaults to `30`. |
| `durationMs` | `number` | no |  |  | Total duration in milliseconds. Bounded with fps by the render budget: at most 36000 frames and 80000000000 pixel-frames, so at 30 fps the longest package is 1200000ms. Defaults to `5000`. |
| `background` | `string` | no |  |  | Document background. Must be a colour Motion renders: hex (#rgb, #rgba, #rrggbb, #rrggbbaa), rgb()/rgba()/hsl()/hsla(), transparent, currentColor, or one of these names: black, white, red, green, blue, navy, yellow, cyan, aqua, magenta, fuchsia, gray, grey, silver, maroon, purple, olive, lime, teal, orange, pink, brown. |
| `empty` | `boolean` | no |  |  | Start with no layers. Off by default because a blank frame is indistinguishable from a failed render. Defaults to `false`. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.package.asset.import`

Tier `edit_motion` · mutates: yes

**Purpose:** Copy one host-approved external file into an existing package revision under assets/ without a template-specific binding. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional author identity recorded in the package-local import receipt. |
| `assetPath` | `string` | yes | `source`, `inputPath` |  | Host-approved external regular file to copy into the new package revision. The engine refuses sources over 64 MiB before allocating file bytes; request arguments cannot widen that ceiling. |
| `assetRef` | `string` | no |  |  | Optional portable target under assets/. Defaults to assets/ plus the source filename; an existing file is never replaced. |
| `createdAt` | `string` | no |  |  | Optional deterministic ISO timestamp for the package-local import receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.package.validate`

Tier `read_motion` · mutates: no

**Purpose:** Check a package is structurally valid without rendering it — identity, layers, dimensions, hosts and lanes.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root to check for structural validity. |
| `receiptsRoot` | `string` | no |  |  | Optional governed host receipt destination, outside packageRoot. Caller paths are fenced to host-approved roots. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.receipts.list`

Tier `read_motion` · mutates: no

**Purpose:** On Linux, list identity-stable retained receipt records from a trusted host root as historical evidence; it is not live job state, queue, or progress.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | yes |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.receipts.read`

Tier `read_motion` · mutates: no

**Purpose:** On Linux, read identity-stable host-owned evidence for the operation.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | no |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. Required with receiptId, and required to bound receiptPath. |
| `receiptPath` | `string` | no | `path` |  | Receipt file path; must resolve inside receiptsRoot. |
| `receiptId` | `string` | no | `id` |  | Receipt id to look up inside receiptsRoot. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.receipts.panel`

Tier `read_motion` · mutates: no

**Purpose:** On Linux, summarize stable host-owned receipts for a panel with recent activity, warnings, failures, and artifacts.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `receiptsRoot` | `string` | yes |  |  | Trusted host receipt root. Identity-stable receipt reads and receipt-derived controls currently require Linux; macOS and Windows return capability_unavailable before reading or writing receipt state. |
| `limit` | `number` | no |  |  | Maximum receipts to return; must be a non-negative integer. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.package.archive`

Tier `write_local` · mutates: yes

**Purpose:** Export the current Motion package as a deterministic portable .shellxmotion archive with receipt evidence.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Motion package root directory to read. |
| `archivePath` | `string` | yes | `outPath`, `out` |  | Destination path for the portable .shellxmotion archive. |
| `receiptPath` | `string` | no |  |  | Optional explicit path for the emitted archive receipt. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.package.extract`

Tier `write_local` · mutates: yes

**Purpose:** Extract a portable .shellxmotion archive into a local Motion package with receipt evidence.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `archivePath` | `string` | yes | `inPath`, `archive` |  | Portable .shellxmotion archive to extract. |
| `packageRoot` | `string` | yes | `outDir`, `out` |  | Trusted output Motion package directory. |
| `receiptPath` | `string` | no |  |  | Optional package archive extraction receipt path. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.review.html.bundle`

Tier `write_local` · mutates: yes

**Purpose:** Collect render, preview, quality, and receipt evidence into a portable HTML review bundle only in trusted absent-or-empty scratch, from host receipt roots.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | no |  |  | Optional Motion package root for package summary metadata. |
| `outDir` | `string` | yes |  |  | Trusted empty output directory for the portable review HTML bundle. |
| `receiptsRoot` | `string` | no |  |  | Host receipts root to collect render, batch, quality, and connector evidence. |
| `title` | `string` | no |  |  | Optional review title shown in the generated HTML. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.support.bundle`

Tier `write_local` · mutates: yes

**Purpose:** Collect redacted local diagnostics for support handoff only in trusted absent scratch, summarizing optional host receipt-root evidence.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | no |  |  | Optional Motion package root to include in diagnostics. |
| `outDir` | `string` | yes |  |  | Linux-only trusted absent output directory for the support bundle. Exact closed-tree publication is unavailable on macOS and Windows, which refuse before creating output state. |
| `receiptsRoot` | `string` | no |  |  | Optional host receipts root to summarize. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

### `motion.package.patch`

Tier `edit_motion` · mutates: yes

**Purpose:** Apply a validated copy-on-write Motion package patch; the source package remains unchanged. Reads the source and writes the separate revision only within host-approved authoring roots; outDir must be outside the source and empty or absent, and the source package remains unchanged.

| Argument | Type | Required | Aliases | Allowed values | Description |
|---|---|---|---|---|---|
| `packageRoot` | `string` | yes |  |  | Source Motion package root; never modified in place. |
| `outDir` | `string` | yes | `packageDir` |  | Empty or absent output directory, outside packageRoot, that receives the edited package copy. |
| `receiptsRoot` | `string` | no |  |  | Optional trusted host receipt mirror; the receipt is also written into outDir/receipts. |
| `createdBy` | `string` | no |  |  | Optional attribution recorded in the emitted receipt. |
| `patch` | `array` | yes | `operations` |  | JSON-Patch style operations applied to the Motion document, each { op, path, value? } with a leading-slash pointer path. |

Any other argument is **rejected**: the call fails with `invalid_args` and the command does not run.

## Argument value enumerations

Shared value sets referenced by the `Allowed values` column above. Published as `argEnums` in `schemas/debug.json`.

### `keyframeTarget`

Animatable keyframe target paths accepted by every motion.timeline.keyframe.* command.

`transform.x`, `transform.y`, `transform.width`, `transform.height`, `transform.originX`, `transform.originY`, `transform.scale`, `transform.rotation`, `opacity`, `volume`, `pan`, `blendMode`, `playbackRate`, `fill`, `style.fill`, `style.color`, `style.stroke`, `style.borderColor`, `style.backgroundColor`, `style.background`, `style.strokeWidth`, `style.borderWidth`, `style.fontSize`, `style.fontWeight`, `style.letterSpacing`, `style.textAlign`, `style.verticalAlign`, `style.alignY`, `style.lineHeight`, `style.width`, `style.height`, `style.radius`, `style.borderRadius`, `style.padding`, `style.paddingX`, `style.paddingY`, `style.paddingTop`, `style.paddingRight`, `style.paddingBottom`, `style.paddingLeft`, `mask.inset.top`, `mask.inset.right`, `mask.inset.bottom`, `mask.inset.left`, `crop.x`, `crop.y`, `crop.width`, `crop.height`, `style.shadow.x`, `style.shadow.y`, `style.shadow.offsetX`, `style.shadow.offsetY`, `style.shadow.blur`, `style.shadow.spread`, `style.shadow.blurRadius`, `style.shadow.spreadRadius`, `style.shadow.color`, `style.textShadow.x`, `style.textShadow.y`, `style.textShadow.offsetX`, `style.textShadow.offsetY`, `style.textShadow.blur`, `style.textShadow.blurRadius`, `style.textShadow.color`, `effects.blur`, `effects.brightness`, `effects.contrast`, `effects.saturate`, `effects.grayscale`, `effects.glow.radius`, `effects.glow.color`, `pathReveal.start`, `pathReveal.end`, `gradient.angle`, `environment.intensity`, `environment.wind`, `environment.dropSpeed`, `environment.dropLength`, `environment.ground.horizon`, `environment.ground.wetness`, `environment.ground.roughness`, `environment.ground.rippleAmount`, `environment.ground.splashAmount`, `environment.ground.reflectionStrength`, `environment.atmosphere.mist`, `environment.atmosphere.lensDroplets`, `environment.surface.horizon`, `environment.surface.waveScale`, `environment.surface.waveHeight`, `environment.surface.waveSpeed`, `environment.surface.direction`, `environment.surface.choppiness`, `environment.optics.reflectionStrength`, `environment.optics.refractionStrength`, `environment.optics.fresnel`, `environment.optics.caustics`, `environment.optics.clarity`, `environment.optics.foam`, `environment.fall.intensity`, `environment.fall.speed`, `environment.fall.wind`, `environment.fall.turbulence`, `environment.fall.flakeSize`, `environment.fall.focusFalloff`, `environment.ground.accumulation`, `environment.ground.drift`, `environment.ground.contactAmount`, `environment.atmosphere.haze`, `environment.atmosphere.depthFade`, `environment.fog.density`, `environment.fog.speed`, `environment.fog.scale`, `environment.fog.turbulence`, `environment.fog.height`, `environment.fog.lightStrength`

### `easing`

Named easings and spring preset aliases. Easing argument schemas also publish bounded cubic-bezier(x1,y1,x2,y2) and steps(count,start|end) grammar, plus the closed spring object { type: "spring", stiffness, damping, mass?, initialVelocity? }.

`linear`, `hold`, `ease-in`, `ease-out`, `ease-in-out`, `back-out`, `bounce-out`, `step-start`, `step-end`, `spring-gentle`, `spring-snappy`, `spring-bouncy`

### `easingPreset`

Easing preset ids returned by motion.timeline.easing.presets.

`linear`, `hold`, `ease-in`, `ease-out`, `ease-in-out`, `back-out`, `bounce-out`, `smooth`, `snappy`, `step-start`, `step-end`, `steps-4-end`, `spring-gentle`, `spring-snappy`, `spring-bouncy`

### `animationPreset`

Animation preset ids accepted by motion.timeline.animation.preset.apply.

`fade-in`, `fade-out`, `slide-up-in`, `slide-down-out`, `lower-third-in`, `lower-third-out`

### `transitionPreset`

Transition preset ids accepted by motion.timeline.transition.preset.apply.

`soft-fade`, `slide-cover`, `wipe-accent`, `card-stack`, `push-zoom`, `scan-sweep`, `split-reveal`

### `exportPreset`

Export preset ids accepted by render, batch, and export planning commands.

`mp4-h264`, `mp4-hevc`, `webm-av1`, `webm-vp9`, `webm-vp9-alpha`, `gif`, `mov-prores`, `png-sequence`, `png-frame`, `jpeg-frame`

### `blendMode`

Layer blend modes accepted by motion.timeline.layer.blend.set and layer.blendMode.

`normal`, `multiply`, `screen`, `overlay`, `darken`, `lighten`, `color-dodge`, `color-burn`, `hard-light`, `soft-light`, `difference`, `exclusion`, `hue`, `saturation`, `color`, `luminosity`, `plus-lighter`

### `mediaFit`

Media fit modes accepted by motion.timeline.layer.fit.set for image and video layers.

`fill`, `contain`, `cover`, `none`, `scale-down`

### `transitionType`

Layer transition types accepted by motion.timeline.transition.upsert.

`fade`, `slide`, `wipe`

### `transitionDirection`

Directional hint for slide and wipe transitions.

`left`, `right`, `up`, `down`

### `transitionEdge`

Which end of a layer a transition applies to.

`in`, `out`

### `captionFormat`

Caption source formats accepted by motion.timeline.caption.import.

`srt`, `vtt`, `plain`

### `keyframeSnapMode`

Rounding mode used by motion.timeline.keyframe.snap.

`nearest`, `floor`, `ceil`

### `duckingMode`

Audio ducking mode; sidechain enables the threshold and ratio knobs.

`timed`, `sidechain`

### `durationResizeMode`

How motion.timeline.duration.policy.set reflows content when the duration changes.

`stretch-middle`, `ripple`, `fixed`

### `deliveryLane`

CLI delivery lanes (shellx-motion render --lane). These are NOT frame rasterizer lanes: --lane does not accept browser. To rasterize frames in the browser, keep --lane ffmpeg and pass --frame-lane browser. The Debug API has no delivery-lane argument; it takes frameLane on motion.render.final.

`native`, `ffmpeg`

### `spatialTangentMode`

Spatial path tangent modes accepted in the spatial object of motion.timeline.spatial.position.upsert.

`linear`, `smooth`, `broken`, `auto`

### `permissionTier`

Motion permission tiers, ordered from least to most privileged.

`read_motion`, `draft_motion`, `render_motion`, `edit_motion`, `write_local`, `push_remote`
