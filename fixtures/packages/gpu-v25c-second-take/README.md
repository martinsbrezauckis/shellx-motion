# Second Take — V25-C3 source fixture

This generated source package is an original 1080x1920, 8-second, 30fps creator composition. Its `assets/second-take-subject.svg` is a transparent, reviewable SVG authored inline by [`templates/generators/second-take/recipe.json`](../../../templates/generators/second-take/recipe.json); `assets/second-take-copy.svg` is the recipe's deterministic original 5x7 path overlay. Neither contains `<text>`, an embedded font, an external reference, executable content, or a network source. No imported or copyrighted media, raster binary, video, audio, or font file is present.

`subject-stage` is the isolated subject group and contains only the transparent vector subject. Its final direct child, `subject-afterimage`, carries a closed `effectModule` reference to installed `motion.afterimage-stack@1.0.0`: four integer-offset, Q16-opacity coral/cobalt/cream echoes and one Q16 amount. The generator-root-only install manifest selects fixed intrinsic `motion.afterimage-stack.v1`; it is not a package asset. The opaque card and aura precede the group, while the keyline, original vector typography, data pills, and bounded particles are later root layers, so they are intentionally not afterimaged or dependent on host-font selection.

This is V25-C3 source evidence only. It asserts generator parity, semantic validation, and installed-registry Browser static admission. It does not assert module runtime execution, segmented delivery, native visual quality, final media, receipt, replay, or cleanup acceptance. The module-off comparison package is generated externally as a scratch-only baseline; see the generator README.
