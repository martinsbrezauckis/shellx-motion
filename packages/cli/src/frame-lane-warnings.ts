// Kept as a CLI-local entry for existing imports; the implementation belongs to the shared
// delivery renderer because the typed Debug API must apply identical receipt semantics.
export { FrameLaneWarnings, type ResolvedAudioHandoff } from "@shellx-motion/renderer-ffmpeg/frame-lane-warnings";
