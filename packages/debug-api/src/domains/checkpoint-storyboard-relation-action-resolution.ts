/** C6C B4a private resolver. Debug/MCP owns host authority and the signed durable link. */
import { lstat } from "node:fs/promises";
import { canonicalJson, isPublicationCommitUncertain } from "@shellx-motion/core";
import { materializeCheckpointStoryboardRelationAction, prepareCheckpointStoryboardRelationActionMaterialization, reopenCheckpointStoryboardRelationActionMaterializationOutput } from "./checkpoint-storyboard-relation-action-materialize-private/checkpoint-storyboard-relation-action-materialize-private.js";
import { readC6B4bReceipt, type C6B4bExactBase } from "./checkpoint-storyboard-relation-action-materialize-private/checkpoint-storyboard-relation-action-materialize-receipt-private.js";
import { assertCheckpointStoryboardRelationActionResolutionAuthorityStore, checkpointStoryboardRelationActionResolutionOutputHandle, checkedCheckpointStoryboardRelationActionResolutionAuthority, withCheckpointStoryboardRelationActionResolutionAuthority, withCheckpointStoryboardRelationActionResolutionOutputAuthority, type CheckpointStoryboardRelationActionResolutionAuthority } from "./checkpoint-storyboard-relation-action-resolution-authority.js";
import { createRelationActionAbandon, createRelationActionBinding, createRelationActionCowStart, createRelationActionDetach, createRelationActionIntent, publishRelationActionAbandon, publishRelationActionBinding, publishRelationActionCowStart, publishRelationActionDetach, publishRelationActionIntent, publishRelationActionStateHead, readRelationActionBinding, readRelationActionCowStart, readRelationActionIntent, readRelationActionResolutionState, relationActionStateHead, type RelationActionBinding, type RelationActionIntent, type RelationActionPlanIdentity } from "./checkpoint-storyboard-relation-action-resolution-journal.js";
import { assertNoLegacyMaterializationEvidence } from "./checkpoint-storyboard-materialization-bindings.js";
import { assertNoBehaviorResolutionEvidence } from "./checkpoint-storyboard-behavior-resolution-journal.js";
import { assertNoRelationResolutionEvidence } from "./checkpoint-storyboard-relation-resolution-journal.js";
import { assertNoLifecycleResolutionEvidence } from "./checkpoint-storyboard-lifecycle-resolution-journal.js";
import { assertNoGeometryMorphResolutionEvidence } from "./checkpoint-storyboard-geometry-morph-resolution-journal.js";
import { assertNoRetainedTraceResolutionEvidence } from "./checkpoint-storyboard-retained-trace-resolution-journal.js";
import { checkedAuthority, withLineageLock } from "./checkpoint-storyboard-record-store-authority.js";
import { readImmutableRecordRoot, readStoredRecordUnlocked } from "./checkpoint-storyboard-record-store-state.js";
import { CheckpointStoryboardRecordStoreError, sameIdentity, storeError, type CheckpointStoryboardRecordIdentity } from "./checkpoint-storyboard-record-store-types.js";

type FaultPhase = "while-lineage-lock-held" | "after-intent-before-state-head" | "after-intent" | "after-cow-start-before-state-head" | "after-cow-start" | "before-c6b4b" | "after-c6b4b-commit" | "after-binding" | "after-bound-state-head-rename" | "after-detach" | "after-detached-state-head-rename" | "after-abandon";
type FaultHooks = Partial<Record<FaultPhase, () => void | Promise<void>>>;
const hooks = new WeakMap<CheckpointStoryboardRelationActionResolutionAuthority, FaultHooks>();
/** Test-only seam; it is intentionally absent from command registry and public exports. */
export function setCheckpointStoryboardRelationActionResolutionFaultHooksForTest(authority: CheckpointStoryboardRelationActionResolutionAuthority, value: FaultHooks | undefined): void { checkedCheckpointStoryboardRelationActionResolutionAuthority(authority); if (value) hooks.set(authority, Object.freeze({ ...value })); else hooks.delete(authority); }
async function fault(authority: CheckpointStoryboardRelationActionResolutionAuthority, phase: FaultPhase): Promise<void> { await hooks.get(authority)?.[phase]?.(); }

export interface CheckpointStoryboardRelationActionResolutionResult { readonly identity: CheckpointStoryboardRecordIdentity; readonly binding: { readonly state: "bound" | "detached"; readonly active: 0 | 1; readonly bindingId: string; readonly outputHandle: string; readonly receiptFingerprint: string }; readonly renderer: { readonly invoked: false; readonly pixels: false }; readonly replayed: boolean; }

export async function resolveCheckpointStoryboardRelationActionStoredRecord(authority: CheckpointStoryboardRelationActionResolutionAuthority, identity: CheckpointStoryboardRecordIdentity): Promise<CheckpointStoryboardRelationActionResolutionResult> {
  try { return await withCheckpointStoryboardRelationActionResolutionOutputAuthority(authority, async (host) => {
    const store = checkedAuthority(host.store), root = await readImmutableRecordRoot(store, identity);
    return await withLineageLock(store, root.id, async () => {
      const record = await readStoredRecordUnlocked(store, identity); assertB4Record(record); assertCheckpointStoryboardRelationActionResolutionAuthorityStore(authority, host.store); await assertForeignPartitionEvidenceAbsent(store, identity);
      const outputHandle = checkpointStoryboardRelationActionResolutionOutputHandle(authority, identity); const state = await readRelationActionResolutionState(store, identity, root, { requireHead: true, allowForwardRecovery: true }); await fault(authority, "while-lineage-lock-held");
      if (state.state === "abandoned") throw storeError("materialization_binding_conflict", "Checkpoint storyboard relation-action resolution was terminally abandoned after a proved no-install outcome.");
      if (state.state === "detached") throw storeError("materialization_detached", "Checkpoint storyboard relation-action resolution has terminally detached its link and cannot resolve again.");
      if (state.state === "bound") return await replayBound(store, host, identity, root, state.bindingId!, outputHandle);
      const prior = await readRelationActionIntent(store, identity);
      if (prior) { const recovered = await recognizeInstalledOutput(host, record.identity, prior.expectedBase, prior.plan); if (recovered) return await publishAndReopen(store, host, authority, identity, root, prior, recovered, outputHandle, true); if (await outputExists(host.outputPackageRoot) || await readRelationActionCowStart(store, identity)) throw storeError("materialization_binding_uncertain", "Checkpoint storyboard relation-action resolution retains an occupied or COW-uncertain output for exact recovery and will not repeat COW."); }
      else if (await outputExists(host.outputPackageRoot)) throw storeError("materialization_binding_uncertain", "Checkpoint storyboard relation-action output is occupied without a durable intent and was retained without replacement.");
      return await withCheckpointStoryboardRelationActionResolutionAuthority(authority, async (sourceHost) => {
        const c6bHost = { sourcePackageRoot: sourceHost.sourcePackageRoot, outputPackageRoot: sourceHost.outputPackageRoot, packageWorkspaceRoot: sourceHost.packageWorkspaceRoot, packageWorkspaceAuthority: sourceHost.packageWorkspaceAuthority };
        assertTwoCatalogOrderBindings(record.storyboard.objectCatalog, sourceHost.objectLayerBindings);
        // B4b canonicalizes source and absent output, including intermediate aliases, before
        // it mints approval or B4a publishes a durable output intent.
        const prepared = await prepareCheckpointStoryboardRelationActionMaterialization(c6bHost, record.storyboard, sourceHost.objectLayerBindings), plan = planIdentity(prepared.plan), intent = createRelationActionIntent({ identity, root, plan, expectedBase: prepared.expected, outputHandle });
        if (prior && canonicalJson(prior) !== canonicalJson(intent)) throw storeError("materialization_binding_conflict", "Checkpoint storyboard action intent conflicts with exact host-selected C6B4 facts.");
        if (!prior) { await publishRelationActionIntent(store, intent); await fault(authority, "after-intent-before-state-head"); await publishRelationActionStateHead(store, relationActionStateHead(identity, root, "preparing", 0, { intent: ref(intent) })); await fault(authority, "after-intent"); }
        if (await outputExists(sourceHost.outputPackageRoot)) throw storeError("materialization_binding_uncertain", "Checkpoint storyboard relation-action output appeared after intent publication and was retained without replacement.");
        const start = createRelationActionCowStart({ identity, root, intent: ref(intent) }); await publishRelationActionCowStart(store, start); await fault(authority, "after-cow-start-before-state-head"); await publishRelationActionStateHead(store, relationActionStateHead(identity, root, "preparing", 0, { intent: ref(intent), cowStart: ref(start) })); await fault(authority, "after-cow-start");
        try { await fault(authority, "before-c6b4b"); await materializeCheckpointStoryboardRelationAction(c6bHost, prepared.approval, { schema: "shellx-motion/private-checkpoint-storyboard-relation-action-materialization-request@1", expected: prepared.expected }); }
        catch (error) { const recovered = await recognizeInstalledOutput(sourceHost, record.identity, prepared.expected, plan); if (recovered) return await publishAndReopen(store, sourceHost, authority, identity, root, intent, recovered, outputHandle, true); if (isPublicationCommitUncertain(error) || await outputExists(sourceHost.outputPackageRoot)) throw storeError("materialization_binding_uncertain", "Checkpoint storyboard relation-action publication may have committed; output was retained for exact recovery."); const abandon = createRelationActionAbandon({ identity, root, intent: ref(intent), reason: "proven-no-install" }); await publishRelationActionAbandon(store, abandon); await fault(authority, "after-abandon"); await publishRelationActionStateHead(store, relationActionStateHead(identity, root, "abandoned", 0, { intent: ref(intent), cowStart: ref(start), abandon: ref(abandon) })); throw storeError("materialization_binding_conflict", "Checkpoint storyboard relation-action COW refused before a recognized output was installed."); }
        await fault(authority, "after-c6b4b-commit"); const installed = await recognizeInstalledOutput(sourceHost, record.identity, prepared.expected, plan); if (!installed) throw storeError("materialization_binding_uncertain", "Checkpoint storyboard relation-action COW completed without an exact output reopen; output was retained."); return await publishAndReopen(store, sourceHost, authority, identity, root, intent, installed, outputHandle, false);
      });
    });
  }); } catch (error) { throw sanitized(error); }
}

export async function detachCheckpointStoryboardRelationActionStoredRecord(authority: CheckpointStoryboardRelationActionResolutionAuthority, identity: CheckpointStoryboardRecordIdentity): Promise<CheckpointStoryboardRelationActionResolutionResult> {
  try { return await withCheckpointStoryboardRelationActionResolutionOutputAuthority(authority, async (host) => {
    const store = checkedAuthority(host.store), root = await readImmutableRecordRoot(store, identity);
    return await withLineageLock(store, root.id, async () => {
      const record = await readStoredRecordUnlocked(store, identity); assertB4Record(record); assertCheckpointStoryboardRelationActionResolutionAuthorityStore(authority, host.store); await assertForeignPartitionEvidenceAbsent(store, identity);
      const outputHandle = checkpointStoryboardRelationActionResolutionOutputHandle(authority, identity); let state = await readRelationActionResolutionState(store, identity, root, { requireHead: true, allowForwardRecovery: true });
      if (state.state === "unbound" || state.state === "abandoned") throw storeError("materialization_not_bound", "Checkpoint storyboard relation-action record has no bound output to detach.");
      if (state.state === "preparing") { const intent = await readRelationActionIntent(store, identity), start = await readRelationActionCowStart(store, identity); if (intent) { const recovered = await recognizeInstalledOutput(host, record.identity, intent.expectedBase, intent.plan); if (recovered) { await publishAndReopen(store, host, authority, identity, root, intent, recovered, outputHandle, true); state = await readRelationActionResolutionState(store, identity, root, { requireHead: true, allowForwardRecovery: true }); } } if (state.state === "preparing" && intent && !start && !(await outputExists(host.outputPackageRoot))) { const abandon = createRelationActionAbandon({ identity, root, intent: ref(intent), reason: "no-cow-start" }); await publishRelationActionAbandon(store, abandon); await publishRelationActionStateHead(store, relationActionStateHead(identity, root, "abandoned", 0, { intent: ref(intent), abandon: ref(abandon) })); throw storeError("materialization_not_bound", "Checkpoint storyboard relation-action intent was abandoned before COW start; no output was deleted."); } if (state.state === "preparing") throw storeError("materialization_binding_uncertain", "Checkpoint storyboard relation-action resolution is COW-uncertain; output was retained and cannot be detached yet."); }
      const binding = await readRelationActionBinding(store, identity); if (!binding) throw storeError("store_integrity_failed", "Checkpoint storyboard relation-action state lost immutable binding.");
      if (state.state === "detached") { await verifyBinding(host, binding, identity, outputHandle); return result(identity, binding, "detached", true); }
      await verifyBinding(host, binding, identity, outputHandle); const detach = createRelationActionDetach({ identity, root, binding: ref(binding) }); await publishRelationActionDetach(store, detach); await fault(authority, "after-detach"); const intent = await readRelationActionIntent(store, identity), start = await readRelationActionCowStart(store, identity); if (!intent || !start) throw storeError("store_integrity_failed", "Checkpoint storyboard relation-action detach lost required intent or COW start."); await publishRelationActionStateHead(store, relationActionStateHead(identity, root, "detached", 0, { intent: ref(intent), cowStart: ref(start), binding: ref(binding), detach: ref(detach) }), async () => await fault(authority, "after-detached-state-head-rename")); const final = await readRelationActionResolutionState(store, identity, root, { requireHead: true, allowForwardRecovery: true }); if (final.state !== "detached" || final.bindingId !== binding.id) throw storeError("materialization_binding_uncertain", "Checkpoint storyboard relation-action detach publication could not be reopened exactly."); return result(identity, binding, "detached", false);
    });
  }); } catch (error) { throw sanitized(error); }
}

function assertB4Record(record: Awaited<ReturnType<typeof readStoredRecordUnlocked>>): void { if (record.archive.terminal) throw storeError("lineage_archived", "Checkpoint storyboard lineage is terminally archived."); if (record.target.state === "tombstoned") throw storeError("record_tombstoned", "A tombstoned checkpoint storyboard record cannot resolve relation actions."); if (record.admission.profile !== "c6b4-relation-action@1") throw storeError("materialization_profile_refused", "C6C B4a action resolution accepts only a sealed C6B4 relation-action profile record."); }
async function assertForeignPartitionEvidenceAbsent(store: ReturnType<typeof checkedAuthority>, identity: CheckpointStoryboardRecordIdentity): Promise<void> { await assertNoLegacyMaterializationEvidence(store, identity); await assertNoBehaviorResolutionEvidence(store, identity); await assertNoRelationResolutionEvidence(store, identity); await assertNoLifecycleResolutionEvidence(store, identity); await assertNoGeometryMorphResolutionEvidence(store, identity); await assertNoRetainedTraceResolutionEvidence(store, identity); }
function assertTwoCatalogOrderBindings(catalog: readonly { readonly objectId: string }[], bindings: readonly { readonly objectId: string; readonly layerId: string }[]): void { if (catalog.length !== 2 || bindings.length !== 2 || bindings.some((binding, index) => binding.objectId !== catalog[index]!.objectId || binding.layerId !== binding.objectId)) throw storeError("materialization_authority_refused", "Checkpoint storyboard action resolution requires exactly two catalog-order same-ID object/layer bindings."); }
function planIdentity(plan: Awaited<ReturnType<typeof prepareCheckpointStoryboardRelationActionMaterialization>>["plan"]): RelationActionPlanIdentity { return Object.freeze({ planFingerprint: plan.fingerprint, profileFingerprint: plan.lowererProfile.fingerprint, actionStoreSchema: plan.projection.action.store.schema, actionStoreSha256: plan.projection.action.store.sha256, actionDefinitionId: plan.projection.action.definition.id, actionDefinitionSha256: plan.projection.action.definition.sha256, actionRequestInstanceId: plan.projection.action.request.instanceId, actionRequestSha256: plan.projection.action.request.sha256, actionApplyPlanFingerprint: plan.projection.action.applyPlan.fingerprint, actionObjects: 0, actionRelations: 1, actionKeyframeWrites: 0, actionChangedPath: plan.projection.action.changedPaths[0]!, actionOutputCanonicalMotionSha256: plan.projection.action.outputCanonicalMotionSha256, relationId: plan.projection.action.relationIds[0], storeSha256: plan.projection.storeSha256, staticFingerprint: plan.projection.staticFingerprint, gpuStaticFingerprint: plan.projection.gpuPreviewStaticPlan.fingerprint, startFramePlanFingerprint: plan.endpointFramePlans.start.fingerprint, endFramePlanFingerprint: plan.endpointFramePlans.end.fingerprint }); }
function ref(value: { readonly id: string; readonly sha256: string }) { return Object.freeze({ id: value.id, sha256: value.sha256 }); }
type Recognized = Readonly<{ output: Awaited<ReturnType<typeof reopenCheckpointStoryboardRelationActionMaterializationOutput>>; receipt: Awaited<ReturnType<typeof readC6B4bReceipt>>; plan: RelationActionPlanIdentity }>;
async function recognizeInstalledOutput(host: { readonly outputPackageRoot: string; readonly packageWorkspaceRoot: string; readonly packageWorkspaceAuthority: Parameters<typeof reopenCheckpointStoryboardRelationActionMaterializationOutput>[0]["packageWorkspaceAuthority"] }, identity: CheckpointStoryboardRecordIdentity, expected: C6B4bExactBase, plan: RelationActionPlanIdentity): Promise<Recognized | null> { try { const output = await reopenCheckpointStoryboardRelationActionMaterializationOutput(host), receipt = await readC6B4bReceipt(host.outputPackageRoot), action = output.relationAction; if (output.storyboard.id !== identity.id || output.storyboard.sha256 !== identity.sha256 || output.storyboard.revision !== identity.revision || output.plan.fingerprint !== plan.planFingerprint || output.profile.fingerprint !== plan.profileFingerprint || action.store.schema !== plan.actionStoreSchema || action.store.sha256 !== plan.actionStoreSha256 || action.definition.id !== plan.actionDefinitionId || action.definition.sha256 !== plan.actionDefinitionSha256 || action.request.instanceId !== plan.actionRequestInstanceId || action.request.sha256 !== plan.actionRequestSha256 || action.apply.planFingerprint !== plan.actionApplyPlanFingerprint || action.apply.objectCount !== 0 || action.apply.relationCount !== 1 || action.apply.keyframeWriteCount !== 0 || action.apply.changedPath !== plan.actionChangedPath || action.apply.outputCanonicalMotionSha256 !== plan.actionOutputCanonicalMotionSha256 || action.apply.changedPath !== `/relations/bindings/${plan.relationId}` || output.relationStore.sha256 !== plan.storeSha256 || output.relationStatic.fingerprint !== plan.staticFingerprint || output.gpuRelationStatic.fingerprint !== plan.gpuStaticFingerprint || output.endpointFramePlans.startFingerprint !== plan.startFramePlanFingerprint || output.endpointFramePlans.endFingerprint !== plan.endFramePlanFingerprint || output.receipt.fingerprint !== receipt.fingerprint || canonicalJson(receipt.base.expected) !== canonicalJson(expected) || canonicalJson(receipt.base.reopened) !== canonicalJson(expected) || receipt.approval.action.store.schema !== plan.actionStoreSchema || receipt.approval.action.store.sha256 !== plan.actionStoreSha256 || receipt.approval.action.definition.id !== plan.actionDefinitionId || receipt.approval.action.definition.sha256 !== plan.actionDefinitionSha256 || receipt.approval.action.request.instanceId !== plan.actionRequestInstanceId || receipt.approval.action.request.sha256 !== plan.actionRequestSha256 || receipt.approval.action.applyPlanFingerprint !== plan.actionApplyPlanFingerprint || receipt.approval.action.changedPath !== plan.actionChangedPath || receipt.approval.action.outputCanonicalMotionSha256 !== plan.actionOutputCanonicalMotionSha256 || receipt.approval.action.relationId !== plan.relationId || receipt.approval.relation.storeSha256 !== plan.storeSha256 || receipt.approval.relation.staticFingerprint !== plan.staticFingerprint || receipt.approval.relation.gpuStaticFingerprint !== plan.gpuStaticFingerprint || receipt.approval.relation.startFramePlanFingerprint !== plan.startFramePlanFingerprint || receipt.approval.relation.endFramePlanFingerprint !== plan.endFramePlanFingerprint || receipt.renderer.invoked !== false || receipt.renderer.pixels !== false) return null; return Object.freeze({ output, receipt, plan }); } catch { return null; } }
async function publishAndReopen(store: ReturnType<typeof checkedAuthority>, host: Parameters<typeof recognizeInstalledOutput>[0], authority: CheckpointStoryboardRelationActionResolutionAuthority, identity: CheckpointStoryboardRecordIdentity, root: CheckpointStoryboardRecordIdentity, intent: RelationActionIntent, recognized: Recognized, outputHandle: string, replayed: boolean): Promise<CheckpointStoryboardRelationActionResolutionResult> { const start = await readRelationActionCowStart(store, identity); if (!start || !sameIdentity(start.identity, identity) || !sameIdentity(start.root, root) || start.intent.id !== intent.id || start.intent.sha256 !== intent.sha256) throw storeError("store_integrity_failed", "Checkpoint storyboard relation-action binding lost durable COW-start evidence."); const binding = createRelationActionBinding({ identity, root, intent: ref(intent), plan: recognized.plan, source: Object.freeze({ expected: recognized.receipt.base.expected, reopened: recognized.receipt.base.reopened }), output: Object.freeze({ expected: recognized.output, reopened: recognized.output }), receiptFingerprint: recognized.receipt.fingerprint, outputHandle }); await publishRelationActionBinding(store, binding); await fault(authority, "after-binding"); await publishRelationActionStateHead(store, relationActionStateHead(identity, root, "bound", 1, { intent: ref(intent), cowStart: ref(start), binding: ref(binding) }), async () => await fault(authority, "after-bound-state-head-rename")); const reopened = await readRelationActionBinding(store, identity); if (!reopened || canonicalJson(reopened) !== canonicalJson(binding)) throw storeError("materialization_binding_uncertain", "Checkpoint storyboard relation-action binding publication could not be reopened exactly."); await verifyBinding(host, reopened, identity, outputHandle); return result(identity, reopened, "bound", replayed); }
async function replayBound(store: ReturnType<typeof checkedAuthority>, host: Parameters<typeof recognizeInstalledOutput>[0], identity: CheckpointStoryboardRecordIdentity, root: CheckpointStoryboardRecordIdentity, bindingId: string, outputHandle: string): Promise<CheckpointStoryboardRelationActionResolutionResult> { const binding = await readRelationActionBinding(store, identity); if (!binding || binding.id !== bindingId || !sameIdentity(binding.root, root)) throw storeError("store_integrity_failed", "Checkpoint storyboard bound action state does not match immutable binding."); await verifyBinding(host, binding, identity, outputHandle); return result(identity, binding, "bound", true); }
async function verifyBinding(host: Parameters<typeof recognizeInstalledOutput>[0], binding: RelationActionBinding, identity: CheckpointStoryboardRecordIdentity, outputHandle: string): Promise<void> { if (binding.outputHandle !== outputHandle) throw storeError("materialization_binding_conflict", "Checkpoint storyboard action binding belongs to a different authority-store output handle."); const output = await reopenCheckpointStoryboardRelationActionMaterializationOutput(host), receipt = await readC6B4bReceipt(host.outputPackageRoot); const recognized = await recognizeInstalledOutput(host, identity, binding.source.expected, binding.plan); if (!recognized || output.receipt.fingerprint !== binding.receiptFingerprint || receipt.fingerprint !== binding.receiptFingerprint || canonicalJson(output) !== canonicalJson(binding.output.expected) || canonicalJson(output) !== canonicalJson(binding.output.reopened) || canonicalJson(receipt.base.expected) !== canonicalJson(binding.source.expected) || canonicalJson(receipt.base.reopened) !== canonicalJson(binding.source.reopened)) throw storeError("materialization_binding_conflict", "Checkpoint storyboard action binding no longer reopens full B4b source, output, receipt, action, relation, static, GPU, and endpoint identity."); }
function result(identity: CheckpointStoryboardRecordIdentity, binding: RelationActionBinding, state: "bound" | "detached", replayed: boolean): CheckpointStoryboardRelationActionResolutionResult { return Object.freeze({ identity, binding: Object.freeze({ state, active: state === "bound" ? 1 as const : 0 as const, bindingId: binding.id, outputHandle: binding.outputHandle, receiptFingerprint: binding.receiptFingerprint }), renderer: Object.freeze({ invoked: false as const, pixels: false as const }), replayed }); }
async function outputExists(file: string): Promise<boolean> { try { await lstat(file); return true; } catch (error) { if (error && typeof error === "object" && "code" in error && (error as { code?: unknown }).code === "ENOENT") return false; throw storeError("materialization_binding_uncertain", "Checkpoint storyboard action output could not be safely classified as absent and was not recreated."); } }
function sanitized(error: unknown): never { if (error instanceof CheckpointStoryboardRecordStoreError) throw error; throw storeError("materialization_binding_uncertain", "Checkpoint storyboard relation-action resolution did not complete exact host/package verification; output was retained and no path evidence was returned."); }
